[project]
name = "pantryos"
version = "0.1.0"
description = "Local PantryOS app and Home Assistant custom integration for home food intelligence."
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

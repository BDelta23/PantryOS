# PantryOS release readiness

This file is generated by `python scripts/release_readiness.py --write` from the acceptance criteria and implementation status ledger.

## Decision

- Status: **NOT READY**
- Generated: `2026-08-26T21:16:31Z`
- Acceptance criteria in contract: `85`
- Open acceptance criteria: `85`
- Phase gates in ledger: `11`

## Required Release Commands

- `python scripts/check.py`
- `python scripts/smoke_e2e.py`
- `python scripts/concurrency_smoke.py`
- `node scripts/browser_smoke.cjs`
- `python scripts/container_smoke.py --isolated`
- `python scripts/receipt_ocr_corpus_smoke.py`
- `python scripts/image_hardening_audit.py`
- `python scripts/supply_chain_audit.py`
- `python scripts/ha_installed_smoke.py`
- `python scripts/ha_core_live_smoke.py`
- `python scripts/release_artifact_audit.py`
- `python scripts/manual_release_evidence.py`
- `python scripts/release_readiness.py --check`

## Open Acceptance Criteria

- A1. There is one authoritative SQLite database owned by PantryOS Core.
- A2. The web UI and Home Assistant do not maintain independent inventory state.
- A3. Adding an item in the web UI changes Home Assistant state promptly without either process restarting.
- A4. Consuming an item through a Home Assistant action changes the web UI promptly.
- A5. Restarting PantryOS and Home Assistant preserves and reconciles the same state revision.
- A6. A test with at least 20 concurrent supported mutations produces no lost successful writes and no corrupt database.
- A7. Database transactions roll back all related changes on injected failure.
- A8. Legacy JSON import is validated, backed up, atomic, and idempotent.
- A9. A backup can be restored into a clean instance and passes an integrity/summary comparison.
- B1. Two lots of the same product can have different locations, expirations, and costs.
- B2. Product minimum stock remains after all active lots reach zero.
- B3. Location rename/move does not rewrite every inventory lot or break references.
- B4. Consumption across lots follows FEFO and returns the exact allocation.
- B5. Over-consumption is rejected with available quantity unless an explicit administrative adjustment is used.
- B6. Opening a lot is idempotent and correctly applies configured opened-life policy.
- B7. Discard and waste create historical events and remove quantity from usable stock.
- B8. Inventory event history reconstructs the reason and source of every tested mutation.
- B9. Negative quantities, invalid dates, incompatible units, duplicate constrained names, and cyclic locations are rejected.
- C1. `1 lb` satisfies an `8 oz` requirement and reports the correct remainder.
- C2. Incompatible dimensions are rejected; mass-to-volume is not guessed.
- C3. Product aliases resolve recipe text to stable product IDs.
- C4. Expired/discarded lots do not satisfy recipes by default.
- C5. Recipe quantities scale correctly with servings.
- C6. Ready, missing-N, and max-time queries return deterministic explainable results.
- C7. Unknown prep time follows the documented max-time policy.
- C8. Use-soon recommendations identify which urgent lots a recipe consumes and why it ranks highly.
- D1. A weekly plan stores date, meal type, recipe ID, servings, and status.
- D2. Ingredient demand aggregates across all planned meals and subtracts usable inventory.
- D3. Rebuilding shopping demand twice produces the same generated demand, not double quantities.
- D4. Repeating “add missing” for one recipe/plan source is idempotent.
- D5. Manual demand and generated demand can coexist with visible source breakdown.
- D6. Minimum-stock suggestions remain separate until accepted.
- D7. Shopping lines can be edited, checked, unchecked, suppressed/removed, and assigned notes/store.
- D8. Completing a purchase creates purchase lines and selected inventory lots transactionally.
- D9. A user override is preserved or explicitly reconciled when generated demand is rebuilt.
- E1. Starting cooking emits an event but does not consume inventory.
- E2. Completing cooking requires confirmed allocations and is atomic.
- E3. Completing cooking can create leftover lots with servings, made time, use-by time, location, and recipe/session provenance.
- E4. Leftovers appear in the next-meal/use-soon experience.
- E5. Monthly waste is calculated from waste events and survives lot cleanup.
- E6. Inventory value by freezer/refrigerator/location is available to the API and Home Assistant.
- F1. A known barcode scan resolves a product/package and can add a lot.
- F2. An unknown barcode opens a mapping/product form; manual entry works without camera support.
- F3. Barcode mappings are unique and persist across restart.
- F4. Receipt upload enforces supported type and size limits and stores files outside the static web root.
- F5. At least one local extraction implementation works in the documented container environment.
- F6. Extracted receipt data is editable and cannot mutate inventory before confirmation.
- F7. Duplicate receipt commit is detected and does not duplicate purchase lines or inventory.
- F8. Purchase history shows store, date, package/quantity, total, and comparable unit price.
- F9. Price anomaly output uses compatible units, a documented baseline, and an explanation.
- G1. All primary inventory, recipe, plan, shopping, cooking, barcode, receipt, and settings workflows are reachable in the UI.
- G2. No visible control is a toast-only stub or hard-coded success simulation.
- G3. Loading, empty, offline, validation, conflict, and server-error states are clear.
- G4. Destructive actions require appropriate confirmation and remain keyboard accessible.
- G5. Critical workflows pass automated browser tests at phone and kitchen-tablet viewport sizes.
- G6. Forms have labels, focus handling, error association, and no critical automated accessibility violations.
- G7. PWA install metadata and an intentional offline/error strategy exist; offline writes are not falsely reported as committed.
- H1. Config flow accepts and validates base URL/token and uses the PantryOS instance ID as unique identity.
- H2. Reconfigure and authentication failure recovery are supported.
- H3. The integration uses one coordinated cache/push subscription and performs no I/O in entity properties.
- H4. Entities become unavailable when Core is unreachable and recover automatically.
- H5. Actions call the API, return useful errors/responses, and remain registered according to current HA conventions.
- H6. Existing action names are retained or have a documented compatibility/migration path.
- H7. Sensors cover total inventory, expiring soon, shopping, possible meals, leftovers, waste, and location values/counts without unbounded attributes.
- H8. Diagnostics redact token, authorization headers, session data, receipt text/images, and secrets.
- H9. HA tests cover setup, unload, auth failure, unavailable/recovery, sensor updates, and representative actions.
- H10. Example automations demonstrate use-soon notification, grocery arrival count, cooking mode, and freezer risk/value.
- I1. Unauthorized API mutation and event subscription fail.
- I2. Browser authentication/session, CSRF, CORS, origin, and cookie policy are explicit and tested.
- I3. Request body, upload size, filename/path, MIME/content, and rate limits are enforced.
- I4. No secrets appear in source, logs, client errors, diagnostics, fixtures, or generated artifacts.
- I5. Container runs as non-root and writes only intended volume paths.
- I6. Liveness and readiness have different semantics and tests.
- I7. Structured logs include request/correlation IDs without sensitive payloads.
- I8. Migration failure leaves the prior database/backup recoverable.
- I9. `pantryos doctor`, backup, restore verification, and legacy dry-run are documented and smoke-tested.
- I10. Core operation is usable without internet access after dependencies/images are installed.
- J1. Unit, integration, API, migration, concurrency, HA, browser, and container smoke suites pass from a clean checkout.
- J2. Lint, format, and type checks pass.
- J3. Domain/application code has meaningful coverage, including failure paths; the configured threshold passes without broad exclusions.
- J4. OpenAPI, user setup, Home Assistant setup, backup/restore, migration, and troubleshooting docs match actual behavior.
- J5. No completion-critical TODO, placeholder URL, fake response, disabled test, or skipped release check remains without a documented non-blocking reason.
- J6. A scripted demo proves add → sync → plan → shop → cook → leftover → use-soon across the supported surfaces.
- J7. Independent review reports no open Critical/High findings and no release-blocking Medium finding.
- J8. `docs/release/RELEASE_READINESS.md` records exact commands, results, residual risks, and a justified `PASS`.

## Open Phase Gates

- Phase 6 — barcode, receipts, purchases, and price history (implemented paths are automated; physical barcode camera and representative real-receipt evidence remain manual release blockers)
- Phase 8 — security, operations, UX, and release hardening (automated hardening is implemented; stricter manual evidence JSON, published-image signature verification, final PASS readiness, and final no-blocker review remain pending)
- Independent full review completed (tracked focused review covers reviewed revision `4e144e88786a0f2c979033c6c2a63fdb26d5340c` and reports no open Critical/High findings, with prior HA/container High findings resolved; final PASS review against the target release commit remains pending because release-blocking Medium external evidence gaps remain)
- Release gate PASS

## Residual Risks

- `docs/release/manual-validation.json` is absent. The manual release gate requires PASS evidence for `physical-barcode-camera`, `real-receipt-ocr`, `published-image-signature`, and `independent-full-review` on the target release commit; the `docs/release/manual-validation.json` file itself must be git-tracked and clean in the validating checkout, may contain only the generated required check IDs, must keep each check's generated acceptance IDs exact, must stay on the generated schema with no ignored extra fields, local evidence artifacts must be git-tracked and clean under `docs/release/evidence/` and each listed evidence artifact must be valid UTF-8 text that mentions the check ID it supports, signature evidence must include a SemVer tag, `sha256:<digest>`, SemVer-tagged digest-pinned image reference, Rekor/Sigstore transparency-log URL or explicit unavailability reason, plus a passing `cosign verify` invocation against the recorded image reference, digest, and signature identity with the recorded identity in a `--certificate-identity` value or `--certificate-identity-regexp` pattern and no insecure cosign verification flags, and review evidence must point to a git-tracked and clean artifact under `docs/reviews/` that mentions the matching commit and includes machine-readable `decision=PASS`, `open_critical_high=0`, and `release_blocking_medium=0` markers.
- Physical-device barcode camera validation and representative real-receipt OCR/review/commit evidence remain pending. Deterministic browser/OCR automation exists, but these user-facing capture paths still need real-device/manual release artifacts. Final barcode evidence must cover separate valid 8-14 digit GTIN known-barcode and unknown/manual-fallback product or lot outcomes; final real-receipt evidence must identify a representative real receipt source, describe OCR extraction from a receipt image/photo/scan, and include committed receipt/purchase IDs matching the PantryOS `receipt_<32 lowercase hex>` and `purchase_<32 lowercase hex>` formats, lot count, and price-history confirmation covering store, date, package/quantity, total, and unit price.
- Published-image signature verification remains pending until the final tag/image is published and verified; final evidence must keep the SemVer tag, image reference, recorded digest, signature identity, and `cosign verify` identity flag value/pattern consistent and avoid insecure verification flags.
- The tracked focused review at `docs/reviews/2026-08-26-current-head-review.md` covers reviewed revision `4e144e88786a0f2c979033c6c2a63fdb26d5340c`, found no open Critical/High issues, and recorded the prior HA stale-sensor and non-isolated-container findings as resolved. It still reports release-blocking Medium external evidence gaps; J7 cannot pass until a final review artifact mentions the target release commit and reports no release-blocking Medium findings.
- Release readiness remains `NOT READY` until the acceptance criteria checklist is fully checked/evidenced or explicitly scoped, the manual evidence JSON validates, the final image signature evidence exists, every required release command is rerun, readiness is regenerated as PASS, and a final no-blocker independent review is recorded.

## Latest Evidence

- 2026-08-26 `Phase 8/J`: Hardened scripts/release_readiness.py so docs/release/RELEASE_READINESS.md now treats unchecked docs/handoff/08_ACCEPTANCE_CRITERIA.md items as explicit open acceptance gates; a PASS decision now requires every contract criterion to be checked, not merely counted. README and release-plan wording now document the acceptance-criteria readiness gate.
  Evidence: `.\.venv\Scripts\python.exe -m pytest tests\test_release_smoke.py::test_release_readiness_generator_tracks_acceptance_and_blockers tests\test_release_smoke.py::test_release_readiness_latest_evidence_is_newest_first -q` -> 2 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; `.\.venv\Scripts\python.exe scripts\supply_chain_audit.py --write` -> `ok=true`, source file count `72`, OS package count `157`; `.\.venv\Scripts\python.exe scripts\release_readiness.py --write` -> regenerated readiness with `Open acceptance criteria: 85`; full ruff format/check/mypy -> passed; release_readiness.py --check -> current; supply_chain_audit.py -> `ok=true`; release_artifact_audit.py -> ok, scanned files `84`, allowed matches `21`; `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 141 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.
  Remaining: The acceptance criteria checklist still has 85 unchecked contract items that must be individually evidenced/checked or explicitly scoped before PASS; physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean UTF-8 text artifacts under docs/release/evidence/ that mention the check ID they support, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending
- 2026-08-26 `Phase 6/8/J`: Hardened scripts/manual_release_evidence.py so local manual release artifacts must decode as valid UTF-8 text before they can satisfy a release check; binary or corrupt artifacts can no longer pass by replacement decoding. README, release-plan, blocker wording, and release readiness now document the valid UTF-8 evidence artifact requirement.
  Evidence: `.\.venv\Scripts\python.exe -m pytest tests\test_release_smoke.py::test_manual_release_evidence_accepts_complete_current_commit_record tests\test_release_smoke.py::test_manual_release_evidence_rejects_artifact_without_check_id tests\test_release_smoke.py::test_manual_release_evidence_rejects_artifact_that_is_not_utf8_text tests\test_release_smoke.py::test_manual_release_evidence_rejects_untracked_evidence_file tests\test_release_smoke.py::test_manual_release_evidence_rejects_dirty_tracked_artifacts -q` -> 5 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; `.\.venv\Scripts\python.exe scripts\supply_chain_audit.py --write` -> `ok=true`, source file count `72`, OS package count `157`; full ruff format/check/mypy -> passed; initial `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 141 tests passed, api concurrency smoke 20 mutations passed, then stopped on stale release readiness before regeneration; rerun `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 141 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean UTF-8 text artifacts under docs/release/evidence/ that mention the check ID they support, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending
- 2026-08-26 `Phase 6/8/J`: Hardened scripts/manual_release_evidence.py so each local docs/release/evidence/ artifact listed by a manual release check must mention that check's ID; generic tracked artifacts can no longer satisfy unrelated external gates. README, release-plan, blocker wording, and release readiness now document the artifact-to-check binding requirement.
  Evidence: `.\.venv\Scripts\python.exe -m pytest tests\test_release_smoke.py::test_manual_release_evidence_accepts_complete_current_commit_record tests\test_release_smoke.py::test_manual_release_evidence_rejects_artifacts_outside_release_evidence_dir tests\test_release_smoke.py::test_manual_release_evidence_rejects_artifact_without_check_id tests\test_release_smoke.py::test_manual_release_evidence_rejects_non_release_evidence_path tests\test_release_smoke.py::test_manual_release_evidence_rejects_untracked_evidence_file tests\test_release_smoke.py::test_manual_release_evidence_rejects_dirty_tracked_artifacts tests\test_release_smoke.py::test_manual_release_evidence_rejects_untracked_git_artifacts -q` -> 7 passed; `.\.venv\Scripts\python.exe -m ruff format scripts\manual_release_evidence.py tests\test_release_smoke.py` -> 2 files reformatted; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; `.\.venv\Scripts\python.exe scripts\supply_chain_audit.py --write` -> `ok=true`, source file count `72`, OS package count `157`; full ruff format/check/mypy -> passed; initial `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 140 tests passed, api concurrency smoke 20 mutations passed, then stopped on stale release readiness before regeneration; rerun `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 140 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\.venv\Scripts\python.exe scripts\container_smoke.py --isolated --build-timeout 600` -> `ok=true`, Docker server `29.6.1`, UID `10001`, source/restored counts matched; `$env:PANTRYOS_API_TOKEN = 'docker-status-token'; docker compose ps` -> `pantryos` Up healthy on `0.0.0.0:8765`.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean UTF-8 text artifacts under docs/release/evidence/ that mention the check ID they support, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending
- 2026-08-26 `Phase 6/8/J`: Hardened scripts/manual_release_evidence.py so real-receipt OCR release evidence must use concrete PantryOS receipt_<32 lowercase hex> and purchase_<32 lowercase hex> identifiers instead of bare prefixes or placeholder-like IDs. README, release-plan, blocker wording, and release readiness now document the actual receipt/purchase ID evidence format.
  Evidence: focused real-receipt evidence pytest -> 5 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 139 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending
- 2026-08-26 `Phase 8/J`: Hardened scripts/manual_release_evidence.py so published-image signature evidence must record an actual cosign verify invocation, not shell text that merely contains those words. README, supply-chain policy, release-plan, blocker wording, and release readiness now document the exact cosign verification invocation requirement.
  Evidence: focused signature-evidence pytest -> 8 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 138 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending
- 2026-08-26 `Phase 8/J`: Hardened scripts/manual_release_evidence.py so published-image signature evidence rejects cosign verify commands that use insecure verification flags such as --insecure-ignore-tlog, --insecure-ignore-sct, --allow-insecure-registry, or --insecure. README, supply-chain policy, release-plan, blocker wording, and release readiness now document the no-insecure-cosign-flags evidence requirement.
  Evidence: focused signature-evidence pytest -> 7 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 137 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, cosign identity flag-value constraint, and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending
- 2026-08-26 `Phase 6/8/J`: Hardened scripts/manual_release_evidence.py so physical barcode camera evidence for the unknown-code path must describe a manual fallback product or lot outcome, not just that the fallback form opened. README, release-plan, blocker wording, and release readiness now document the concrete manual-fallback outcome requirement.
  Evidence: focused physical-barcode evidence pytest -> 5 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 136 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, and cosign identity flag-value and no-insecure-flag constraints, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending
- 2026-08-26 `Phase 8/J`: Hardened scripts/manual_release_evidence.py so published-image signature evidence rejects generic HTTP links for transparency_log_url; final signature evidence must now provide a Rekor/Sigstore transparency-log URL or an explicit not available: ... reason. README, supply-chain policy, release-plan, blocker wording, and release readiness now document the stricter transparency-log evidence requirement.
  Evidence: focused transparency-log pytest -> 4 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 135 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, and cosign identity flag-value and no-insecure-flag constraints, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending

## Machine Summary

```json
{
  "acceptance_criteria_count": 85,
  "decision": "NOT READY",
  "generated_at": "2026-08-26T21:16:31Z",
  "latest_evidence": [
    {
      "change": "Hardened scripts/release_readiness.py so docs/release/RELEASE_READINESS.md now treats unchecked docs/handoff/08_ACCEPTANCE_CRITERIA.md items as explicit open acceptance gates; a PASS decision now requires every contract criterion to be checked, not merely counted. README and release-plan wording now document the acceptance-criteria readiness gate.",
      "commands": "`.\\.venv\\Scripts\\python.exe -m pytest tests\\test_release_smoke.py::test_release_readiness_generator_tracks_acceptance_and_blockers tests\\test_release_smoke.py::test_release_readiness_latest_evidence_is_newest_first -q` -> 2 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; `.\\.venv\\Scripts\\python.exe scripts\\supply_chain_audit.py --write` -> `ok=true`, source file count `72`, OS package count `157`; `.\\.venv\\Scripts\\python.exe scripts\\release_readiness.py --write` -> regenerated readiness with `Open acceptance criteria: 85`; full ruff format/check/mypy -> passed; release_readiness.py --check -> current; supply_chain_audit.py -> `ok=true`; release_artifact_audit.py -> ok, scanned files `84`, allowed matches `21`; `.\\.venv\\Scripts\\python.exe scripts\\check.py` -> python compile 46 files passed, 141 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.",
      "date": "2026-08-26",
      "gap": "The acceptance criteria checklist still has 85 unchecked contract items that must be individually evidenced/checked or explicitly scoped before PASS; physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean UTF-8 text artifacts under docs/release/evidence/ that mention the check ID they support, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending",
      "phase": "Phase 8/J"
    },
    {
      "change": "Hardened scripts/manual_release_evidence.py so local manual release artifacts must decode as valid UTF-8 text before they can satisfy a release check; binary or corrupt artifacts can no longer pass by replacement decoding. README, release-plan, blocker wording, and release readiness now document the valid UTF-8 evidence artifact requirement.",
      "commands": "`.\\.venv\\Scripts\\python.exe -m pytest tests\\test_release_smoke.py::test_manual_release_evidence_accepts_complete_current_commit_record tests\\test_release_smoke.py::test_manual_release_evidence_rejects_artifact_without_check_id tests\\test_release_smoke.py::test_manual_release_evidence_rejects_artifact_that_is_not_utf8_text tests\\test_release_smoke.py::test_manual_release_evidence_rejects_untracked_evidence_file tests\\test_release_smoke.py::test_manual_release_evidence_rejects_dirty_tracked_artifacts -q` -> 5 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; `.\\.venv\\Scripts\\python.exe scripts\\supply_chain_audit.py --write` -> `ok=true`, source file count `72`, OS package count `157`; full ruff format/check/mypy -> passed; initial `.\\.venv\\Scripts\\python.exe scripts\\check.py` -> python compile 46 files passed, 141 tests passed, api concurrency smoke 20 mutations passed, then stopped on stale release readiness before regeneration; rerun `.\\.venv\\Scripts\\python.exe scripts\\check.py` -> python compile 46 files passed, 141 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean UTF-8 text artifacts under docs/release/evidence/ that mention the check ID they support, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending",
      "phase": "Phase 6/8/J"
    },
    {
      "change": "Hardened scripts/manual_release_evidence.py so each local docs/release/evidence/ artifact listed by a manual release check must mention that check's ID; generic tracked artifacts can no longer satisfy unrelated external gates. README, release-plan, blocker wording, and release readiness now document the artifact-to-check binding requirement.",
      "commands": "`.\\.venv\\Scripts\\python.exe -m pytest tests\\test_release_smoke.py::test_manual_release_evidence_accepts_complete_current_commit_record tests\\test_release_smoke.py::test_manual_release_evidence_rejects_artifacts_outside_release_evidence_dir tests\\test_release_smoke.py::test_manual_release_evidence_rejects_artifact_without_check_id tests\\test_release_smoke.py::test_manual_release_evidence_rejects_non_release_evidence_path tests\\test_release_smoke.py::test_manual_release_evidence_rejects_untracked_evidence_file tests\\test_release_smoke.py::test_manual_release_evidence_rejects_dirty_tracked_artifacts tests\\test_release_smoke.py::test_manual_release_evidence_rejects_untracked_git_artifacts -q` -> 7 passed; `.\\.venv\\Scripts\\python.exe -m ruff format scripts\\manual_release_evidence.py tests\\test_release_smoke.py` -> 2 files reformatted; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; `.\\.venv\\Scripts\\python.exe scripts\\supply_chain_audit.py --write` -> `ok=true`, source file count `72`, OS package count `157`; full ruff format/check/mypy -> passed; initial `.\\.venv\\Scripts\\python.exe scripts\\check.py` -> python compile 46 files passed, 140 tests passed, api concurrency smoke 20 mutations passed, then stopped on stale release readiness before regeneration; rerun `.\\.venv\\Scripts\\python.exe scripts\\check.py` -> python compile 46 files passed, 140 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\\.venv\\Scripts\\python.exe scripts\\container_smoke.py --isolated --build-timeout 600` -> `ok=true`, Docker server `29.6.1`, UID `10001`, source/restored counts matched; `$env:PANTRYOS_API_TOKEN = 'docker-status-token'; docker compose ps` -> `pantryos` Up healthy on `0.0.0.0:8765`.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean UTF-8 text artifacts under docs/release/evidence/ that mention the check ID they support, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending",
      "phase": "Phase 6/8/J"
    },
    {
      "change": "Hardened scripts/manual_release_evidence.py so real-receipt OCR release evidence must use concrete PantryOS receipt_<32 lowercase hex> and purchase_<32 lowercase hex> identifiers instead of bare prefixes or placeholder-like IDs. README, release-plan, blocker wording, and release readiness now document the actual receipt/purchase ID evidence format.",
      "commands": "focused real-receipt evidence pytest -> 5 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 139 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending",
      "phase": "Phase 6/8/J"
    },
    {
      "change": "Hardened scripts/manual_release_evidence.py so published-image signature evidence must record an actual cosign verify invocation, not shell text that merely contains those words. README, supply-chain policy, release-plan, blocker wording, and release readiness now document the exact cosign verification invocation requirement.",
      "commands": "focused signature-evidence pytest -> 8 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 138 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, actual cosign verify invocation with identity flag-value constraint and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending",
      "phase": "Phase 8/J"
    },
    {
      "change": "Hardened scripts/manual_release_evidence.py so published-image signature evidence rejects cosign verify commands that use insecure verification flags such as --insecure-ignore-tlog, --insecure-ignore-sct, --allow-insecure-registry, or --insecure. README, supply-chain policy, release-plan, blocker wording, and release readiness now document the no-insecure-cosign-flags evidence requirement.",
      "commands": "focused signature-evidence pytest -> 7 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 137 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, cosign identity flag-value constraint, and no insecure cosign verification flags, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending",
      "phase": "Phase 8/J"
    },
    {
      "change": "Hardened scripts/manual_release_evidence.py so physical barcode camera evidence for the unknown-code path must describe a manual fallback product or lot outcome, not just that the fallback form opened. README, release-plan, blocker wording, and release readiness now document the concrete manual-fallback outcome requirement.",
      "commands": "focused physical-barcode evidence pytest -> 5 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 136 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, and cosign identity flag-value and no-insecure-flag constraints, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending",
      "phase": "Phase 6/8/J"
    },
    {
      "change": "Hardened scripts/manual_release_evidence.py so published-image signature evidence rejects generic HTTP links for transparency_log_url; final signature evidence must now provide a Rekor/Sigstore transparency-log URL or an explicit not available: ... reason. README, supply-chain policy, release-plan, blocker wording, and release readiness now document the stricter transparency-log evidence requirement.",
      "commands": "focused transparency-log pytest -> 4 passed; targeted ruff check -> all checks passed; targeted mypy -> success in 1 source file; supply_chain_audit.py --write -> `ok=true`, source file count `72`, OS package count `157`; scripts/check.py -> python compile 46 files passed, 135 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; full ruff format/check/mypy -> passed.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final SemVer tag with matching tag-plus-digest image reference, digest, identity, Rekor/Sigstore transparency-log URL or explicit unavailability reason, and cosign identity flag-value and no-insecure-flag constraints, concrete tracked-and-clean docs/release/manual-validation.json on the generated schema with exact required checks, git-tracked and clean artifacts under docs/release/evidence/, final no-blocker independent review under docs/reviews/ against the target release commit, and release readiness PASS remain pending",
      "phase": "Phase 8/J"
    }
  ],
  "open_acceptance_criteria": [
    "A1. There is one authoritative SQLite database owned by PantryOS Core.",
    "A2. The web UI and Home Assistant do not maintain independent inventory state.",
    "A3. Adding an item in the web UI changes Home Assistant state promptly without either process restarting.",
    "A4. Consuming an item through a Home Assistant action changes the web UI promptly.",
    "A5. Restarting PantryOS and Home Assistant preserves and reconciles the same state revision.",
    "A6. A test with at least 20 concurrent supported mutations produces no lost successful writes and no corrupt database.",
    "A7. Database transactions roll back all related changes on injected failure.",
    "A8. Legacy JSON import is validated, backed up, atomic, and idempotent.",
    "A9. A backup can be restored into a clean instance and passes an integrity/summary comparison.",
    "B1. Two lots of the same product can have different locations, expirations, and costs.",
    "B2. Product minimum stock remains after all active lots reach zero.",
    "B3. Location rename/move does not rewrite every inventory lot or break references.",
    "B4. Consumption across lots follows FEFO and returns the exact allocation.",
    "B5. Over-consumption is rejected with available quantity unless an explicit administrative adjustment is used.",
    "B6. Opening a lot is idempotent and correctly applies configured opened-life policy.",
    "B7. Discard and waste create historical events and remove quantity from usable stock.",
    "B8. Inventory event history reconstructs the reason and source of every tested mutation.",
    "B9. Negative quantities, invalid dates, incompatible units, duplicate constrained names, and cyclic locations are rejected.",
    "C1. `1 lb` satisfies an `8 oz` requirement and reports the correct remainder.",
    "C2. Incompatible dimensions are rejected; mass-to-volume is not guessed.",
    "C3. Product aliases resolve recipe text to stable product IDs.",
    "C4. Expired/discarded lots do not satisfy recipes by default.",
    "C5. Recipe quantities scale correctly with servings.",
    "C6. Ready, missing-N, and max-time queries return deterministic explainable results.",
    "C7. Unknown prep time follows the documented max-time policy.",
    "C8. Use-soon recommendations identify which urgent lots a recipe consumes and why it ranks highly.",
    "D1. A weekly plan stores date, meal type, recipe ID, servings, and status.",
    "D2. Ingredient demand aggregates across all planned meals and subtracts usable inventory.",
    "D3. Rebuilding shopping demand twice produces the same generated demand, not double quantities.",
    "D4. Repeating \u201cadd missing\u201d for one recipe/plan source is idempotent.",
    "D5. Manual demand and generated demand can coexist with visible source breakdown.",
    "D6. Minimum-stock suggestions remain separate until accepted.",
    "D7. Shopping lines can be edited, checked, unchecked, suppressed/removed, and assigned notes/store.",
    "D8. Completing a purchase creates purchase lines and selected inventory lots transactionally.",
    "D9. A user override is preserved or explicitly reconciled when generated demand is rebuilt.",
    "E1. Starting cooking emits an event but does not consume inventory.",
    "E2. Completing cooking requires confirmed allocations and is atomic.",
    "E3. Completing cooking can create leftover lots with servings, made time, use-by time, location, and recipe/session provenance.",
    "E4. Leftovers appear in the next-meal/use-soon experience.",
    "E5. Monthly waste is calculated from waste events and survives lot cleanup.",
    "E6. Inventory value by freezer/refrigerator/location is available to the API and Home Assistant.",
    "F1. A known barcode scan resolves a product/package and can add a lot.",
    "F2. An unknown barcode opens a mapping/product form; manual entry works without camera support.",
    "F3. Barcode mappings are unique and persist across restart.",
    "F4. Receipt upload enforces supported type and size limits and stores files outside the static web root.",
    "F5. At least one local extraction implementation works in the documented container environment.",
    "F6. Extracted receipt data is editable and cannot mutate inventory before confirmation.",
    "F7. Duplicate receipt commit is detected and does not duplicate purchase lines or inventory.",
    "F8. Purchase history shows store, date, package/quantity, total, and comparable unit price.",
    "F9. Price anomaly output uses compatible units, a documented baseline, and an explanation.",
    "G1. All primary inventory, recipe, plan, shopping, cooking, barcode, receipt, and settings workflows are reachable in the UI.",
    "G2. No visible control is a toast-only stub or hard-coded success simulation.",
    "G3. Loading, empty, offline, validation, conflict, and server-error states are clear.",
    "G4. Destructive actions require appropriate confirmation and remain keyboard accessible.",
    "G5. Critical workflows pass automated browser tests at phone and kitchen-tablet viewport sizes.",
    "G6. Forms have labels, focus handling, error association, and no critical automated accessibility violations.",
    "G7. PWA install metadata and an intentional offline/error strategy exist; offline writes are not falsely reported as committed.",
    "H1. Config flow accepts and validates base URL/token and uses the PantryOS instance ID as unique identity.",
    "H2. Reconfigure and authentication failure recovery are supported.",
    "H3. The integration uses one coordinated cache/push subscription and performs no I/O in entity properties.",
    "H4. Entities become unavailable when Core is unreachable and recover automatically.",
    "H5. Actions call the API, return useful errors/responses, and remain registered according to current HA conventions.",
    "H6. Existing action names are retained or have a documented compatibility/migration path.",
    "H7. Sensors cover total inventory, expiring soon, shopping, possible meals, leftovers, waste, and location values/counts without unbounded attributes.",
    "H8. Diagnostics redact token, authorization headers, session data, receipt text/images, and secrets.",
    "H9. HA tests cover setup, unload, auth failure, unavailable/recovery, sensor updates, and representative actions.",
    "H10. Example automations demonstrate use-soon notification, grocery arrival count, cooking mode, and freezer risk/value.",
    "I1. Unauthorized API mutation and event subscription fail.",
    "I2. Browser authentication/session, CSRF, CORS, origin, and cookie policy are explicit and tested.",
    "I3. Request body, upload size, filename/path, MIME/content, and rate limits are enforced.",
    "I4. No secrets appear in source, logs, client errors, diagnostics, fixtures, or generated artifacts.",
    "I5. Container runs as non-root and writes only intended volume paths.",
    "I6. Liveness and readiness have different semantics and tests.",
    "I7. Structured logs include request/correlation IDs without sensitive payloads.",
    "I8. Migration failure leaves the prior database/backup recoverable.",
    "I9. `pantryos doctor`, backup, restore verification, and legacy dry-run are documented and smoke-tested.",
    "I10. Core operation is usable without internet access after dependencies/images are installed.",
    "J1. Unit, integration, API, migration, concurrency, HA, browser, and container smoke suites pass from a clean checkout.",
    "J2. Lint, format, and type checks pass.",
    "J3. Domain/application code has meaningful coverage, including failure paths; the configured threshold passes without broad exclusions.",
    "J4. OpenAPI, user setup, Home Assistant setup, backup/restore, migration, and troubleshooting docs match actual behavior.",
    "J5. No completion-critical TODO, placeholder URL, fake response, disabled test, or skipped release check remains without a documented non-blocking reason.",
    "J6. A scripted demo proves add \u2192 sync \u2192 plan \u2192 shop \u2192 cook \u2192 leftover \u2192 use-soon across the supported surfaces.",
    "J7. Independent review reports no open Critical/High findings and no release-blocking Medium finding.",
    "J8. `docs/release/RELEASE_READINESS.md` records exact commands, results, residual risks, and a justified `PASS`."
  ],
  "open_blockers": [
    "`docs/release/manual-validation.json` is absent. The manual release gate requires PASS evidence for `physical-barcode-camera`, `real-receipt-ocr`, `published-image-signature`, and `independent-full-review` on the target release commit; the `docs/release/manual-validation.json` file itself must be git-tracked and clean in the validating checkout, may contain only the generated required check IDs, must keep each check's generated acceptance IDs exact, must stay on the generated schema with no ignored extra fields, local evidence artifacts must be git-tracked and clean under `docs/release/evidence/` and each listed evidence artifact must be valid UTF-8 text that mentions the check ID it supports, signature evidence must include a SemVer tag, `sha256:<digest>`, SemVer-tagged digest-pinned image reference, Rekor/Sigstore transparency-log URL or explicit unavailability reason, plus a passing `cosign verify` invocation against the recorded image reference, digest, and signature identity with the recorded identity in a `--certificate-identity` value or `--certificate-identity-regexp` pattern and no insecure cosign verification flags, and review evidence must point to a git-tracked and clean artifact under `docs/reviews/` that mentions the matching commit and includes machine-readable `decision=PASS`, `open_critical_high=0`, and `release_blocking_medium=0` markers.",
    "Physical-device barcode camera validation and representative real-receipt OCR/review/commit evidence remain pending. Deterministic browser/OCR automation exists, but these user-facing capture paths still need real-device/manual release artifacts. Final barcode evidence must cover separate valid 8-14 digit GTIN known-barcode and unknown/manual-fallback product or lot outcomes; final real-receipt evidence must identify a representative real receipt source, describe OCR extraction from a receipt image/photo/scan, and include committed receipt/purchase IDs matching the PantryOS `receipt_<32 lowercase hex>` and `purchase_<32 lowercase hex>` formats, lot count, and price-history confirmation covering store, date, package/quantity, total, and unit price.",
    "Published-image signature verification remains pending until the final tag/image is published and verified; final evidence must keep the SemVer tag, image reference, recorded digest, signature identity, and `cosign verify` identity flag value/pattern consistent and avoid insecure verification flags.",
    "The tracked focused review at `docs/reviews/2026-08-26-current-head-review.md` covers reviewed revision `4e144e88786a0f2c979033c6c2a63fdb26d5340c`, found no open Critical/High issues, and recorded the prior HA stale-sensor and non-isolated-container findings as resolved. It still reports release-blocking Medium external evidence gaps; J7 cannot pass until a final review artifact mentions the target release commit and reports no release-blocking Medium findings.",
    "Release readiness remains `NOT READY` until the acceptance criteria checklist is fully checked/evidenced or explicitly scoped, the manual evidence JSON validates, the final image signature evidence exists, every required release command is rerun, readiness is regenerated as PASS, and a final no-blocker independent review is recorded."
  ],
  "open_phase_gates": [
    "Phase 6 \u2014 barcode, receipts, purchases, and price history (implemented paths are automated; physical barcode camera and representative real-receipt evidence remain manual release blockers)",
    "Phase 8 \u2014 security, operations, UX, and release hardening (automated hardening is implemented; stricter manual evidence JSON, published-image signature verification, final PASS readiness, and final no-blocker review remain pending)",
    "Independent full review completed (tracked focused review covers reviewed revision `4e144e88786a0f2c979033c6c2a63fdb26d5340c` and reports no open Critical/High findings, with prior HA/container High findings resolved; final PASS review against the target release commit remains pending because release-blocking Medium external evidence gaps remain)",
    "Release gate PASS"
  ],
  "phase_gate_count": 11,
  "required_release_commands": [
    "python scripts/check.py",
    "python scripts/smoke_e2e.py",
    "python scripts/concurrency_smoke.py",
    "node scripts/browser_smoke.cjs",
    "python scripts/container_smoke.py --isolated",
    "python scripts/receipt_ocr_corpus_smoke.py",
    "python scripts/image_hardening_audit.py",
    "python scripts/supply_chain_audit.py",
    "python scripts/ha_installed_smoke.py",
    "python scripts/ha_core_live_smoke.py",
    "python scripts/release_artifact_audit.py",
    "python scripts/manual_release_evidence.py",
    "python scripts/release_readiness.py --check"
  ]
}
```

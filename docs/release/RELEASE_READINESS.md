# PantryOS release readiness

This file is generated by `python scripts/release_readiness.py --write` from the acceptance criteria and implementation status ledger.

## Decision

- Status: **NOT READY**
- Generated: `2026-08-26T13:39:11Z`
- Acceptance criteria in contract: `85`
- Phase gates in ledger: `11`

## Required Release Commands

- `python scripts/check.py`
- `python scripts/smoke_e2e.py`
- `python scripts/concurrency_smoke.py`
- `node scripts/browser_smoke.cjs`
- `python scripts/container_smoke.py --isolated`
- `python scripts/receipt_ocr_corpus_smoke.py`
- `python scripts/image_hardening_audit.py`
- `python scripts/supply_chain_audit.py`
- `python scripts/ha_installed_smoke.py`
- `python scripts/ha_core_live_smoke.py`
- `python scripts/release_artifact_audit.py`
- `python scripts/manual_release_evidence.py`
- `python scripts/release_readiness.py --check`

## Open Phase Gates

- Phase 6 — barcode, receipts, purchases, and price history (implemented paths are automated; physical barcode camera and representative real-receipt evidence remain manual release blockers)
- Phase 8 — security, operations, UX, and release hardening (automated hardening is implemented; manual evidence JSON, published-image signature verification, final PASS readiness, and final no-blocker review remain pending)
- Independent full review completed (latest review covers pre-D9 HEAD `82d7f32` and reports release-blocking Medium evidence gaps; final PASS review against the final commit pending)
- Release gate PASS

## Residual Risks

- `docs/release/manual-validation.json` is absent. The manual release gate requires PASS evidence for `physical-barcode-camera`, `real-receipt-ocr`, `published-image-signature`, and `independent-full-review` on the final release commit.
- Physical-device barcode camera validation and representative real-receipt OCR/review/commit evidence remain pending. Deterministic browser/OCR automation exists, but these user-facing capture paths still need real-device/manual release artifacts.
- Published-image signature verification remains pending until the final tag/image is published and verified.
- The independent review at `docs/reviews/2026-08-26-current-head-review.md` reviewed pre-D9 HEAD `82d7f3284ea3457139e332c7cafb77a037ce3d52`, found no open Critical/High issues, but still reports release-blocking Medium evidence gaps; J7 cannot pass until a final review against the final commit reports no release-blocking Medium findings.
- Release readiness remains `NOT READY` until the manual evidence JSON validates, the final image signature evidence exists, every required release command is rerun, readiness is regenerated as PASS, and a final no-blocker independent review is recorded.

## Latest Evidence

- 2026-08-26 `Phase 0/8/J`: Reverified the reproducible dev-tooling slice against Docker after adding the local venv gates and deterministic import bootstraps. The rebuilt Compose service is running at http://127.0.0.1:8765 with the local development token.
  Evidence: `PANTRYOS_API_TOKEN=local-dev-token docker compose up -d --build --force-recreate pantryos` -> rebuilt `pantryos-pantryos:latest` and restarted `pantryos`; `python scripts/supply_chain_audit.py --write` and `python scripts/supply_chain_audit.py` -> `ok=true`, source files `70`, OS packages `157`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python scripts/receipt_ocr_corpus_smoke.py --timeout 180` -> `ok=true`, `case_count=3`, parsed `OCR Beans`, `Milk`, `Bananas`, `Tomatoes`, and `Eggs`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, UID `10001`, backup `/app/data/backups/container-smoke-20260826102812.zip`, restored counts matched source counts (`events=104`, `lots=49`, `products=46`, `purchases=19`, `recipes=4`).
  Remaining: Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending
- 2026-08-26 `Phase 7/H/J`: Reworked scripts/ha_core_live_smoke.py so the required live Home Assistant Core proof no longer needs the user's PANTRYOS_API_TOKEN or mutates the user's PantryOS database. The smoke now creates a disposable Docker network, starts an isolated PantryOS Core container from the local image with a fixed non-secret smoke token and disposable volume, mounts the local custom integration into a real Home Assistant Core container, creates a real config entry, verifies sensors/services, calls pantryos.add_item, writes a direct Core item through the HA runtime client, waits for coordinator revision movement, unloads the entry, and removes the generated Core container/network/volume.
  Evidence: `python scripts/ha_core_live_smoke.py --timeout 180` -> `ok=true`, `core_mode=isolated`, Home Assistant `2026.8.3`, `sensor_count=16`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `event_count=1`, `unloaded=true`, `remaining_services=[]`; cleanup checks `docker ps/network/volume ls --filter name=pantryos-ha-core-smoke` -> no generated containers, networks, or volumes remained.
  Remaining: Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending
- 2026-08-26 `Phase 7/8/H/I/J`: Completed a Docker-backed release verification pass for the isolated Home Assistant Core smoke and hardened local tooling. scripts/ha_core_live_smoke.py now avoids live user tokens/databases by creating a disposable PantryOS Core container/network/volume with a fixed non-secret smoke token, the browser smoke now ignores invalid directory-style Python paths and requires a Python 3.12+ runtime, and the status/readiness wording records the old live-token HA path as superseded.
  Evidence: `.\.venv\Scripts\python.exe -m ruff format --check .` -> 70 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components\pantryos scripts\ha_core_live_smoke.py` -> no issues in 17 source files; `.\.venv\Scripts\python.exe -m pytest -q` -> 96 passed; `.\.venv\Scripts\python.exe -m coverage report` -> total coverage 83%, above 80% gate; `PANTRYOS_API_TOKEN=local-dev-token docker compose up -d --build --force-recreate pantryos` -> rebuilt `pantryos-pantryos:latest` and restarted `pantryos`; `python scripts/supply_chain_audit.py --write` -> `ok=true`, source files `70`, OS packages `157`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python scripts/check.py` -> python compile 44 files passed, 96 tests passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `PANTRYOS_API_TOKEN=e2e-smoke-token python scripts/smoke_e2e.py` -> `ok=true`, revision `22`, matching browser/HA lot id, leftover count `2`; `PANTRYOS_API_TOKEN=browser-smoke-token node scripts/browser_smoke.cjs` -> browser smoke passed for `phone` and `kitchen-tablet`; `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true`, Home Assistant `2026.8.3`, `stream_task_cancelled=true`; `python scripts/receipt_ocr_corpus_smoke.py --timeout 180` -> `ok=true`, `case_count=3`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, UID `10001`, backup `/app/data/backups/container-smoke-20260826113158.zip`, source/restored counts matched (`events=112`, `lots=53`, `products=50`, `purchases=21`, `recipes=4`); `python scripts/ha_core_live_smoke.py --timeout 180` -> `ok=true`, isolated Home Assistant `2026.8.3`, `sensor_count=16`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `unloaded=true`, `remaining_services=[]`; cleanup checks for `pantryos-ha-core-smoke` containers/networks/volumes -> none remained.
  Remaining: Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending
- 2026-08-26 `Phase 1/2/8/J`: Added scripts/concurrency_smoke.py, a dependency-free authenticated API durability smoke for A6, and sized the local PantryHTTPServer accept queue for the 20-request burst. The smoke starts a temporary threaded PantryOS Core server, seeds independent lots, releases 20 concurrent supported API mutations across inventory add/open/consume/discard endpoints, verifies every successful mutation in the final dashboard, checks revision and event-count deltas, and runs SQLite integrity verification against the resulting database. scripts/check.py, release readiness, and the Docker in-image verifier now include this smoke as a required gate.
  Evidence: `python scripts/concurrency_smoke.py` -> `ok=true`, `successful_mutations=20`, `baseline_revision=24`, `final_revision=44`, `event_count=44`, `product_count=27`, `active_lot_count=23`; `docker exec --user 10001:10001 pantryos python scripts/check.py` -> python compile 45 files passed, 97 tests passed, api concurrency smoke 20 mutations passed, release checks current; `python scripts/container_smoke.py` -> `ok=true`, `uid=10001`, `revision_after_restart=120`, restored counts matched source counts; `python -m pytest -q` -> 97 passed; `python scripts/supply_chain_audit.py --write` -> `ok=true`, `source_file_count=71`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python -m mypy src custom_components\pantryos scripts\ha_core_live_smoke.py scripts\concurrency_smoke.py` -> success; `python scripts/check.py` -> passed.
  Remaining: Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending
- 2026-08-26 `Phase 7/H/J`: Strengthened scripts/ha_core_live_smoke.py so the isolated live Home Assistant Core proof creates the PantryOS entry through Home Assistant's config-flow manager against the live disposable Core API instead of manually constructing a ConfigEntry. The smoke now records the flow result type and PantryOS instance unique ID before proving setup, services, event listener revision movement, sensors, unload, and cleanup. README and HA contract tests now require this config-flow path.
  Evidence: `python scripts/ha_core_live_smoke.py --timeout 240` -> `ok=true`, `config_flow_result_type=create_entry`, `entry_unique_id=inst_ca167d95cf7a4c329ccb390b3ce46896`, Home Assistant `2026.8.3`, `core_mode=isolated`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `sensor_count=16`, `unloaded=true`, `remaining_services=[]`; cleanup checks `docker ps/network/volume ls --filter name=pantryos-ha-core-smoke` -> no generated containers, networks, or volumes remained; `python -m pytest tests/test_ha_contract.py -q` -> 11 passed; `python -m pytest -q` -> 97 passed; `python -m ruff format --check .` -> 71 files already formatted; `python -m ruff check .` -> all checks passed; `python -m mypy src custom_components\pantryos scripts\ha_core_live_smoke.py scripts\concurrency_smoke.py` -> success; `docker compose up -d --build --force-recreate pantryos` -> rebuilt image and restarted container; `docker exec --user 10001:10001 pantryos python scripts/check.py` -> python compile 45 files passed, 97 tests passed, api concurrency smoke 20 mutations passed, release checks current; `python scripts/container_smoke.py` -> `ok=true`, `uid=10001`, `revision_after_restart=124`, restored counts matched source counts; `python scripts/check.py` -> passed; `python scripts/supply_chain_audit.py --write` -> `ok=true`, `source_file_count=71`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`.
  Remaining: Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-clicked HA frontend config-entry proof if required, and final independent review refresh remain pending
- 2026-08-26 `Phase 7/8/H/I/J`: Fixed the live Home Assistant Core release smoke false-pass gap by requiring the final HA state_revision and total_items sensor states to match the coordinator snapshot after a direct Core mutation. Added scripts/manual_release_evidence.py, a release gate for external proof that validates docs/release/manual-validation.json against the current Git commit and requires PASS evidence for physical barcode camera, real receipt OCR, published image signature, and independent full review checks. Release readiness now lists the manual evidence validator as a required release command.
  Evidence: `.\.venv\Scripts\python.exe scripts\ha_core_live_smoke.py --timeout 240` -> `ok=true`, Home Assistant `2026.8.3`, `config_flow_result_type=create_entry`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `sensor_count=16`, `unloaded=true`; `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 100 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\.venv\Scripts\python.exe -m ruff format --check .` -> 72 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py` -> success; `.\.venv\Scripts\python.exe scripts\manual_release_evidence.py --json` -> `ok=false`, missing `docs/release/manual-validation.json` as the expected remaining release blocker.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final release tag, manual evidence JSON, release readiness PASS, and final independent review refresh remain pending
- 2026-08-26 `Phase 8/I/J`: Added an isolated release-review mode to scripts/container_smoke.py and made python scripts/container_smoke.py --isolated the required release command. The isolated mode writes a temporary Compose file under the checkout with the same hardened service contract, a generated container name, Docker-assigned host port, and generated data volume; it reruns the same live API, restart, backup/restore, and in-image verifier checks, then removes the generated Compose project resources.
  Evidence: `.\.venv\Scripts\python.exe scripts\container_smoke.py --isolated --build-timeout 360 --ready-timeout 120` -> `ok=true`, `isolated=true`, Docker server `29.6.1`, generated base URL `http://127.0.0.1:50820`, generated container `pantryos-container-smoke-91b02b6a901b`, generated volume `pantryos-container-smoke-data-91b02b6a901b`, UID `10001`, `revision_after_restart=4`, backup `/app/data/backups/container-smoke-20260826131152.zip`, restore database `/app/data/container-smoke-restored-20260826131152.sqlite3`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`), in-image verifier completed with documented Node JS syntax skip; cleanup checks for generated isolated smoke containers/networks/volumes -> none remained; `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 100 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\.venv\Scripts\python.exe -m ruff format --check .` -> 72 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py scripts/container_smoke.py` -> success.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final release tag, manual evidence JSON, release readiness PASS, and final independent review refresh remain pending
- 2026-08-26 `Phase 4/6/7/8/J`: Fixed the D9 generated shopping override gap and reconciled phase-gate status against current automated evidence and the superseding independent review for pre-fix HEAD 82d7f32. Meal-plan shopping rebuild now refreshes generated quantities without silently reactivating user-suppressed or removed generated rows; explicit user reactivation restores the row to accepted active demand. Phases 2, 3, 4, 5, and 7 are now marked complete from accumulated automated evidence; Phase 6, Phase 8, independent review, and release PASS remain open for manual/external release proof.
  Evidence: `.\.venv\Scripts\python.exe -m pytest tests\core\test_sqlite_core.py::test_meal_plan_rebuild_preserves_suppressed_generated_demand_until_reactivated -q` -> 1 passed; `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 101 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\.venv\Scripts\python.exe -m ruff format --check .` -> 73 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py scripts/container_smoke.py` -> success in 21 source files; `.\.venv\Scripts\python.exe scripts\container_smoke.py --isolated --build-timeout 360 --ready-timeout 120` -> `ok=true`, Docker server `29.6.1`, generated base URL `http://127.0.0.1:57379`, generated container `pantryos-container-smoke-04a1643c62f9`, generated volume `pantryos-container-smoke-data-04a1643c62f9`, UID `10001`, `revision_after_restart=4`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`). `docs/reviews/2026-08-26-current-head-review.md` reviewed exact pre-fix HEAD `82d7f3284ea3457139e332c7cafb77a037ce3d52`, confirmed prior HA/container High findings resolved, found no open Critical/High findings, and reported release-blocking Medium gaps for physical barcode camera, representative real receipt OCR, published image signature, and missing manual release evidence JSON.
  Remaining: Physical-device camera/real-receipt validation, published-image signature verification after final tag, docs/release/manual-validation.json, final no-blocker independent review against the final commit, and release readiness PASS remain pending

## Machine Summary

```json
{
  "acceptance_criteria_count": 85,
  "decision": "NOT READY",
  "generated_at": "2026-08-26T13:39:11Z",
  "latest_evidence": [
    {
      "change": "Reverified the reproducible dev-tooling slice against Docker after adding the local venv gates and deterministic import bootstraps. The rebuilt Compose service is running at http://127.0.0.1:8765 with the local development token.",
      "commands": "`PANTRYOS_API_TOKEN=local-dev-token docker compose up -d --build --force-recreate pantryos` -> rebuilt `pantryos-pantryos:latest` and restarted `pantryos`; `python scripts/supply_chain_audit.py --write` and `python scripts/supply_chain_audit.py` -> `ok=true`, source files `70`, OS packages `157`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python scripts/receipt_ocr_corpus_smoke.py --timeout 180` -> `ok=true`, `case_count=3`, parsed `OCR Beans`, `Milk`, `Bananas`, `Tomatoes`, and `Eggs`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, UID `10001`, backup `/app/data/backups/container-smoke-20260826102812.zip`, restored counts matched source counts (`events=104`, `lots=49`, `products=46`, `purchases=19`, `recipes=4`).",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending",
      "phase": "Phase 0/8/J"
    },
    {
      "change": "Reworked scripts/ha_core_live_smoke.py so the required live Home Assistant Core proof no longer needs the user's PANTRYOS_API_TOKEN or mutates the user's PantryOS database. The smoke now creates a disposable Docker network, starts an isolated PantryOS Core container from the local image with a fixed non-secret smoke token and disposable volume, mounts the local custom integration into a real Home Assistant Core container, creates a real config entry, verifies sensors/services, calls pantryos.add_item, writes a direct Core item through the HA runtime client, waits for coordinator revision movement, unloads the entry, and removes the generated Core container/network/volume.",
      "commands": "`python scripts/ha_core_live_smoke.py --timeout 180` -> `ok=true`, `core_mode=isolated`, Home Assistant `2026.8.3`, `sensor_count=16`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `event_count=1`, `unloaded=true`, `remaining_services=[]`; cleanup checks `docker ps/network/volume ls --filter name=pantryos-ha-core-smoke` -> no generated containers, networks, or volumes remained.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending",
      "phase": "Phase 7/H/J"
    },
    {
      "change": "Completed a Docker-backed release verification pass for the isolated Home Assistant Core smoke and hardened local tooling. scripts/ha_core_live_smoke.py now avoids live user tokens/databases by creating a disposable PantryOS Core container/network/volume with a fixed non-secret smoke token, the browser smoke now ignores invalid directory-style Python paths and requires a Python 3.12+ runtime, and the status/readiness wording records the old live-token HA path as superseded.",
      "commands": "`.\\.venv\\Scripts\\python.exe -m ruff format --check .` -> 70 files already formatted; `.\\.venv\\Scripts\\python.exe -m ruff check .` -> all checks passed; `.\\.venv\\Scripts\\python.exe -m mypy src custom_components\\pantryos scripts\\ha_core_live_smoke.py` -> no issues in 17 source files; `.\\.venv\\Scripts\\python.exe -m pytest -q` -> 96 passed; `.\\.venv\\Scripts\\python.exe -m coverage report` -> total coverage 83%, above 80% gate; `PANTRYOS_API_TOKEN=local-dev-token docker compose up -d --build --force-recreate pantryos` -> rebuilt `pantryos-pantryos:latest` and restarted `pantryos`; `python scripts/supply_chain_audit.py --write` -> `ok=true`, source files `70`, OS packages `157`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python scripts/check.py` -> python compile 44 files passed, 96 tests passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `PANTRYOS_API_TOKEN=e2e-smoke-token python scripts/smoke_e2e.py` -> `ok=true`, revision `22`, matching browser/HA lot id, leftover count `2`; `PANTRYOS_API_TOKEN=browser-smoke-token node scripts/browser_smoke.cjs` -> browser smoke passed for `phone` and `kitchen-tablet`; `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true`, Home Assistant `2026.8.3`, `stream_task_cancelled=true`; `python scripts/receipt_ocr_corpus_smoke.py --timeout 180` -> `ok=true`, `case_count=3`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, UID `10001`, backup `/app/data/backups/container-smoke-20260826113158.zip`, source/restored counts matched (`events=112`, `lots=53`, `products=50`, `purchases=21`, `recipes=4`); `python scripts/ha_core_live_smoke.py --timeout 180` -> `ok=true`, isolated Home Assistant `2026.8.3`, `sensor_count=16`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `unloaded=true`, `remaining_services=[]`; cleanup checks for `pantryos-ha-core-smoke` containers/networks/volumes -> none remained.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending",
      "phase": "Phase 7/8/H/I/J"
    },
    {
      "change": "Added scripts/concurrency_smoke.py, a dependency-free authenticated API durability smoke for A6, and sized the local PantryHTTPServer accept queue for the 20-request burst. The smoke starts a temporary threaded PantryOS Core server, seeds independent lots, releases 20 concurrent supported API mutations across inventory add/open/consume/discard endpoints, verifies every successful mutation in the final dashboard, checks revision and event-count deltas, and runs SQLite integrity verification against the resulting database. scripts/check.py, release readiness, and the Docker in-image verifier now include this smoke as a required gate.",
      "commands": "`python scripts/concurrency_smoke.py` -> `ok=true`, `successful_mutations=20`, `baseline_revision=24`, `final_revision=44`, `event_count=44`, `product_count=27`, `active_lot_count=23`; `docker exec --user 10001:10001 pantryos python scripts/check.py` -> python compile 45 files passed, 97 tests passed, api concurrency smoke 20 mutations passed, release checks current; `python scripts/container_smoke.py` -> `ok=true`, `uid=10001`, `revision_after_restart=120`, restored counts matched source counts; `python -m pytest -q` -> 97 passed; `python scripts/supply_chain_audit.py --write` -> `ok=true`, `source_file_count=71`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python -m mypy src custom_components\\pantryos scripts\\ha_core_live_smoke.py scripts\\concurrency_smoke.py` -> success; `python scripts/check.py` -> passed.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending",
      "phase": "Phase 1/2/8/J"
    },
    {
      "change": "Strengthened scripts/ha_core_live_smoke.py so the isolated live Home Assistant Core proof creates the PantryOS entry through Home Assistant's config-flow manager against the live disposable Core API instead of manually constructing a ConfigEntry. The smoke now records the flow result type and PantryOS instance unique ID before proving setup, services, event listener revision movement, sensors, unload, and cleanup. README and HA contract tests now require this config-flow path.",
      "commands": "`python scripts/ha_core_live_smoke.py --timeout 240` -> `ok=true`, `config_flow_result_type=create_entry`, `entry_unique_id=inst_ca167d95cf7a4c329ccb390b3ce46896`, Home Assistant `2026.8.3`, `core_mode=isolated`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `sensor_count=16`, `unloaded=true`, `remaining_services=[]`; cleanup checks `docker ps/network/volume ls --filter name=pantryos-ha-core-smoke` -> no generated containers, networks, or volumes remained; `python -m pytest tests/test_ha_contract.py -q` -> 11 passed; `python -m pytest -q` -> 97 passed; `python -m ruff format --check .` -> 71 files already formatted; `python -m ruff check .` -> all checks passed; `python -m mypy src custom_components\\pantryos scripts\\ha_core_live_smoke.py scripts\\concurrency_smoke.py` -> success; `docker compose up -d --build --force-recreate pantryos` -> rebuilt image and restarted container; `docker exec --user 10001:10001 pantryos python scripts/check.py` -> python compile 45 files passed, 97 tests passed, api concurrency smoke 20 mutations passed, release checks current; `python scripts/container_smoke.py` -> `ok=true`, `uid=10001`, `revision_after_restart=124`, restored counts matched source counts; `python scripts/check.py` -> passed; `python scripts/supply_chain_audit.py --write` -> `ok=true`, `source_file_count=71`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-clicked HA frontend config-entry proof if required, and final independent review refresh remain pending",
      "phase": "Phase 7/H/J"
    },
    {
      "change": "Fixed the live Home Assistant Core release smoke false-pass gap by requiring the final HA state_revision and total_items sensor states to match the coordinator snapshot after a direct Core mutation. Added scripts/manual_release_evidence.py, a release gate for external proof that validates docs/release/manual-validation.json against the current Git commit and requires PASS evidence for physical barcode camera, real receipt OCR, published image signature, and independent full review checks. Release readiness now lists the manual evidence validator as a required release command.",
      "commands": "`.\\.venv\\Scripts\\python.exe scripts\\ha_core_live_smoke.py --timeout 240` -> `ok=true`, Home Assistant `2026.8.3`, `config_flow_result_type=create_entry`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `sensor_count=16`, `unloaded=true`; `.\\.venv\\Scripts\\python.exe scripts\\check.py` -> python compile 46 files passed, 100 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\\.venv\\Scripts\\python.exe -m ruff format --check .` -> 72 files already formatted; `.\\.venv\\Scripts\\python.exe -m ruff check .` -> all checks passed; `.\\.venv\\Scripts\\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py` -> success; `.\\.venv\\Scripts\\python.exe scripts\\manual_release_evidence.py --json` -> `ok=false`, missing `docs/release/manual-validation.json` as the expected remaining release blocker.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final release tag, manual evidence JSON, release readiness PASS, and final independent review refresh remain pending",
      "phase": "Phase 7/8/H/I/J"
    },
    {
      "change": "Added an isolated release-review mode to scripts/container_smoke.py and made python scripts/container_smoke.py --isolated the required release command. The isolated mode writes a temporary Compose file under the checkout with the same hardened service contract, a generated container name, Docker-assigned host port, and generated data volume; it reruns the same live API, restart, backup/restore, and in-image verifier checks, then removes the generated Compose project resources.",
      "commands": "`.\\.venv\\Scripts\\python.exe scripts\\container_smoke.py --isolated --build-timeout 360 --ready-timeout 120` -> `ok=true`, `isolated=true`, Docker server `29.6.1`, generated base URL `http://127.0.0.1:50820`, generated container `pantryos-container-smoke-91b02b6a901b`, generated volume `pantryos-container-smoke-data-91b02b6a901b`, UID `10001`, `revision_after_restart=4`, backup `/app/data/backups/container-smoke-20260826131152.zip`, restore database `/app/data/container-smoke-restored-20260826131152.sqlite3`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`), in-image verifier completed with documented Node JS syntax skip; cleanup checks for generated isolated smoke containers/networks/volumes -> none remained; `.\\.venv\\Scripts\\python.exe scripts\\check.py` -> python compile 46 files passed, 100 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\\.venv\\Scripts\\python.exe -m ruff format --check .` -> 72 files already formatted; `.\\.venv\\Scripts\\python.exe -m ruff check .` -> all checks passed; `.\\.venv\\Scripts\\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py scripts/container_smoke.py` -> success.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final release tag, manual evidence JSON, release readiness PASS, and final independent review refresh remain pending",
      "phase": "Phase 8/I/J"
    },
    {
      "change": "Fixed the D9 generated shopping override gap and reconciled phase-gate status against current automated evidence and the superseding independent review for pre-fix HEAD 82d7f32. Meal-plan shopping rebuild now refreshes generated quantities without silently reactivating user-suppressed or removed generated rows; explicit user reactivation restores the row to accepted active demand. Phases 2, 3, 4, 5, and 7 are now marked complete from accumulated automated evidence; Phase 6, Phase 8, independent review, and release PASS remain open for manual/external release proof.",
      "commands": "`.\\.venv\\Scripts\\python.exe -m pytest tests\\core\\test_sqlite_core.py::test_meal_plan_rebuild_preserves_suppressed_generated_demand_until_reactivated -q` -> 1 passed; `.\\.venv\\Scripts\\python.exe scripts\\check.py` -> python compile 46 files passed, 101 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\\.venv\\Scripts\\python.exe -m ruff format --check .` -> 73 files already formatted; `.\\.venv\\Scripts\\python.exe -m ruff check .` -> all checks passed; `.\\.venv\\Scripts\\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py scripts/container_smoke.py` -> success in 21 source files; `.\\.venv\\Scripts\\python.exe scripts\\container_smoke.py --isolated --build-timeout 360 --ready-timeout 120` -> `ok=true`, Docker server `29.6.1`, generated base URL `http://127.0.0.1:57379`, generated container `pantryos-container-smoke-04a1643c62f9`, generated volume `pantryos-container-smoke-data-04a1643c62f9`, UID `10001`, `revision_after_restart=4`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`). `docs/reviews/2026-08-26-current-head-review.md` reviewed exact pre-fix HEAD `82d7f3284ea3457139e332c7cafb77a037ce3d52`, confirmed prior HA/container High findings resolved, found no open Critical/High findings, and reported release-blocking Medium gaps for physical barcode camera, representative real receipt OCR, published image signature, and missing manual release evidence JSON.",
      "date": "2026-08-26",
      "gap": "Physical-device camera/real-receipt validation, published-image signature verification after final tag, docs/release/manual-validation.json, final no-blocker independent review against the final commit, and release readiness PASS remain pending",
      "phase": "Phase 4/6/7/8/J"
    }
  ],
  "open_blockers": [
    "`docs/release/manual-validation.json` is absent. The manual release gate requires PASS evidence for `physical-barcode-camera`, `real-receipt-ocr`, `published-image-signature`, and `independent-full-review` on the final release commit.",
    "Physical-device barcode camera validation and representative real-receipt OCR/review/commit evidence remain pending. Deterministic browser/OCR automation exists, but these user-facing capture paths still need real-device/manual release artifacts.",
    "Published-image signature verification remains pending until the final tag/image is published and verified.",
    "The independent review at `docs/reviews/2026-08-26-current-head-review.md` reviewed pre-D9 HEAD `82d7f3284ea3457139e332c7cafb77a037ce3d52`, found no open Critical/High issues, but still reports release-blocking Medium evidence gaps; J7 cannot pass until a final review against the final commit reports no release-blocking Medium findings.",
    "Release readiness remains `NOT READY` until the manual evidence JSON validates, the final image signature evidence exists, every required release command is rerun, readiness is regenerated as PASS, and a final no-blocker independent review is recorded."
  ],
  "open_phase_gates": [
    "Phase 6 \u2014 barcode, receipts, purchases, and price history (implemented paths are automated; physical barcode camera and representative real-receipt evidence remain manual release blockers)",
    "Phase 8 \u2014 security, operations, UX, and release hardening (automated hardening is implemented; manual evidence JSON, published-image signature verification, final PASS readiness, and final no-blocker review remain pending)",
    "Independent full review completed (latest review covers pre-D9 HEAD `82d7f32` and reports release-blocking Medium evidence gaps; final PASS review against the final commit pending)",
    "Release gate PASS"
  ],
  "phase_gate_count": 11,
  "required_release_commands": [
    "python scripts/check.py",
    "python scripts/smoke_e2e.py",
    "python scripts/concurrency_smoke.py",
    "node scripts/browser_smoke.cjs",
    "python scripts/container_smoke.py --isolated",
    "python scripts/receipt_ocr_corpus_smoke.py",
    "python scripts/image_hardening_audit.py",
    "python scripts/supply_chain_audit.py",
    "python scripts/ha_installed_smoke.py",
    "python scripts/ha_core_live_smoke.py",
    "python scripts/release_artifact_audit.py",
    "python scripts/manual_release_evidence.py",
    "python scripts/release_readiness.py --check"
  ]
}
```

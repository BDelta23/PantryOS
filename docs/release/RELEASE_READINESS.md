# PantryOS release readiness

This file is generated by `python scripts/release_readiness.py --write` from the acceptance criteria and implementation status ledger.

## Decision

- Status: **NOT READY**
- Generated: `2026-08-26T08:28:11Z`
- Acceptance criteria in contract: `85`
- Phase gates in ledger: `11`

## Required Release Commands

- `python scripts/check.py`
- `python scripts/smoke_e2e.py`
- `node scripts/browser_smoke.cjs`
- `python scripts/container_smoke.py`
- `python scripts/image_hardening_audit.py`
- `python scripts/ha_installed_smoke.py`
- `python scripts/release_readiness.py --check`

## Open Phase Gates

- Phase 2 — inventory domain and authenticated API foundation (in progress; current HA-used routes are authenticated under `/api/v1`, documented by served OpenAPI, and browser inventory add/open/consume/move/discard plus product settings actions call Core routes)
- Phase 3 — web + Home Assistant one-source-of-truth proof (in progress; automated web/API-to-HA-client sync proof added)
- Phase 4 — recipes, use-soon, meal plan, idempotent shopping (in progress; recipe add/edit/retire UI, shopping rebuild, edit/check/remove, complete-purchase API, and browser purchase workflow added)
- Phase 5 — cooking, leftovers, waste, and location value (in progress; cooking sessions, leftover creation, waste metrics, location value surfaces, and browser cooking workflow and open-lot shelf-life policy added)
- Phase 6 — barcode, receipts, purchases, and price history (in progress; barcode mapping, browser camera feature detection/manual fallback, structured receipt text/image review/commit, local PNG/JPEG OCR, upload limits/retention cleanup, purchase history, price history, median-based anomaly output, and browser purchase/price analysis panel added)
- Phase 7 — complete Home Assistant surface (in progress; expanded API client, service surface, runtime coordinator/cache, continuous SSE background subscription with audit fallback, setup/unload/auth-failure/reconfigure/reauth runtime coverage, leftover/revision sensors, diagnostics redaction helpers, typed update events, example automation docs, and installed HA Docker runtime smoke added)
- Phase 8 — security, operations, UX, and release hardening (in progress; OpenAPI contract, authenticated bounded event stream, request/upload limits and receipt image/OCR timeout/rate limits/retention cleanup, persistent browser session/CSRF/same-origin/Secure-cookie controls, token setup UX, PWA install/offline policy, structured redacted request logs, doctor/backup/restore/import dry-run/purge operations, upload-inclusive backup archives, automatic migration-failure recovery, read-only-root Docker runtime/checks, explicit data/backup path allowlists, browser viewport/accessibility smoke automation with expanded axe-style checks, scripted cross-surface release demo, automated Docker release smoke, image hardening audit, and generated release-readiness ledger added)
- Independent full review completed
- Release gate PASS

## Residual Risks

- Home Assistant uses the PantryOS Core API setup path, stores runtime client/coordinator state on the config entry, keeps entity properties on a coordinator snapshot, runs a continuous background SSE stream subscription with event-audit and snapshot recovery fallback, emits typed `pantryos_updated` payload metadata for recent Core events, documents H10 example automations, supports reconfigure and reauth for URL/token changes, maps setup auth failures to credential recovery, has dependency-light tests for setup/unload, service action refresh, auth failure, stream listener behavior, update event metadata, reconfigure, and reauth, and has a Docker-backed installed Home Assistant smoke for imports, setup, background stream task lifecycle, service handlers, unload, and auth-failure behavior. A full live HA Core config-entry UI/end-to-end harness is still pending.
- Browser compatibility routes now require same-site browser session cookies, per-session CSRF on unsafe methods, same-origin browser requests, restart-persistent file-backed browser sessions, explicit HTTPS/Secure-cookie deployment mode, and setup-token configuration status in the sign-in UI. API JSON body, receipt upload MIME/size/path policies, and receipt upload/extract rate limits now exist for the implemented local text/CSV/image receipt path.
- Browser visual automation now has deterministic Playwright evidence for phone and kitchen-tablet viewports covering token setup status, login, inventory, location settings save, barcode manual fallback, recipe edit, meal planning, cooking leftovers, receipt review/commit, purchase visibility, horizontal overflow, keyboard focus on a destructive control, and expanded axe-style accessibility checks across login, initial app, and completed workflow states. The audit covers landmarks, labels, ARIA references, media labels, live regions, focusable target sizing, focus styling, duplicate IDs, and text contrast; it caught and fixed a warning-badge contrast failure. A scripted release demo now proves add -> sync -> plan -> shop -> cook -> leftover -> use-soon through browser session routes and the HA API client. Remaining browser proof gaps are physical-device camera validation and a broader installed cross-interface harness; third-party axe-core was not added because the current repo has no Node package toolchain.
- OpenAPI 3.1 is served from authenticated `GET /api/v1/openapi.json` and route/security/problem shape is tested; richer per-resource schema precision and release readiness PASS remains pending.
- Events are persisted and authenticated `/api/v1/events` returns a bounded SSE hello/replay/live revision stream with heartbeat and cursor support; HA now keeps a continuous background subscription that reconnects across bounded stream windows, exposes bounded recent event metadata on `pantryos_updated`, and falls back to the authenticated event audit plus snapshot refresh. A full live HA Core config-entry UI/end-to-end harness remains pending.
- Local receipt image/OCR ingestion now supports reviewed PNG/JPEG uploads through the documented `content_base64` API and Docker-provided `tesseract-ocr`; broader real-world OCR corpus coverage remains a release-hardening gap. Receipt upload retention now purges old uncommitted private payload files with dry-run support while retaining committed purchase evidence. Browser inventory lifecycle controls now cover add, consume, move, and discard against Core routes; browser location settings now rename/move hierarchical location records without rewriting lot references; browser product settings now edit category, default unit, minimum stock, preferred location, and shelf-life defaults against Core product records; browser recipes can be added, edited, and retired with active-plan protection; browser inventory lots can be opened with the product opened shelf-life policy; browser barcode camera feature detection now has deterministic adapter-backed capture/cleanup automation with manual fallback preserved, and the browser can inspect purchase history and product price anomaly analysis backed by the documented recent-median compatible-unit baseline with ratio/status/sample evidence.
- CLI doctor/backup/restore/import dry-run/purge-receipts operations exist and are locally/container smoke-tested; `.zip` backups now include non-purged private receipt upload files and restore them into the destination data directory; failed pending migrations now restore an automatic pre-migration backup and leave a `.failed` copy for inspection.
- Docker now starts through a root entrypoint only long enough to repair `/app/data` ownership, then runs PantryOS as UID/GID `10001`; Compose uses a read-only root filesystem, drops all capabilities by default with only startup `CHOWN`/`SETGID`/`SETUID` added, sets `pids_limit: 256`, uses `/tmp` tmpfs, `no-new-privileges`, and env-enforced data/backup path allowlists. `scripts/container_smoke.py` automates the release container smoke, and `scripts/image_hardening_audit.py` verifies static Docker controls plus live image/container/runtime properties. Remaining supply-chain hardening review items include digest pinning, SBOM generation, and signing policy.
- Completion acceptance criteria are not fully evidenced.

## Latest Evidence

- 2026-08-26 `Phase 8/G/I/J`: Polished browser token setup UX: /api/session reports non-secret setup_token_configured metadata, the sign-in panel starts disabled until session configuration is known, displays local cookie policy, gives specific missing-token and invalid-token errors, preserves problem codes in client errors, supports a show/hide token control, and keeps token material out of persisted session state.
  Evidence: `python scripts/check.py` -> python compile: 32 files passed; 78 tests passed; javascript syntax: 2 files passed; Playwright browser smoke with bundled Python/Node -> setup-token status plus show/hide toggle and existing vertical slice passed for `phone` and `kitchen-tablet`; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose up -d --build --force-recreate` -> image rebuilt and container started; `docker exec pantryos python scripts/check.py` -> python compile: 32 files passed, 78 tests passed, javascript syntax skipped because node is not in image; live Docker `GET /api/session` -> `setup_token_configured=true`, `cookie.same_site=Lax`, `cookie.secure=false`; `docker inspect pantryos` -> `running healthy`.
  Remaining: Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending
- 2026-08-26 `Phase 3/4/5/8/J`: Added scripts/smoke_e2e.py, a scripted release demo that starts a temporary PantryOS server, signs in through the browser session surface, adds an expiring lot through /api/items, verifies the same lot through the Home Assistant API client, creates/plans a recipe, adds missing shopping demand, completes a purchase, starts/completes cooking with confirmed allocation and leftovers, verifies browser-visible meal plan/leftovers/use-soon state, and checks cooking events. Registered the script in the dependency-free test runner and documented the command in README.
  Evidence: `PANTRYOS_API_TOKEN=e2e-smoke-token python scripts/smoke_e2e.py` -> `ok=true`, browser-created lot id matched HA-synced lot id, purchase/session ids returned, use-soon included `Smoke Rice` and `Smoke Rice Bowl Leftovers`; `python scripts/check.py` -> python compile: 34 files passed; 79 tests passed; javascript syntax: 2 files passed; Playwright browser smoke with bundled Python/Node -> `browser smoke passed` for `phone` and `kitchen-tablet`; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose up -d --build --force-recreate` -> image rebuilt and container started; `docker exec pantryos python scripts/check.py` -> python compile: 34 files passed, 79 tests passed, javascript syntax skipped because node is not in image; `docker exec -e PANTRYOS_API_TOKEN=e2e-smoke-token pantryos python scripts/smoke_e2e.py` -> `ok=true` with matching browser/HA lot ids; Docker readiness `GET /api/v1/health/ready` -> `{"status":"ready"}`; `docker inspect pantryos` -> `running healthy`.
  Remaining: Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending
- 2026-08-26 `Phase 8/I/J`: Added scripts/container_smoke.py, a dependency-free Docker release smoke runner that validates Compose config, builds and starts the service, waits for readiness, checks the actual PantryOS process UID/GID and no-new-privileges, confirms read-only-root behavior, proves bearer-token auth, creates inventory and receipt state through the live API, restarts against the same Docker volume, creates a receipt-inclusive backup archive, restores it into a second database under /app/data, compares source/restored counts, and runs scripts/check.py inside the image. README now documents the command as the Docker release smoke.
  Evidence: `python scripts/check.py` -> python compile: 35 files passed; 80 tests passed; javascript syntax: 2 files passed. `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, readiness `ready`, created `Container Smoke Rice 20260826065932`, extracted/committed `Container Smoke Beans 20260826065932`, backup `/app/data/backups/container-smoke-20260826065932.zip`, restore database `/app/data/container-smoke-restored-20260826065932.sqlite3`, source/restored counts matched (`events=56`, `lots=25`, `products=22`, `purchases=7`, `recipes=4`), backup/restore and in-image verifier ran as UID/GID `10001:10001`, and JS syntax was skipped inside the image because node is not installed there.
  Remaining: Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, broader image hardening review, and independent review remain pending
- 2026-08-26 `Phase 8/G/J`: Expanded scripts/browser_smoke.cjs from form/control checks into a broader axe-style browser accessibility audit across login, initial app, and completed workflow states. The audit now checks visible main/header/h1 landmarks, document language/title, duplicate IDs, missing ARIA references, unexpected roles, visible form labels, button names, media labels, live status regions, focusable target size, visible focus styling, and text contrast. The login surface now uses a visible main landmark, barcode camera preview has an accessible label, warning badge text was darkened after the audit caught a 3.76:1 contrast failure, and online browser smoke blocks service workers so offline-handler tests stay isolated.
  Evidence: `python scripts/check.py` -> python compile: 35 files passed; 80 tests passed; javascript syntax: 2 files passed. `PANTRYOS_API_TOKEN=browser-smoke-token node scripts/browser_smoke.cjs` with bundled Node/Python -> `browser smoke passed` for `phone` and `kitchen-tablet` with expanded login/initial/completed accessibility audits. `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, readiness `ready`, created `Container Smoke Rice 20260826071620`, extracted/committed `Container Smoke Beans 20260826071620`, backup `/app/data/backups/container-smoke-20260826071620.zip`, restore database `/app/data/container-smoke-restored-20260826071620.sqlite3`, source/restored counts matched (`events=64`, `lots=29`, `products=26`, `purchases=9`, `recipes=4`).
  Remaining: Physical-device camera validation, full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, release readiness PASS, broader image hardening review, and independent review remain pending
- 2026-08-26 `Phase 7/H/J`: Added scripts/ha_installed_smoke.py, a Docker-backed installed Home Assistant runtime smoke that creates a temporary HA config, copies custom_components/pantryos, imports the integration inside the HA Python environment, and exercises setup, sensor platform forwarding, bounded event polling/update metadata, actual service schemas and add/open handlers, unload cleanup, and auth-failure recovery against a fake PantryOS client. README documents the command and PANTRYOS_HA_IMAGE override.
  Evidence: `python scripts/check.py` -> python compile: 36 files passed; 81 tests passed; javascript syntax: 2 files passed. `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true` against Home Assistant `2026.8.3`; services registered/removed: `add_item`, `consume_item`, `delete_item`, `discard_item`, `move_item`, `open_item`, `add_recipe`, `plan_meal`, `add_shopping_item`, `add_missing_to_shopping_list`, `rebuild_shopping`, `promote_suggested_purchases`, `start_cooking`, `complete_cooking`, and `cancel_cooking`; `last_event_types=[cooking.started]`, `last_revision=4`.
  Remaining: Full live HA Core config-entry UI/end-to-end harness, physical-device camera validation, unbounded WebSocket-style push fanout, release readiness PASS, broader image hardening review, and independent review remain pending
- 2026-08-26 `Phase 7/H/J`: Replaced the Home Assistant interval-style event refresh with a continuous background SSE subscription task stored on entry.runtime_data; the coordinator now loops across bounded stream windows, refreshes snapshots on newer revisions, emits pantryos_updated through an async callback, falls back to event-audit/snapshot recovery on stream errors, and cancels the task during unload.
  Evidence: `python scripts/check.py` -> python compile: 36 files passed; 82 tests passed; javascript syntax: 2 files passed. `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true` against Home Assistant `2026.8.3`, `last_event_types=[cooking.started]`, `last_revision=4`, `stream_task_cancelled=true`, sensor platform forwarded, and all PantryOS services registered/removed.
  Remaining: Full live HA Core config-entry UI/end-to-end harness, physical-device camera validation, release readiness PASS, broader image hardening review, and independent review remain pending
- 2026-08-26 `Phase 8/J`: Added scripts/release_readiness.py and generated docs/release/RELEASE_READINESS.md. The readiness artifact parses the binding acceptance criteria and implementation status ledger, records required release commands, latest evidence, open phase gates, and residual risks, and stays NOT READY until phase gates and blockers are cleared. scripts/check.py now enforces that the generated readiness document is current.
  Evidence: `python scripts/release_readiness.py --write --json` -> decision `NOT READY`, acceptance criteria `85`, phase gates `11`, open phase gates `9`. `python scripts/check.py` -> python compile: 37 files passed; 83 tests passed; `release readiness: current`; javascript syntax: 2 files passed.
  Remaining: Release readiness PASS, physical-device camera validation, full live HA Core config-entry UI/end-to-end harness, broader image hardening review, and independent review remain pending
- 2026-08-26 `Phase 8/I/J`: Added scripts/image_hardening_audit.py and hardened Compose with cap_drop: ALL, minimal startup-only CHOWN/SETGID/SETUID capability additions, and pids_limit: 256; the audit checks Dockerfile, packaged .dockerignore, rendered Compose, image metadata, live container settings, writable mounts, runtime env, UID/GID, NoNewPrivs, and process effective capabilities.
  Evidence: `python scripts/image_hardening_audit.py --skip-live` -> `ok=true`, `check_count=27`; `docker compose up -d --build --force-recreate pantryos` -> image rebuilt and container started; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=43`, container healthy, `CapDrop=ALL`, minimal startup `CapAdd`, `PidsLimit=256`, single writable `/app/data` volume, UID/GID `10001`, and zero effective Linux capabilities after privilege drop; `python scripts/run_tests.py` -> 86 tests passed; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, backup `/app/data/backups/container-smoke-20260826082654.zip`, restore database `/app/data/container-smoke-restored-20260826082654.sqlite3`, source/restored counts matched (`events=72`, `lots=33`, `products=30`, `purchases=11`, `recipes=4`), and the in-image verifier completed with JS syntax skipped because node is not installed there.
  Remaining: Physical-device camera validation, full live HA Core config-entry UI/end-to-end harness, release readiness PASS, supply-chain hardening items such as digest pinning/SBOM/signature policy, and independent review remain pending

## Machine Summary

```json
{
  "acceptance_criteria_count": 85,
  "decision": "NOT READY",
  "generated_at": "2026-08-26T08:28:11Z",
  "latest_evidence": [
    {
      "change": "Polished browser token setup UX: /api/session reports non-secret setup_token_configured metadata, the sign-in panel starts disabled until session configuration is known, displays local cookie policy, gives specific missing-token and invalid-token errors, preserves problem codes in client errors, supports a show/hide token control, and keeps token material out of persisted session state.",
      "commands": "`python scripts/check.py` -> python compile: 32 files passed; 78 tests passed; javascript syntax: 2 files passed; Playwright browser smoke with bundled Python/Node -> setup-token status plus show/hide toggle and existing vertical slice passed for `phone` and `kitchen-tablet`; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose up -d --build --force-recreate` -> image rebuilt and container started; `docker exec pantryos python scripts/check.py` -> python compile: 32 files passed, 78 tests passed, javascript syntax skipped because node is not in image; live Docker `GET /api/session` -> `setup_token_configured=true`, `cookie.same_site=Lax`, `cookie.secure=false`; `docker inspect pantryos` -> `running healthy`.",
      "date": "2026-08-26",
      "gap": "Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending",
      "phase": "Phase 8/G/I/J"
    },
    {
      "change": "Added scripts/smoke_e2e.py, a scripted release demo that starts a temporary PantryOS server, signs in through the browser session surface, adds an expiring lot through /api/items, verifies the same lot through the Home Assistant API client, creates/plans a recipe, adds missing shopping demand, completes a purchase, starts/completes cooking with confirmed allocation and leftovers, verifies browser-visible meal plan/leftovers/use-soon state, and checks cooking events. Registered the script in the dependency-free test runner and documented the command in README.",
      "commands": "`PANTRYOS_API_TOKEN=e2e-smoke-token python scripts/smoke_e2e.py` -> `ok=true`, browser-created lot id matched HA-synced lot id, purchase/session ids returned, use-soon included `Smoke Rice` and `Smoke Rice Bowl Leftovers`; `python scripts/check.py` -> python compile: 34 files passed; 79 tests passed; javascript syntax: 2 files passed; Playwright browser smoke with bundled Python/Node -> `browser smoke passed` for `phone` and `kitchen-tablet`; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose up -d --build --force-recreate` -> image rebuilt and container started; `docker exec pantryos python scripts/check.py` -> python compile: 34 files passed, 79 tests passed, javascript syntax skipped because node is not in image; `docker exec -e PANTRYOS_API_TOKEN=e2e-smoke-token pantryos python scripts/smoke_e2e.py` -> `ok=true` with matching browser/HA lot ids; Docker readiness `GET /api/v1/health/ready` -> `{\"status\":\"ready\"}`; `docker inspect pantryos` -> `running healthy`.",
      "date": "2026-08-26",
      "gap": "Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending",
      "phase": "Phase 3/4/5/8/J"
    },
    {
      "change": "Added scripts/container_smoke.py, a dependency-free Docker release smoke runner that validates Compose config, builds and starts the service, waits for readiness, checks the actual PantryOS process UID/GID and no-new-privileges, confirms read-only-root behavior, proves bearer-token auth, creates inventory and receipt state through the live API, restarts against the same Docker volume, creates a receipt-inclusive backup archive, restores it into a second database under /app/data, compares source/restored counts, and runs scripts/check.py inside the image. README now documents the command as the Docker release smoke.",
      "commands": "`python scripts/check.py` -> python compile: 35 files passed; 80 tests passed; javascript syntax: 2 files passed. `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, readiness `ready`, created `Container Smoke Rice 20260826065932`, extracted/committed `Container Smoke Beans 20260826065932`, backup `/app/data/backups/container-smoke-20260826065932.zip`, restore database `/app/data/container-smoke-restored-20260826065932.sqlite3`, source/restored counts matched (`events=56`, `lots=25`, `products=22`, `purchases=7`, `recipes=4`), backup/restore and in-image verifier ran as UID/GID `10001:10001`, and JS syntax was skipped inside the image because node is not installed there.",
      "date": "2026-08-26",
      "gap": "Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, broader image hardening review, and independent review remain pending",
      "phase": "Phase 8/I/J"
    },
    {
      "change": "Expanded scripts/browser_smoke.cjs from form/control checks into a broader axe-style browser accessibility audit across login, initial app, and completed workflow states. The audit now checks visible main/header/h1 landmarks, document language/title, duplicate IDs, missing ARIA references, unexpected roles, visible form labels, button names, media labels, live status regions, focusable target size, visible focus styling, and text contrast. The login surface now uses a visible main landmark, barcode camera preview has an accessible label, warning badge text was darkened after the audit caught a 3.76:1 contrast failure, and online browser smoke blocks service workers so offline-handler tests stay isolated.",
      "commands": "`python scripts/check.py` -> python compile: 35 files passed; 80 tests passed; javascript syntax: 2 files passed. `PANTRYOS_API_TOKEN=browser-smoke-token node scripts/browser_smoke.cjs` with bundled Node/Python -> `browser smoke passed` for `phone` and `kitchen-tablet` with expanded login/initial/completed accessibility audits. `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, readiness `ready`, created `Container Smoke Rice 20260826071620`, extracted/committed `Container Smoke Beans 20260826071620`, backup `/app/data/backups/container-smoke-20260826071620.zip`, restore database `/app/data/container-smoke-restored-20260826071620.sqlite3`, source/restored counts matched (`events=64`, `lots=29`, `products=26`, `purchases=9`, `recipes=4`).",
      "date": "2026-08-26",
      "gap": "Physical-device camera validation, full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, release readiness PASS, broader image hardening review, and independent review remain pending",
      "phase": "Phase 8/G/J"
    },
    {
      "change": "Added scripts/ha_installed_smoke.py, a Docker-backed installed Home Assistant runtime smoke that creates a temporary HA config, copies custom_components/pantryos, imports the integration inside the HA Python environment, and exercises setup, sensor platform forwarding, bounded event polling/update metadata, actual service schemas and add/open handlers, unload cleanup, and auth-failure recovery against a fake PantryOS client. README documents the command and PANTRYOS_HA_IMAGE override.",
      "commands": "`python scripts/check.py` -> python compile: 36 files passed; 81 tests passed; javascript syntax: 2 files passed. `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true` against Home Assistant `2026.8.3`; services registered/removed: `add_item`, `consume_item`, `delete_item`, `discard_item`, `move_item`, `open_item`, `add_recipe`, `plan_meal`, `add_shopping_item`, `add_missing_to_shopping_list`, `rebuild_shopping`, `promote_suggested_purchases`, `start_cooking`, `complete_cooking`, and `cancel_cooking`; `last_event_types=[cooking.started]`, `last_revision=4`.",
      "date": "2026-08-26",
      "gap": "Full live HA Core config-entry UI/end-to-end harness, physical-device camera validation, unbounded WebSocket-style push fanout, release readiness PASS, broader image hardening review, and independent review remain pending",
      "phase": "Phase 7/H/J"
    },
    {
      "change": "Replaced the Home Assistant interval-style event refresh with a continuous background SSE subscription task stored on entry.runtime_data; the coordinator now loops across bounded stream windows, refreshes snapshots on newer revisions, emits pantryos_updated through an async callback, falls back to event-audit/snapshot recovery on stream errors, and cancels the task during unload.",
      "commands": "`python scripts/check.py` -> python compile: 36 files passed; 82 tests passed; javascript syntax: 2 files passed. `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true` against Home Assistant `2026.8.3`, `last_event_types=[cooking.started]`, `last_revision=4`, `stream_task_cancelled=true`, sensor platform forwarded, and all PantryOS services registered/removed.",
      "date": "2026-08-26",
      "gap": "Full live HA Core config-entry UI/end-to-end harness, physical-device camera validation, release readiness PASS, broader image hardening review, and independent review remain pending",
      "phase": "Phase 7/H/J"
    },
    {
      "change": "Added scripts/release_readiness.py and generated docs/release/RELEASE_READINESS.md. The readiness artifact parses the binding acceptance criteria and implementation status ledger, records required release commands, latest evidence, open phase gates, and residual risks, and stays NOT READY until phase gates and blockers are cleared. scripts/check.py now enforces that the generated readiness document is current.",
      "commands": "`python scripts/release_readiness.py --write --json` -> decision `NOT READY`, acceptance criteria `85`, phase gates `11`, open phase gates `9`. `python scripts/check.py` -> python compile: 37 files passed; 83 tests passed; `release readiness: current`; javascript syntax: 2 files passed.",
      "date": "2026-08-26",
      "gap": "Release readiness PASS, physical-device camera validation, full live HA Core config-entry UI/end-to-end harness, broader image hardening review, and independent review remain pending",
      "phase": "Phase 8/J"
    },
    {
      "change": "Added scripts/image_hardening_audit.py and hardened Compose with cap_drop: ALL, minimal startup-only CHOWN/SETGID/SETUID capability additions, and pids_limit: 256; the audit checks Dockerfile, packaged .dockerignore, rendered Compose, image metadata, live container settings, writable mounts, runtime env, UID/GID, NoNewPrivs, and process effective capabilities.",
      "commands": "`python scripts/image_hardening_audit.py --skip-live` -> `ok=true`, `check_count=27`; `docker compose up -d --build --force-recreate pantryos` -> image rebuilt and container started; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=43`, container healthy, `CapDrop=ALL`, minimal startup `CapAdd`, `PidsLimit=256`, single writable `/app/data` volume, UID/GID `10001`, and zero effective Linux capabilities after privilege drop; `python scripts/run_tests.py` -> 86 tests passed; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, backup `/app/data/backups/container-smoke-20260826082654.zip`, restore database `/app/data/container-smoke-restored-20260826082654.sqlite3`, source/restored counts matched (`events=72`, `lots=33`, `products=30`, `purchases=11`, `recipes=4`), and the in-image verifier completed with JS syntax skipped because node is not installed there.",
      "date": "2026-08-26",
      "gap": "Physical-device camera validation, full live HA Core config-entry UI/end-to-end harness, release readiness PASS, supply-chain hardening items such as digest pinning/SBOM/signature policy, and independent review remain pending",
      "phase": "Phase 8/I/J"
    }
  ],
  "open_blockers": [
    "Home Assistant uses the PantryOS Core API setup path, stores runtime client/coordinator state on the config entry, keeps entity properties on a coordinator snapshot, runs a continuous background SSE stream subscription with event-audit and snapshot recovery fallback, emits typed `pantryos_updated` payload metadata for recent Core events, documents H10 example automations, supports reconfigure and reauth for URL/token changes, maps setup auth failures to credential recovery, has dependency-light tests for setup/unload, service action refresh, auth failure, stream listener behavior, update event metadata, reconfigure, and reauth, and has a Docker-backed installed Home Assistant smoke for imports, setup, background stream task lifecycle, service handlers, unload, and auth-failure behavior. A full live HA Core config-entry UI/end-to-end harness is still pending.",
    "Browser compatibility routes now require same-site browser session cookies, per-session CSRF on unsafe methods, same-origin browser requests, restart-persistent file-backed browser sessions, explicit HTTPS/Secure-cookie deployment mode, and setup-token configuration status in the sign-in UI. API JSON body, receipt upload MIME/size/path policies, and receipt upload/extract rate limits now exist for the implemented local text/CSV/image receipt path.",
    "Browser visual automation now has deterministic Playwright evidence for phone and kitchen-tablet viewports covering token setup status, login, inventory, location settings save, barcode manual fallback, recipe edit, meal planning, cooking leftovers, receipt review/commit, purchase visibility, horizontal overflow, keyboard focus on a destructive control, and expanded axe-style accessibility checks across login, initial app, and completed workflow states. The audit covers landmarks, labels, ARIA references, media labels, live regions, focusable target sizing, focus styling, duplicate IDs, and text contrast; it caught and fixed a warning-badge contrast failure. A scripted release demo now proves add -> sync -> plan -> shop -> cook -> leftover -> use-soon through browser session routes and the HA API client. Remaining browser proof gaps are physical-device camera validation and a broader installed cross-interface harness; third-party axe-core was not added because the current repo has no Node package toolchain.",
    "OpenAPI 3.1 is served from authenticated `GET /api/v1/openapi.json` and route/security/problem shape is tested; richer per-resource schema precision and release readiness PASS remains pending.",
    "Events are persisted and authenticated `/api/v1/events` returns a bounded SSE hello/replay/live revision stream with heartbeat and cursor support; HA now keeps a continuous background subscription that reconnects across bounded stream windows, exposes bounded recent event metadata on `pantryos_updated`, and falls back to the authenticated event audit plus snapshot refresh. A full live HA Core config-entry UI/end-to-end harness remains pending.",
    "Local receipt image/OCR ingestion now supports reviewed PNG/JPEG uploads through the documented `content_base64` API and Docker-provided `tesseract-ocr`; broader real-world OCR corpus coverage remains a release-hardening gap. Receipt upload retention now purges old uncommitted private payload files with dry-run support while retaining committed purchase evidence. Browser inventory lifecycle controls now cover add, consume, move, and discard against Core routes; browser location settings now rename/move hierarchical location records without rewriting lot references; browser product settings now edit category, default unit, minimum stock, preferred location, and shelf-life defaults against Core product records; browser recipes can be added, edited, and retired with active-plan protection; browser inventory lots can be opened with the product opened shelf-life policy; browser barcode camera feature detection now has deterministic adapter-backed capture/cleanup automation with manual fallback preserved, and the browser can inspect purchase history and product price anomaly analysis backed by the documented recent-median compatible-unit baseline with ratio/status/sample evidence.",
    "CLI doctor/backup/restore/import dry-run/purge-receipts operations exist and are locally/container smoke-tested; `.zip` backups now include non-purged private receipt upload files and restore them into the destination data directory; failed pending migrations now restore an automatic pre-migration backup and leave a `.failed` copy for inspection.",
    "Docker now starts through a root entrypoint only long enough to repair `/app/data` ownership, then runs PantryOS as UID/GID `10001`; Compose uses a read-only root filesystem, drops all capabilities by default with only startup `CHOWN`/`SETGID`/`SETUID` added, sets `pids_limit: 256`, uses `/tmp` tmpfs, `no-new-privileges`, and env-enforced data/backup path allowlists. `scripts/container_smoke.py` automates the release container smoke, and `scripts/image_hardening_audit.py` verifies static Docker controls plus live image/container/runtime properties. Remaining supply-chain hardening review items include digest pinning, SBOM generation, and signing policy.",
    "Completion acceptance criteria are not fully evidenced."
  ],
  "open_phase_gates": [
    "Phase 2 \u2014 inventory domain and authenticated API foundation (in progress; current HA-used routes are authenticated under `/api/v1`, documented by served OpenAPI, and browser inventory add/open/consume/move/discard plus product settings actions call Core routes)",
    "Phase 3 \u2014 web + Home Assistant one-source-of-truth proof (in progress; automated web/API-to-HA-client sync proof added)",
    "Phase 4 \u2014 recipes, use-soon, meal plan, idempotent shopping (in progress; recipe add/edit/retire UI, shopping rebuild, edit/check/remove, complete-purchase API, and browser purchase workflow added)",
    "Phase 5 \u2014 cooking, leftovers, waste, and location value (in progress; cooking sessions, leftover creation, waste metrics, location value surfaces, and browser cooking workflow and open-lot shelf-life policy added)",
    "Phase 6 \u2014 barcode, receipts, purchases, and price history (in progress; barcode mapping, browser camera feature detection/manual fallback, structured receipt text/image review/commit, local PNG/JPEG OCR, upload limits/retention cleanup, purchase history, price history, median-based anomaly output, and browser purchase/price analysis panel added)",
    "Phase 7 \u2014 complete Home Assistant surface (in progress; expanded API client, service surface, runtime coordinator/cache, continuous SSE background subscription with audit fallback, setup/unload/auth-failure/reconfigure/reauth runtime coverage, leftover/revision sensors, diagnostics redaction helpers, typed update events, example automation docs, and installed HA Docker runtime smoke added)",
    "Phase 8 \u2014 security, operations, UX, and release hardening (in progress; OpenAPI contract, authenticated bounded event stream, request/upload limits and receipt image/OCR timeout/rate limits/retention cleanup, persistent browser session/CSRF/same-origin/Secure-cookie controls, token setup UX, PWA install/offline policy, structured redacted request logs, doctor/backup/restore/import dry-run/purge operations, upload-inclusive backup archives, automatic migration-failure recovery, read-only-root Docker runtime/checks, explicit data/backup path allowlists, browser viewport/accessibility smoke automation with expanded axe-style checks, scripted cross-surface release demo, automated Docker release smoke, image hardening audit, and generated release-readiness ledger added)",
    "Independent full review completed",
    "Release gate PASS"
  ],
  "phase_gate_count": 11,
  "required_release_commands": [
    "python scripts/check.py",
    "python scripts/smoke_e2e.py",
    "node scripts/browser_smoke.cjs",
    "python scripts/container_smoke.py",
    "python scripts/image_hardening_audit.py",
    "python scripts/ha_installed_smoke.py",
    "python scripts/release_readiness.py --check"
  ]
}
```

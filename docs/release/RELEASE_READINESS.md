# PantryOS release readiness

This file is generated by `python scripts/release_readiness.py --write` from the acceptance criteria and implementation status ledger.

## Decision

- Status: **NOT READY**
- Generated: `2026-08-28T04:29:39Z`
- Acceptance criteria in contract: `85`
- Open acceptance criteria: `25`
- Phase gates in ledger: `11`

## Required Release Commands

- `python scripts/check.py`
- `python scripts/smoke_e2e.py`
- `python scripts/concurrency_smoke.py`
- `node scripts/browser_smoke.cjs`
- `python scripts/container_smoke.py --isolated`
- `python scripts/receipt_ocr_corpus_smoke.py`
- `python scripts/image_hardening_audit.py`
- `python scripts/supply_chain_audit.py`
- `python scripts/ha_installed_smoke.py`
- `python scripts/ha_core_live_smoke.py`
- `python scripts/release_artifact_audit.py`
- `python scripts/manual_release_evidence.py`
- `python scripts/release_readiness.py --check`

## Open Acceptance Criteria

- G1. All primary inventory, recipe, plan, shopping, cooking, barcode, receipt, and settings workflows are reachable in the UI.
- G2. No visible control is a toast-only stub or hard-coded success simulation.
- G3. Loading, empty, offline, validation, conflict, and server-error states are clear.
- G4. Destructive actions require appropriate confirmation and remain keyboard accessible.
- G5. Critical workflows pass automated browser tests at phone and kitchen-tablet viewport sizes.
- G6. Forms have labels, focus handling, error association, and no critical automated accessibility violations.
- G7. PWA install metadata and an intentional offline/error strategy exist; offline writes are not falsely reported as committed.
- I1. Unauthorized API mutation and event subscription fail.
- I2. Browser authentication/session, CSRF, CORS, origin, and cookie policy are explicit and tested.
- I3. Request body, upload size, filename/path, MIME/content, and rate limits are enforced.
- I4. No secrets appear in source, logs, client errors, diagnostics, fixtures, or generated artifacts.
- I5. Container runs as non-root and writes only intended volume paths.
- I6. Liveness and readiness have different semantics and tests.
- I7. Structured logs include request/correlation IDs without sensitive payloads.
- I8. Migration failure leaves the prior database/backup recoverable.
- I9. `pantryos doctor`, backup, restore verification, and legacy dry-run are documented and smoke-tested.
- I10. Core operation is usable without internet access after dependencies/images are installed.
- J1. Unit, integration, API, migration, concurrency, HA, browser, and container smoke suites pass from a clean checkout.
- J2. Lint, format, and type checks pass.
- J3. Domain/application code has meaningful coverage, including failure paths; the configured threshold passes without broad exclusions.
- J4. OpenAPI, user setup, Home Assistant setup, backup/restore, migration, and troubleshooting docs match actual behavior.
- J5. No completion-critical TODO, placeholder URL, fake response, disabled test, or skipped release check remains without a documented non-blocking reason.
- J6. A scripted demo proves add → sync → plan → shop → cook → leftover → use-soon across the supported surfaces.
- J7. Independent review reports no open Critical/High findings and no release-blocking Medium finding.
- J8. `docs/release/RELEASE_READINESS.md` records exact commands, results, residual risks, and a justified `PASS`.

## Open Phase Gates

- Phase 6 — barcode, receipts, purchases, and price history (implemented paths are automated; physical barcode camera and representative real-receipt evidence remain manual release blockers)
- Phase 8 — security, operations, UX, and release hardening (automated hardening is implemented; stricter manual evidence JSON, published-image signature verification, final PASS readiness, and final no-blocker review remain pending)
- Independent full review completed (tracked focused review covers reviewed revision `4e144e88786a0f2c979033c6c2a63fdb26d5340c` and reports no open Critical/High findings, with prior HA/container High findings resolved; final PASS review against the target release commit remains pending because release-blocking Medium external evidence gaps remain)
- Release gate PASS

## Residual Risks

- `docs/release/manual-validation.json` is absent. The manual release gate requires PASS evidence for `physical-barcode-camera`, `real-receipt-ocr`, `published-image-signature`, and `independent-full-review` on the target release commit; the `docs/release/manual-validation.json` file itself must be git-tracked and clean in the validating checkout, may contain only the generated required check IDs, must keep each check's generated acceptance IDs exact, must stay on the generated schema with no ignored extra fields, local evidence artifacts must be git-tracked and clean under `docs/release/evidence/` and each listed evidence artifact must be valid UTF-8 text that mentions the check ID it supports, signature evidence must include a SemVer tag, `sha256:<digest>`, SemVer-tagged digest-pinned image reference, Rekor/Sigstore transparency-log URL or explicit unavailability reason, plus a passing `cosign verify` invocation against the recorded image reference, digest, and signature identity with the recorded identity in a `--certificate-identity` value or `--certificate-identity-regexp` pattern and no insecure cosign verification flags, and review evidence must point to a git-tracked and clean artifact under `docs/reviews/` that mentions the matching commit and includes machine-readable `decision=PASS`, `open_critical_high=0`, and `release_blocking_medium=0` markers.
- Physical-device barcode camera validation and representative real-receipt OCR/review/commit evidence remain pending. Deterministic browser/OCR automation exists, but these user-facing capture paths still need real-device/manual release artifacts. Final barcode evidence must cover separate valid 8-14 digit GTIN known-barcode and unknown/manual-fallback product or lot outcomes; final real-receipt evidence must identify a representative real receipt source, describe OCR extraction from a receipt image/photo/scan, and include committed receipt/purchase IDs matching the PantryOS `receipt_<32 lowercase hex>` and `purchase_<32 lowercase hex>` formats, lot count, and price-history confirmation covering store, date, package/quantity, total, and unit price.
- Published-image signature verification remains pending until the final tag/image is published and verified; final evidence must keep the SemVer tag, image reference, recorded digest, signature identity, and `cosign verify` identity flag value/pattern consistent and avoid insecure verification flags.
- The tracked focused review at `docs/reviews/2026-08-26-current-head-review.md` covers reviewed revision `4e144e88786a0f2c979033c6c2a63fdb26d5340c`, found no open Critical/High issues, and recorded the prior HA stale-sensor and non-isolated-container findings as resolved. It still reports release-blocking Medium external evidence gaps; J7 cannot pass until a final review artifact mentions the target release commit and reports no release-blocking Medium findings.
- Release readiness remains `NOT READY` until the acceptance criteria checklist is fully checked/evidenced or explicitly scoped, the manual evidence JSON validates, the final image signature evidence exists, every required release command is rerun, readiness is regenerated as PASS, and a final no-blocker independent review is recorded.

## Latest Evidence

- 2026-08-28 `Phase 6/F`: Verified and checked off automated barcode, receipt, purchase, and price-history acceptance criteria F1-F9. Improved the browser unknown-barcode path so an unmapped code without a product name focuses the Product name field, marks it invalid for assistive tech, updates the barcode status, and keeps the manual mapping form ready instead of surfacing only a generic API error. Extended the browser smoke to exercise that manual fallback before successfully mapping and adding the lot. Existing focused Core/API tests prove known-barcode package lookup/add-lot, barcode uniqueness and persistence across Core reopen, receipt upload type/size/path controls, image receipt OCR boundary, review-before-mutation, editable receipt review, duplicate commit idempotency, purchase/lot/price-history output, and recent-median compatible-unit anomaly explanations.
  Evidence: focused F pytest -> 11 relevant Core/API/API-client tests passed; bundled Node JS syntax checks for `app\static\app.js` and `scripts\browser_smoke.cjs` -> passed; browser smoke via bundled Node -> passed for `phone` and `kitchen-tablet`, including camera adapter capture, unknown-barcode manual fallback, barcode mapping/add-lot, receipt review/commit, and responsive/a11y checks; Docker-backed OCR corpus -> `ok=true`, `case_count=3`, parsed and committed `standard_single_item`, `multi_item_with_barcode`, and `header_footer_noise` receipt images inside the running PantryOS container.
  Remaining: F1-F9 are now checked for implemented automated paths; final physical barcode-camera validation and representative real-receipt OCR/review/commit evidence remain manual release blockers, along with published signed image evidence, final clean independent review, complete non-A/C/D/E/F/H acceptance checklist evidence, and release readiness PASS
- 2026-08-28 `Phase 5/E`: Verified and checked off cooking, leftovers, waste, and value acceptance criteria E1-E6. Added a direct browser/API-compatible regression that creates a leftover through real cooking completion, proves starting cooking leaves source inventory unchanged while emitting cooking.started, and verifies the created leftover appears in both the leftovers surface and a next-meal/use-soon recommendation before its use-by date. Existing Core/API/HA focused checks prove confirmed-allocation atomic completion, rollback on over-allocation, leftover session provenance, historical monthly waste from discard events after lot cleanup, and location inventory value availability to both the API client and Home Assistant sensors.
  Evidence: focused E pytest -> `tests\test_app_server.py::test_cooking_leftover_appears_in_next_meal_use_soon_experience`, `tests\core\test_sqlite_core.py::test_cooking_session_start_complete_leftover_and_rollback`, and `tests\core\test_sqlite_core.py::test_discard_records_monthly_waste_and_location_values` all passed; API/HA focused pytest -> `tests\test_api_client.py::test_api_client_reports_waste_and_location_values`, `tests\test_api_client.py::test_api_client_completes_cooking_session_with_leftover`, `tests\test_api_client.py::test_api_client_receipt_purchase_price_and_leftover_routes`, `tests\test_ha_contract.py::test_home_assistant_services_sensors_and_translations_cover_current_surface`, and `tests\test_ha_contract.py::test_home_assistant_live_core_smoke_uses_real_core_api_and_release_docs` all passed; full pytest -> 140 passed; Ruff format/check -> passed; mypy -> success in 20 source files; final `scripts/check.py` -> python compile 43 files passed, 140 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.
  Remaining: E1-E6 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-A/C/D/E/H acceptance checklist evidence, and release readiness PASS remain pending
- 2026-08-28 `Phase 4/D`: Verified and checked off meal-planning and shopping acceptance criteria D1-D9. Added explicit shopping source_breakdown snapshots, meal-plan generated source breakdowns, alias-aware add-missing product resolution, and generated-demand override preservation so user-edited generated quantities are not silently overwritten by rebuilds. Added a focused browser/API-compatible D-section regression covering structured weekly plan rows, aggregate/subtract demand, rebuild and add-missing idempotency, manual/generated/minimum-stock source separation, edit/check/uncheck/suppress/remove shopping lifecycle, purchase-to-lot transaction output, and override preservation. Also fixed a date-sensitive FEFO test fixture that had expired on 2026-08-27.
  Evidence: focused D/FEFO pytest -> `tests/test_app_server.py::test_meal_plan_and_shopping_d_section_contracts` and `tests/core/test_sqlite_core.py::test_consume_product_uses_fefo_and_rejects_over_consumption` both passed; broader app/core/API pytest -> 59 passed; Ruff check for D-touched app/core/API tests and source -> passed; mypy for `src/pantryos/core.py`, `app/server.py`, and `custom_components/pantryos/api_client.py` -> success in 3 source files.
  Remaining: D1-D9 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-A/C/D/H acceptance checklist evidence, and release readiness PASS remain pending
- 2026-08-28 `Phase 4/C`: Verified and checked off recipe intelligence acceptance criteria C1-C8. Added product-alias resolution for recipe ingredient linking in Core, legacy import, and the browser/API recipe helper; product updates can now replace a product's aliases. Expanded recipe matching to report per-ingredient required/available/remainder/allocation details, requested-serving scaling, bounded missing-N queries, max-prep filtering with unknown-time exclusion by default and explicit inclusion override, and use-soon recipe recommendations with urgent-lot and score-component explanations.
  Evidence: focused C-gate pytest -> `tests/test_app_server.py::test_recipe_intelligence_c_section_contracts` and `tests/core/test_sqlite_core.py::test_incompatible_unit_conversion_is_rejected` both passed; app/core pytest -> 51 passed; Ruff check for `app/server.py`, `src/pantryos/core.py`, and `tests/test_app_server.py` -> passed; mypy for `app/server.py` and `src/pantryos/core.py` -> success in 2 source files; full pytest -> 138 passed.
  Remaining: C1-C8 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-A/C/H acceptance checklist evidence, and release readiness PASS remain pending
- 2026-08-28 `Phase 1/3/A`: Verified and checked off source-of-truth acceptance criteria A1-A5. Strengthened scripts/ha_core_live_smoke.py so the live HA/Core smoke now restarts the isolated PantryOS Core container, starts Home Assistant a second time against the same temporary Core volume/config, and verifies HA reconciles the persisted state revision through PantryOS sensors after restart.
  Evidence: current branch/status -> `master...origin/master` with only pre-existing untracked review draft; architecture scan -> no active `PantryStore`, HA Store, `.inventory` import, or legacy HA-local inventory engine references in active app/custom_components/tests/scripts/README/release docs; focused HA/cross-surface pytest -> 13 passed; full pytest -> 137 passed; Ruff format/check -> passed; mypy -> success in 19 source files; updated live HA/Core smoke -> `ok=true`, Home Assistant `2026.8.3`, config flow `create_entry`, unique ID `inst_2a1ca915e4ed463cb9c4fcb3f618e410`, HA service revision `1`, direct Core push revision `2`, sensors `sensor.pantryos_state_revision=2` and `sensor.pantryos_total_items=2`, restart recovery `ok=true`, restarted HA/Core revision `2`, restarted sensor state revision `2`, 16 sensors, 15 services, unload succeeded; final `scripts/check.py` -> python compile 43 files passed, 137 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.
  Remaining: A1-A5 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-A/H acceptance checklist evidence, and release readiness PASS remain pending
- 2026-08-28 `Phase 7/H`: Verified and checked off Home Assistant acceptance criteria H1-H10. Removed the obsolete prototype custom_components/pantryos/store.py, custom_components/pantryos/inventory.py, and tests/test_inventory.py modules so the HACS integration no longer ships or tests a Home Assistant-local inventory store; the dependency-free runner now targets the SQLite Core/API-backed implementation.
  Evidence: obsolete HA Store reference scan across active code/docs/release artifacts -> no active references to the removed local-store path; focused HA/cross-surface pytest -> 13 passed; dependency-free `scripts/run_tests.py` -> 137 passed; full pytest -> 137 passed; Ruff format/check -> passed; mypy -> success in 19 source files; supply-chain audit write -> `ok=true`, source file count `72`; final `scripts/check.py` -> python compile 43 files passed, 137 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; installed HA smoke -> `ok=true`, Home Assistant `2026.8.3`, 15 services, stream task cancelled; live HA/Core smoke -> `ok=true`, Home Assistant `2026.8.3`, config flow `create_entry`, unique ID `inst_5903b5bb0efc4fd3b2152dde23de4e86`, 16 sensors, 15 services, HA service revision `1`, direct Core push revision `2`, `sensor.pantryos_state_revision=2`, unload succeeded.
  Remaining: H1-H10 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-H acceptance checklist evidence, and release readiness PASS remain pending
- 2026-08-28 `NAS Docker/HACS repository identity`: Wired the Home Assistant custom integration to the supplied GitHub repository identity: set origin to https://github.com/BDelta23/PantryOS.git, added manifest documentation, issue_tracker, and codeowners metadata for HACS, made HACS README instructions use the exact repository URL, and fixed the GHCR workflow/docs to publish the Core image under lowercase ghcr.io/bdelta23/pantryos:<version> tags.
  Evidence: `git remote -v` -> `origin https://github.com/BDelta23/PantryOS.git`; focused HACS/container pytest -> 13 passed; full pytest -> 145 passed; Ruff format/check -> passed; mypy -> success in 21 source files; supply-chain audit write -> `ok=true`, source file count `75`, OS package count `157`; release readiness regenerated; final `scripts/check.py` -> python compile 46 files passed, 145 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.
  Remaining: HACS public-release metadata is no longer blocked by missing repository identity; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete acceptance checklist evidence, and release readiness PASS remain pending
- 2026-08-27 `NAS Docker/HACS/HA deployment`: Built the HACS/NAS household deployment slice: added HACS metadata, English runtime translations, integration PNG branding, complete Home Assistant action/field translation coverage, local-push HA metadata, global HA action registration, non-polling device-backed sensors, incremental SSE event-stream reads, /data Docker persistence with env-driven server defaults, NAS deploy examples, GitHub CI/GHCR workflows, line-ending policy, and README deployment/update/backup instructions. Verified the HA flow against Home Assistant 2026.8.3 with all 16 sensors and 15 actions.
  Evidence: `git remote -v` and `git config --get remote.origin.url` -> no configured remote; focused HA/API pytest -> 19 passed; focused HA/Docker contract pytest -> 16 passed; full pytest -> 145 passed; Ruff format/check -> passed; mypy -> success in 21 source files; static image hardening audit -> `ok=true`, 31 checks; `docker compose build pantryos` -> built `pantryos-pantryos:latest`; isolated container smoke -> `ok=true`, Docker server `29.6.1`, readiness `status=ready`, UID `10001`, backup `/data/backups/container-smoke-20260827040808.zip`, restored database `/data/container-smoke-restored-20260827040808.sqlite3`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`); installed HA smoke -> `ok=true`, Home Assistant `2026.8.3`, all 15 actions registered, stream task cancelled; live HA/Core smoke -> `ok=true`, Home Assistant `2026.8.3`, isolated Core, config flow `create_entry`, 16 sensors, 15 actions, HA action advanced revision to `1`, direct Core push advanced revision to `2`, `sensor.pantryos_state_revision=2`, `sensor.pantryos_total_items=2`; supply-chain audit write -> `ok=true`, source file count `75`, OS package count `157`; release readiness regenerated; final `scripts/check.py` -> python compile 46 files passed, 145 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.
  Remaining: HACS public-release metadata is still blocked on an established GitHub remote/owner so manifest.json can truthfully include repository-specific documentation, issue_tracker, and non-empty GitHub codeowners; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete acceptance checklist evidence, and release readiness PASS remain pending

## Machine Summary

```json
{
  "acceptance_criteria_count": 85,
  "decision": "NOT READY",
  "generated_at": "2026-08-28T04:29:39Z",
  "latest_evidence": [
    {
      "change": "Verified and checked off automated barcode, receipt, purchase, and price-history acceptance criteria F1-F9. Improved the browser unknown-barcode path so an unmapped code without a product name focuses the Product name field, marks it invalid for assistive tech, updates the barcode status, and keeps the manual mapping form ready instead of surfacing only a generic API error. Extended the browser smoke to exercise that manual fallback before successfully mapping and adding the lot. Existing focused Core/API tests prove known-barcode package lookup/add-lot, barcode uniqueness and persistence across Core reopen, receipt upload type/size/path controls, image receipt OCR boundary, review-before-mutation, editable receipt review, duplicate commit idempotency, purchase/lot/price-history output, and recent-median compatible-unit anomaly explanations.",
      "commands": "focused F pytest -> 11 relevant Core/API/API-client tests passed; bundled Node JS syntax checks for `app\\static\\app.js` and `scripts\\browser_smoke.cjs` -> passed; browser smoke via bundled Node -> passed for `phone` and `kitchen-tablet`, including camera adapter capture, unknown-barcode manual fallback, barcode mapping/add-lot, receipt review/commit, and responsive/a11y checks; Docker-backed OCR corpus -> `ok=true`, `case_count=3`, parsed and committed `standard_single_item`, `multi_item_with_barcode`, and `header_footer_noise` receipt images inside the running PantryOS container.",
      "date": "2026-08-28",
      "gap": "F1-F9 are now checked for implemented automated paths; final physical barcode-camera validation and representative real-receipt OCR/review/commit evidence remain manual release blockers, along with published signed image evidence, final clean independent review, complete non-A/C/D/E/F/H acceptance checklist evidence, and release readiness PASS",
      "phase": "Phase 6/F"
    },
    {
      "change": "Verified and checked off cooking, leftovers, waste, and value acceptance criteria E1-E6. Added a direct browser/API-compatible regression that creates a leftover through real cooking completion, proves starting cooking leaves source inventory unchanged while emitting cooking.started, and verifies the created leftover appears in both the leftovers surface and a next-meal/use-soon recommendation before its use-by date. Existing Core/API/HA focused checks prove confirmed-allocation atomic completion, rollback on over-allocation, leftover session provenance, historical monthly waste from discard events after lot cleanup, and location inventory value availability to both the API client and Home Assistant sensors.",
      "commands": "focused E pytest -> `tests\\test_app_server.py::test_cooking_leftover_appears_in_next_meal_use_soon_experience`, `tests\\core\\test_sqlite_core.py::test_cooking_session_start_complete_leftover_and_rollback`, and `tests\\core\\test_sqlite_core.py::test_discard_records_monthly_waste_and_location_values` all passed; API/HA focused pytest -> `tests\\test_api_client.py::test_api_client_reports_waste_and_location_values`, `tests\\test_api_client.py::test_api_client_completes_cooking_session_with_leftover`, `tests\\test_api_client.py::test_api_client_receipt_purchase_price_and_leftover_routes`, `tests\\test_ha_contract.py::test_home_assistant_services_sensors_and_translations_cover_current_surface`, and `tests\\test_ha_contract.py::test_home_assistant_live_core_smoke_uses_real_core_api_and_release_docs` all passed; full pytest -> 140 passed; Ruff format/check -> passed; mypy -> success in 20 source files; final `scripts/check.py` -> python compile 43 files passed, 140 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.",
      "date": "2026-08-28",
      "gap": "E1-E6 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-A/C/D/E/H acceptance checklist evidence, and release readiness PASS remain pending",
      "phase": "Phase 5/E"
    },
    {
      "change": "Verified and checked off meal-planning and shopping acceptance criteria D1-D9. Added explicit shopping source_breakdown snapshots, meal-plan generated source breakdowns, alias-aware add-missing product resolution, and generated-demand override preservation so user-edited generated quantities are not silently overwritten by rebuilds. Added a focused browser/API-compatible D-section regression covering structured weekly plan rows, aggregate/subtract demand, rebuild and add-missing idempotency, manual/generated/minimum-stock source separation, edit/check/uncheck/suppress/remove shopping lifecycle, purchase-to-lot transaction output, and override preservation. Also fixed a date-sensitive FEFO test fixture that had expired on 2026-08-27.",
      "commands": "focused D/FEFO pytest -> `tests/test_app_server.py::test_meal_plan_and_shopping_d_section_contracts` and `tests/core/test_sqlite_core.py::test_consume_product_uses_fefo_and_rejects_over_consumption` both passed; broader app/core/API pytest -> 59 passed; Ruff check for D-touched app/core/API tests and source -> passed; mypy for `src/pantryos/core.py`, `app/server.py`, and `custom_components/pantryos/api_client.py` -> success in 3 source files.",
      "date": "2026-08-28",
      "gap": "D1-D9 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-A/C/D/H acceptance checklist evidence, and release readiness PASS remain pending",
      "phase": "Phase 4/D"
    },
    {
      "change": "Verified and checked off recipe intelligence acceptance criteria C1-C8. Added product-alias resolution for recipe ingredient linking in Core, legacy import, and the browser/API recipe helper; product updates can now replace a product's aliases. Expanded recipe matching to report per-ingredient required/available/remainder/allocation details, requested-serving scaling, bounded missing-N queries, max-prep filtering with unknown-time exclusion by default and explicit inclusion override, and use-soon recipe recommendations with urgent-lot and score-component explanations.",
      "commands": "focused C-gate pytest -> `tests/test_app_server.py::test_recipe_intelligence_c_section_contracts` and `tests/core/test_sqlite_core.py::test_incompatible_unit_conversion_is_rejected` both passed; app/core pytest -> 51 passed; Ruff check for `app/server.py`, `src/pantryos/core.py`, and `tests/test_app_server.py` -> passed; mypy for `app/server.py` and `src/pantryos/core.py` -> success in 2 source files; full pytest -> 138 passed.",
      "date": "2026-08-28",
      "gap": "C1-C8 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-A/C/H acceptance checklist evidence, and release readiness PASS remain pending",
      "phase": "Phase 4/C"
    },
    {
      "change": "Verified and checked off source-of-truth acceptance criteria A1-A5. Strengthened scripts/ha_core_live_smoke.py so the live HA/Core smoke now restarts the isolated PantryOS Core container, starts Home Assistant a second time against the same temporary Core volume/config, and verifies HA reconciles the persisted state revision through PantryOS sensors after restart.",
      "commands": "current branch/status -> `master...origin/master` with only pre-existing untracked review draft; architecture scan -> no active `PantryStore`, HA Store, `.inventory` import, or legacy HA-local inventory engine references in active app/custom_components/tests/scripts/README/release docs; focused HA/cross-surface pytest -> 13 passed; full pytest -> 137 passed; Ruff format/check -> passed; mypy -> success in 19 source files; updated live HA/Core smoke -> `ok=true`, Home Assistant `2026.8.3`, config flow `create_entry`, unique ID `inst_2a1ca915e4ed463cb9c4fcb3f618e410`, HA service revision `1`, direct Core push revision `2`, sensors `sensor.pantryos_state_revision=2` and `sensor.pantryos_total_items=2`, restart recovery `ok=true`, restarted HA/Core revision `2`, restarted sensor state revision `2`, 16 sensors, 15 services, unload succeeded; final `scripts/check.py` -> python compile 43 files passed, 137 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.",
      "date": "2026-08-28",
      "gap": "A1-A5 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-A/H acceptance checklist evidence, and release readiness PASS remain pending",
      "phase": "Phase 1/3/A"
    },
    {
      "change": "Verified and checked off Home Assistant acceptance criteria H1-H10. Removed the obsolete prototype custom_components/pantryos/store.py, custom_components/pantryos/inventory.py, and tests/test_inventory.py modules so the HACS integration no longer ships or tests a Home Assistant-local inventory store; the dependency-free runner now targets the SQLite Core/API-backed implementation.",
      "commands": "obsolete HA Store reference scan across active code/docs/release artifacts -> no active references to the removed local-store path; focused HA/cross-surface pytest -> 13 passed; dependency-free `scripts/run_tests.py` -> 137 passed; full pytest -> 137 passed; Ruff format/check -> passed; mypy -> success in 19 source files; supply-chain audit write -> `ok=true`, source file count `72`; final `scripts/check.py` -> python compile 43 files passed, 137 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; installed HA smoke -> `ok=true`, Home Assistant `2026.8.3`, 15 services, stream task cancelled; live HA/Core smoke -> `ok=true`, Home Assistant `2026.8.3`, config flow `create_entry`, unique ID `inst_5903b5bb0efc4fd3b2152dde23de4e86`, 16 sensors, 15 services, HA service revision `1`, direct Core push revision `2`, `sensor.pantryos_state_revision=2`, unload succeeded.",
      "date": "2026-08-28",
      "gap": "H1-H10 are now checked; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete non-H acceptance checklist evidence, and release readiness PASS remain pending",
      "phase": "Phase 7/H"
    },
    {
      "change": "Wired the Home Assistant custom integration to the supplied GitHub repository identity: set origin to https://github.com/BDelta23/PantryOS.git, added manifest documentation, issue_tracker, and codeowners metadata for HACS, made HACS README instructions use the exact repository URL, and fixed the GHCR workflow/docs to publish the Core image under lowercase ghcr.io/bdelta23/pantryos:<version> tags.",
      "commands": "`git remote -v` -> `origin https://github.com/BDelta23/PantryOS.git`; focused HACS/container pytest -> 13 passed; full pytest -> 145 passed; Ruff format/check -> passed; mypy -> success in 21 source files; supply-chain audit write -> `ok=true`, source file count `75`, OS package count `157`; release readiness regenerated; final `scripts/check.py` -> python compile 46 files passed, 145 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.",
      "date": "2026-08-28",
      "gap": "HACS public-release metadata is no longer blocked by missing repository identity; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete acceptance checklist evidence, and release readiness PASS remain pending",
      "phase": "NAS Docker/HACS repository identity"
    },
    {
      "change": "Built the HACS/NAS household deployment slice: added HACS metadata, English runtime translations, integration PNG branding, complete Home Assistant action/field translation coverage, local-push HA metadata, global HA action registration, non-polling device-backed sensors, incremental SSE event-stream reads, /data Docker persistence with env-driven server defaults, NAS deploy examples, GitHub CI/GHCR workflows, line-ending policy, and README deployment/update/backup instructions. Verified the HA flow against Home Assistant 2026.8.3 with all 16 sensors and 15 actions.",
      "commands": "`git remote -v` and `git config --get remote.origin.url` -> no configured remote; focused HA/API pytest -> 19 passed; focused HA/Docker contract pytest -> 16 passed; full pytest -> 145 passed; Ruff format/check -> passed; mypy -> success in 21 source files; static image hardening audit -> `ok=true`, 31 checks; `docker compose build pantryos` -> built `pantryos-pantryos:latest`; isolated container smoke -> `ok=true`, Docker server `29.6.1`, readiness `status=ready`, UID `10001`, backup `/data/backups/container-smoke-20260827040808.zip`, restored database `/data/container-smoke-restored-20260827040808.sqlite3`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`); installed HA smoke -> `ok=true`, Home Assistant `2026.8.3`, all 15 actions registered, stream task cancelled; live HA/Core smoke -> `ok=true`, Home Assistant `2026.8.3`, isolated Core, config flow `create_entry`, 16 sensors, 15 actions, HA action advanced revision to `1`, direct Core push advanced revision to `2`, `sensor.pantryos_state_revision=2`, `sensor.pantryos_total_items=2`; supply-chain audit write -> `ok=true`, source file count `75`, OS package count `157`; release readiness regenerated; final `scripts/check.py` -> python compile 46 files passed, 145 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed.",
      "date": "2026-08-27",
      "gap": "HACS public-release metadata is still blocked on an established GitHub remote/owner so manifest.json can truthfully include repository-specific documentation, issue_tracker, and non-empty GitHub codeowners; final physical barcode/real-receipt evidence, published signed image evidence, final clean independent review, complete acceptance checklist evidence, and release readiness PASS remain pending",
      "phase": "NAS Docker/HACS/HA deployment"
    }
  ],
  "open_acceptance_criteria": [
    "G1. All primary inventory, recipe, plan, shopping, cooking, barcode, receipt, and settings workflows are reachable in the UI.",
    "G2. No visible control is a toast-only stub or hard-coded success simulation.",
    "G3. Loading, empty, offline, validation, conflict, and server-error states are clear.",
    "G4. Destructive actions require appropriate confirmation and remain keyboard accessible.",
    "G5. Critical workflows pass automated browser tests at phone and kitchen-tablet viewport sizes.",
    "G6. Forms have labels, focus handling, error association, and no critical automated accessibility violations.",
    "G7. PWA install metadata and an intentional offline/error strategy exist; offline writes are not falsely reported as committed.",
    "I1. Unauthorized API mutation and event subscription fail.",
    "I2. Browser authentication/session, CSRF, CORS, origin, and cookie policy are explicit and tested.",
    "I3. Request body, upload size, filename/path, MIME/content, and rate limits are enforced.",
    "I4. No secrets appear in source, logs, client errors, diagnostics, fixtures, or generated artifacts.",
    "I5. Container runs as non-root and writes only intended volume paths.",
    "I6. Liveness and readiness have different semantics and tests.",
    "I7. Structured logs include request/correlation IDs without sensitive payloads.",
    "I8. Migration failure leaves the prior database/backup recoverable.",
    "I9. `pantryos doctor`, backup, restore verification, and legacy dry-run are documented and smoke-tested.",
    "I10. Core operation is usable without internet access after dependencies/images are installed.",
    "J1. Unit, integration, API, migration, concurrency, HA, browser, and container smoke suites pass from a clean checkout.",
    "J2. Lint, format, and type checks pass.",
    "J3. Domain/application code has meaningful coverage, including failure paths; the configured threshold passes without broad exclusions.",
    "J4. OpenAPI, user setup, Home Assistant setup, backup/restore, migration, and troubleshooting docs match actual behavior.",
    "J5. No completion-critical TODO, placeholder URL, fake response, disabled test, or skipped release check remains without a documented non-blocking reason.",
    "J6. A scripted demo proves add \u2192 sync \u2192 plan \u2192 shop \u2192 cook \u2192 leftover \u2192 use-soon across the supported surfaces.",
    "J7. Independent review reports no open Critical/High findings and no release-blocking Medium finding.",
    "J8. `docs/release/RELEASE_READINESS.md` records exact commands, results, residual risks, and a justified `PASS`."
  ],
  "open_blockers": [
    "`docs/release/manual-validation.json` is absent. The manual release gate requires PASS evidence for `physical-barcode-camera`, `real-receipt-ocr`, `published-image-signature`, and `independent-full-review` on the target release commit; the `docs/release/manual-validation.json` file itself must be git-tracked and clean in the validating checkout, may contain only the generated required check IDs, must keep each check's generated acceptance IDs exact, must stay on the generated schema with no ignored extra fields, local evidence artifacts must be git-tracked and clean under `docs/release/evidence/` and each listed evidence artifact must be valid UTF-8 text that mentions the check ID it supports, signature evidence must include a SemVer tag, `sha256:<digest>`, SemVer-tagged digest-pinned image reference, Rekor/Sigstore transparency-log URL or explicit unavailability reason, plus a passing `cosign verify` invocation against the recorded image reference, digest, and signature identity with the recorded identity in a `--certificate-identity` value or `--certificate-identity-regexp` pattern and no insecure cosign verification flags, and review evidence must point to a git-tracked and clean artifact under `docs/reviews/` that mentions the matching commit and includes machine-readable `decision=PASS`, `open_critical_high=0`, and `release_blocking_medium=0` markers.",
    "Physical-device barcode camera validation and representative real-receipt OCR/review/commit evidence remain pending. Deterministic browser/OCR automation exists, but these user-facing capture paths still need real-device/manual release artifacts. Final barcode evidence must cover separate valid 8-14 digit GTIN known-barcode and unknown/manual-fallback product or lot outcomes; final real-receipt evidence must identify a representative real receipt source, describe OCR extraction from a receipt image/photo/scan, and include committed receipt/purchase IDs matching the PantryOS `receipt_<32 lowercase hex>` and `purchase_<32 lowercase hex>` formats, lot count, and price-history confirmation covering store, date, package/quantity, total, and unit price.",
    "Published-image signature verification remains pending until the final tag/image is published and verified; final evidence must keep the SemVer tag, image reference, recorded digest, signature identity, and `cosign verify` identity flag value/pattern consistent and avoid insecure verification flags.",
    "The tracked focused review at `docs/reviews/2026-08-26-current-head-review.md` covers reviewed revision `4e144e88786a0f2c979033c6c2a63fdb26d5340c`, found no open Critical/High issues, and recorded the prior HA stale-sensor and non-isolated-container findings as resolved. It still reports release-blocking Medium external evidence gaps; J7 cannot pass until a final review artifact mentions the target release commit and reports no release-blocking Medium findings.",
    "Release readiness remains `NOT READY` until the acceptance criteria checklist is fully checked/evidenced or explicitly scoped, the manual evidence JSON validates, the final image signature evidence exists, every required release command is rerun, readiness is regenerated as PASS, and a final no-blocker independent review is recorded."
  ],
  "open_phase_gates": [
    "Phase 6 \u2014 barcode, receipts, purchases, and price history (implemented paths are automated; physical barcode camera and representative real-receipt evidence remain manual release blockers)",
    "Phase 8 \u2014 security, operations, UX, and release hardening (automated hardening is implemented; stricter manual evidence JSON, published-image signature verification, final PASS readiness, and final no-blocker review remain pending)",
    "Independent full review completed (tracked focused review covers reviewed revision `4e144e88786a0f2c979033c6c2a63fdb26d5340c` and reports no open Critical/High findings, with prior HA/container High findings resolved; final PASS review against the target release commit remains pending because release-blocking Medium external evidence gaps remain)",
    "Release gate PASS"
  ],
  "phase_gate_count": 11,
  "required_release_commands": [
    "python scripts/check.py",
    "python scripts/smoke_e2e.py",
    "python scripts/concurrency_smoke.py",
    "node scripts/browser_smoke.cjs",
    "python scripts/container_smoke.py --isolated",
    "python scripts/receipt_ocr_corpus_smoke.py",
    "python scripts/image_hardening_audit.py",
    "python scripts/supply_chain_audit.py",
    "python scripts/ha_installed_smoke.py",
    "python scripts/ha_core_live_smoke.py",
    "python scripts/release_artifact_audit.py",
    "python scripts/manual_release_evidence.py",
    "python scripts/release_readiness.py --check"
  ]
}
```

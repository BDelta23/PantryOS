# PantryOS implementation status

Codex should update this file throughout the completion goal. Record exact commands and concise evidence; do not mark a gate complete based on code inspection alone when it is runnable.

## Baseline

- [x] Original source preserved in handoff package.
- [x] `python scripts/run_tests.py` — 11 tests passed on 2026-08-25.
- [x] Python compile and JavaScript syntax checks passed.
- [x] JSON lost-update defect reproduced.
- [x] `docker compose config` — passed on 2026-08-25 in local Docker Desktop environment.

## Phase gates

- [x] Phase 0 — baseline regression tests and engineering tooling
- [x] Phase 1 — SQLite, migrations, legacy import, package extraction
- [x] Phase 2 — inventory domain and authenticated API foundation
- [x] Phase 3 — web + Home Assistant one-source-of-truth proof
- [x] Phase 4 — recipes, use-soon, meal plan, idempotent shopping
- [x] Phase 5 — cooking, leftovers, waste, and location value
- [ ] Phase 6 — barcode, receipts, purchases, and price history (implemented paths are automated; physical barcode camera and representative real-receipt evidence remain manual release blockers)
- [x] Phase 7 — complete Home Assistant surface
- [ ] Phase 8 — security, operations, UX, and release hardening (automated hardening is implemented; stricter manual evidence JSON, published-image signature verification, final PASS readiness, and final no-blocker review remain pending)
- [ ] Independent full review completed (latest review covers pre-D9 HEAD `82d7f32` and reports release-blocking Medium evidence gaps; final PASS review against the final commit pending)
- [ ] Release gate PASS

## Evidence log
| Date/time | Phase | Change or decision | Commands and result | Remaining proof gap |
|---|---|---|---|---|
| 2026-08-25 | Baseline | Handoff created from v0.1.0 prototype | 11 tests pass; compileall pass; node syntax pass | Docker unavailable in inspection environment |
| 2026-08-25 | Phase 0 | Handoff controls installed into repo root; Git repository initialized for baseline checkpoint | `python scripts/run_tests.py` -> 11 tests passed; `python -m compileall -q app custom_components/pantryos scripts tests` -> passed; `node --check app/static/app.js` -> passed; `docker compose config` -> passed; baseline commit `98078e3` created | Formatter/lint/type/CI commands still need final v1 tooling; SQLite Core not started |
| 2026-08-25 | Phase 0 | Deterministic baseline defect reproducer added for JSON lost update and additive shopping demand | `python scripts/reproduce_baseline_defects.py` -> P0/P1 baseline defects reproduced; surviving JSON item list was `['Eggs']`; repeated recipe demand quantity was `6` | Final invariant tests will replace this reproducer after SQLite/idempotent shopping implementation |
| 2026-08-25 | Phase 1 | Added `src/pantryos` Core with SQLite migrations, unit registry, transaction wrapper, product/location/lot/event records, FEFO product consumption, legacy JSON import with backup/import marker, and backup/restore helpers | `python scripts/check.py` -> python compile: 18 files passed; 18 tests passed; javascript syntax passed | Web server still uses JSON repository; Home Assistant still uses HA Store until later phase |
| 2026-08-25 | Phase 1 | Replaced web app JSON repository with SQLite-backed PantryOS Core; app startup imports legacy `data/pantryos.json` only when the Core database is empty; Docker image now copies `src/` and uses `/app/data/pantryos.sqlite3` | `python scripts/check.py` -> python compile: 18 files passed; 18 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 18 tests passed; `GET /api/v1/health/ready` -> `{"status":"ready"}`; `GET /api/v1/instance` -> schema_version 1, state_revision 12 | Home Assistant still uses HA Store and must be replaced in Phase 3 |
| 2026-08-25 | Phase 2 | Added bearer-token enforcement for protected `/api/v1` routes, request IDs, stable problem responses, contract-compatible v1 inventory lot create/consume/move/discard routes, Docker token configuration, and API documentation | `python scripts/check.py` -> python compile: 18 files passed; 19 tests passed; javascript syntax passed; `docker compose config` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 19 tests passed; Docker smoke: container healthy, unauthenticated `GET /api/v1/dashboard` -> `401 unauthorized`, authenticated `POST /api/v1/inventory/lots` + `POST /api/v1/inventory/lots/{id}/consume` -> revision 14 | Browser compatibility routes remain unauthenticated until browser session/CSRF work; OpenAPI, CORS/origin policy, rate limits, and event subscription auth are still pending |
| 2026-08-25 | Phase 3 | Added Home Assistant `PantryAPIClient`, config flow URL/token validation against `/api/v1/instance`, API-backed polling sensor cache, and API-backed service calls for item add/consume/discard/move plus current recipe/shopping compatibility actions; removed HA Store from setup path | `python scripts/check.py` -> python compile: 20 files passed; 21 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 21 tests passed | Full HA runtime test harness is not installed; event push/coordinator, auth recovery diagnostics, and live HA-to-web/browser sync proof remain pending |
| 2026-08-25 | Phase 3 | Moved HA client recipe/meal/shopping calls onto authenticated `/api/v1` routes and added automated cross-surface proof: browser compatibility route add is visible through HA API client refresh; HA API client consume is visible through browser state without restarting either surface | `python scripts/check.py` -> python compile: 21 files passed; 22 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 22 tests passed; Docker readiness `GET /api/v1/health/ready` -> `{"status":"ready"}` | Full HA runtime test harness, Home Assistant entity lifecycle proof, and event push/coordinator remain pending |
| 2026-08-25 | Phase 4 | Added Core meal-plan shopping rebuild that aggregates active planned recipe ingredients, subtracts usable inventory, uses stable generated source keys, inactivates stale generated meal-plan demand, and exposes it through authenticated `POST /api/v1/shopping/rebuild` plus HA client method | `python scripts/check.py` -> python compile: 21 files passed; 23 tests passed; javascript syntax passed; API client test proves two planned meals rebuild to stable generated rows (`Plan Flour` 24 oz after inventory subtraction and `Baking Powder` 2 tbsp) across repeated rebuilds | Full shopping editing/checking/suppression/purchase workflow, manual/generated source breakdown UI, and user override reconciliation remain pending |
| 2026-08-25 | Phase 4 | Added schema migration 002 for purchase/purchase-line history; implemented shopping line list/edit/check/uncheck/remove and transactional purchase completion that creates purchase records, purchase lines, inventory lots linked by `purchase_line_id`, and completes shopping demand | `python scripts/check.py` -> python compile: 21 files passed; 26 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 26 tests passed; Docker smoke `GET /api/v1/instance` with bearer token -> schema_version 2 | Purchase UI, user override reconciliation, price history comparison, and complete browser workflow are still pending |
| 2026-08-25 | Phase 5 | Added schema migration 003 for cooking sessions; implemented start/complete/cancel session Core operations and authenticated `/api/v1/cooking/sessions` routes. Start records a cooking event without consuming inventory; completion atomically consumes confirmed lot allocations, marks linked session completed, and creates leftover lots with `cooking_session_id` provenance; cancel leaves inventory unchanged | `python scripts/check.py` -> python compile: 21 files passed; 29 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 29 tests passed; Docker smoke `GET /api/v1/instance` with bearer token -> schema_version 3 | Waste attribution, location value API/HA surfaces, cooking UI, and full HA runtime proof remain pending |
| 2026-08-25 | Phase 5 | Moved discard into PantryOS Core with event-time waste value metadata, added monthly waste and location count/value summaries from SQLite, exposed authenticated `/api/v1/locations/summary` and `/api/v1/waste/monthly`, and added HA category value sensors | `python scripts/check.py` -> python compile: 21 files passed; 31 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 31 tests passed; Docker smoke `GET /api/v1/locations/summary` -> values returned; `GET /api/v1/waste/monthly` -> `{"food_waste_this_month":"0.00","currency":"USD"}`; `GET /api/v1/instance` -> capabilities include `waste_metrics` and `location_value` | Cooking UI, browser purchase workflow, and full HA runtime proof remain pending |
| 2026-08-25 | Phase 4/5/G | Replaced the browser `Start Cooking` toast stub with a Core-backed cooking form; added browser shopping check/uncheck/remove and complete-purchase workflow; added browser compatibility routes that call the same Core cooking and purchase operations as `/api/v1`; exposed product IDs in item payloads for lot allocation matching | `python scripts/check.py` -> python compile: 21 files passed; 33 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 33 tests passed; non-destructive Docker HTTP smoke via `/api/shopping`, `/api/shopping/{id}/check`, `/api/shopping/complete-purchase`, `/api/cooking/sessions`, and `/api/cooking/sessions/{id}/complete` -> purchase lot created and cooking completed with leftover; Browser plugin visual check unavailable because node_repl setup failed with `CreateProcessWithLogonW failed: 267` | Browser session/CSRF/auth migration, responsive visual browser test, barcode/receipt workflows, and full HA runtime proof remain pending |
| 2026-08-25 | Phase 6/G | Added Core barcode mapping, known-barcode resolution, and add-lot-from-barcode operations using existing `product_barcodes`; exposed authenticated `/api/v1/barcodes/{barcode}`, `/api/v1/barcodes/mappings`, `/api/v1/barcodes/{barcode}/add-lot` plus browser compatibility routes; added browser manual fallback form for unknown barcodes | `python scripts/check.py` -> python compile: 21 files passed; 35 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 35 tests passed; non-destructive Docker smoke `GET /api/v1/barcodes/{barcode}` -> unmatched, `POST /api/v1/barcodes/mappings` -> mapped product, `POST /api/v1/barcodes/{barcode}/add-lot` -> added 2 can lot, second lookup -> matched; Core test proves mapping persistence across Core reopen and duplicate-barcode rejection | Receipt ingestion, price history/anomaly output, camera feature detection, responsive visual browser test, and browser session/CSRF/auth migration remain pending |
| 2026-08-25 | Phase 6/G | Added SQLite receipt upload metadata and price history migration; implemented structured text receipt upload/extract/review/reject/commit, idempotent duplicate commit, purchase detail/history endpoints, per-product price history, and browser receipt review controls | `python scripts/check.py` -> python compile: 21 files passed; 38 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 38 tests passed; non-destructive Docker smoke `POST /api/v1/receipts`, `POST /api/v1/receipts/{id}/extract`, `PATCH /api/v1/receipts/{id}/review`, `POST /api/v1/receipts/{id}/commit`, duplicate commit, `GET /api/v1/purchases/{id}`, and `GET /api/v1/products/{id}/prices` -> container healthy, receipt committed, duplicate true, price returned 1.80 per count | Receipt image/OCR ingestion, richer price anomaly UI, camera feature detection, responsive visual browser test, and browser session/CSRF/auth migration remain pending |
| 2026-08-25 | Phase 7/H | Expanded the Home Assistant API client for leftovers, receipt review, purchase history, and price history routes; added HA actions for explicit discard, shopping rebuild, and cooking start/complete/cancel; added leftover count and state revision sensors; added diagnostics redaction helpers for tokens, auth headers, receipt contents, and local receipt paths | `python scripts/check.py` -> python compile: 23 files passed; 41 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 41 tests passed; Docker smoke `GET /api/v1/dashboard` -> container healthy, summary includes `state_revision` 42 and `leftover_count` 3 | Full HA runtime harness, setup/unload/reconfigure/reauth runtime tests, coordinator/push subscription, automatic unavailable/recovery proof, and service registration lifecycle tests remain pending |
| 2026-08-25 | Phase 8/I/H | Added authenticated bounded event audit APIs and an SSE-compatible `/api/v1/events` hello/replay response over persisted Core events; added HA client event detail/list methods and API tests for unauthorized stream rejection, event detail lookup, recent-event ordering, SSE hello, event frames, and heartbeat | `python scripts/check.py` -> python compile: 23 files passed; 42 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 42 tests passed; Docker smoke `/api/v1/events` without bearer -> 401, `GET /api/v1/inventory/events?limit=5` latest type `ADD`, SSE content type `text/event-stream; charset=utf-8`, hello and heartbeat present, container healthy | Long-lived live push fanout, retained-history expiry semantics, HA coordinator stream consumption, and authenticated event subscription recovery tests remain pending |
| 2026-08-25 | Phase 8/I/J | Added `pantryos` operations CLI with `doctor`, checksum-manifest `backup`, pre-verified `restore --verify`, `restore --verify-only`, and `import-legacy --dry-run`; added checkout wrapper and console-script metadata | `python scripts/check.py` -> python compile: 26 files passed; 43 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 43 tests passed; Docker operations smoke `python scripts/pantryos.py --db /tmp/pantryos-ops.sqlite3 doctor` -> schema 4, `backup --output /tmp/pantryos-ops-backup.sqlite3` -> `.sha256.json` manifest written, `restore --verify` -> schema 4 verified | Richer rollback-package restore including upload files, migration failure recovery proof, and final release readiness docs remain pending |
| 2026-08-25 | Phase 8/I | Hardened Docker runtime with dedicated `pantryos` UID/GID `10001` and entrypoint that prepares `/app/data` ownership before dropping privileges; added container contract test for non-root runtime and named data volume | `python scripts/check.py` -> python compile: 28 files passed; 44 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 44 tests passed; Docker smoke `/proc/1/status` Uid -> 10001, `/app/data` writable, `python scripts/pantryos.py --db /app/data/pantryos.sqlite3 doctor` -> ok true schema 4, `docker inspect pantryos` -> running healthy | Read-only root filesystem, upload/backup path policy, broader container/image hardening, and final release readiness docs remain pending |
| 2026-08-25 | Phase 6/8/F/I | Enforced structured JSON request policy and receipt upload limits: non-empty request bodies must be JSON, oversized API bodies return 413, receipt uploads allow only local text/CSV with basename filenames, matching extension/MIME, 64 KB UTF-8 limit, simple CSV content sanity, private data-directory storage, and no storage path in API responses | `python scripts/check.py` -> python compile: 28 files passed; 46 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 46 tests passed; Docker smoke unsupported media -> 415 `unsupported_media_type`, oversized body -> 413 `request_body_too_large`, CSV receipt upload -> 201 uploaded with no `storage_path`, readiness -> ready | Browser session/CSRF/CORS/origin policy, general rate limits, receipt image/OCR ingestion, upload retention, and backup inclusion of upload files remain pending |
| 2026-08-25 | Phase 2/8/J | Added authenticated `GET /api/v1/openapi.json` serving an OpenAPI 3.1 contract for the current versioned API surface, including bearer auth, public health operation overrides, SSE content type, request body schemas for mutations, `201` create responses where applicable, stable `Problem` response references, and route-surface contract tests | `python scripts/check.py` -> python compile: 30 files passed; 48 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 48 tests passed; Docker smoke unauthenticated OpenAPI -> 401 `unauthorized`, authenticated OpenAPI -> 3.1.0 with 41 paths, `Problem` schema, and inventory lot POST `201` response, readiness -> ready, container -> running healthy | Browser session/CSRF/CORS/origin policy, richer per-resource schema precision, and release readiness PASS remains pending |
| 2026-08-25 | Phase 8/I | Added fixed-window in-process rate limits for receipt upload and receipt extraction endpoints, including `429 rate_limited` problem responses with `Retry-After`, default `20` requests per `60` seconds per client, environment overrides, Compose env wiring, OpenAPI `429` problem response documentation, and HTTP tests for upload/extract throttling | `python scripts/check.py` -> python compile: 30 files passed; 49 tests passed; javascript syntax passed; `docker compose config` with low-limit env -> rate-limit values passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 49 tests passed; Docker low-limit smoke first upload -> 201 uploaded, second upload -> 429 `rate_limited` with `Retry-After`, first extract -> 200 review, second extract -> 429 `rate_limited` with `Retry-After`, readiness -> ready, container -> running healthy; restored normal defaults -> running healthy | Browser session/CSRF/CORS/origin policy, broader per-route rate policies, persistent/distributed throttling, upload retention, and backup inclusion of upload files remain pending |
| 2026-08-25 | Phase 8/I/G | Added browser sign-in for local compatibility routes using `PANTRYOS_API_TOKEN`, in-memory browser sessions, `HttpOnly` `SameSite=Lax` `pantryos_session` cookies, per-session CSRF tokens for unsafe `/api/*` methods, same-origin `Origin` checks, non-wildcard CORS preflight handling, UI login/logout bootstrap, and Docker session TTL wiring | `python scripts/check.py` -> python compile: 30 files passed; 50 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 50 tests passed; Docker HTTP smoke: unauthenticated `GET /api/state` -> 401 `browser_session_required`, `POST /api/session/login` -> 200 with CSRF and `HttpOnly; SameSite=Lax` cookie, missing CSRF mutation -> 403 `csrf_required`, hostile `Origin` mutation -> 403 `origin_forbidden`, same-origin browser mutation -> 201, container -> running healthy | Full browser token setup UX polish, persistent sessions across process restart, HTTPS/Secure-cookie deployment mode, responsive visual/browser automation, broader per-route rate policies, upload retention, and backup inclusion of upload files remain pending |
| 2026-08-25 | Phase 8/I | Added structured JSON request logging for server responses with request ID, method, query-stripped path, status, duration, and client address only; logs intentionally exclude request bodies, authorization headers, cookies, CSRF tokens, browser session values, and receipt contents | `python scripts/check.py` -> python compile: 30 files passed; 51 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 51 tests passed; Docker log smoke authenticated `POST /api/v1/receipts` with `X-Request-ID: docker-log-smoke` -> 201 and container JSON log `event=http.request`, `path=/api/v1/receipts`, `status=201`, `request_id=docker-log-smoke`, no `local-dev-token`, no receipt payload, container -> running healthy | Broader log/diagnostic policy review, log rotation/deployment guidance, and release readiness PASS remains pending |
| 2026-08-25 | Phase 8/G | Added PWA install metadata, SVG app icon, service-worker registration, and an intentional offline strategy: static app shell/assets are cached, but `/api/*` requests remain network-only and offline API failures return `503 offline` with "the request was not committed" instead of queuing writes | `python scripts/check.py` -> python compile: 30 files passed; 52 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 52 tests passed; Docker PWA smoke `/manifest.webmanifest` -> `application/manifest+json` with `display=standalone` and `/icon.svg`, `/service-worker.js` -> `text/javascript` with offline handler and no queue/sync write policy, `/icon.svg` -> `image/svg+xml`, container -> running healthy | Automated browser viewport/accessibility tests, richer offline UI state, conflict-specific UI handling, and final PWA polish remain pending |
| 2026-08-26 | Phase 8/I/J | Added upload-inclusive `.zip` backup archives containing a verified SQLite snapshot, private receipt upload payloads, and archive manifest checksums; `.zip` restore now verifies the archive in a temporary workspace, rewrites receipt storage paths for the destination data directory, restores receipt payload files, and keeps rollback copies of the previous database/receipts | `python scripts/check.py` -> python compile: 30 files passed; 53 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 53 tests passed; Docker archive smoke created a receipt upload, `backup --output /tmp/pantryos-archive-smoke/backup.zip` -> format archive, `restore --verify-only` -> 1 receipt upload verified, `restore --verify` -> 1 receipt upload restored, restored storage file exists, `extract_receipt` -> store `Smoke Market`; `scripts/pantryos.py` wrapper now forces `src/` before `scripts/` on `sys.path` for source-checkout/container CLI invocations | Migration-failure recovery proof, final release readiness docs, and independent review remain pending |
| 2026-08-26 | Phase 8/I/J | Hardened SQLite migration execution and recovery: migrations now run statement-by-statement inside the active transaction instead of `executescript`, existing nonempty databases get an automatic pre-migration backup before pending migrations, failed upgrades save a `.failed` copy and restore the pre-migration backup, and database replacement clears stale SQLite sidecar files | `python scripts/check.py` -> python compile: 30 files passed; 54 tests passed; javascript syntax passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 54 tests passed; Docker failed-migration smoke injected `005_broken.sql` -> `OperationalError`, pre-migration backups `1`, failed copies `1`, schema max version `4`, partial table `false`, original lot count `1`, summary preserved `true` | Final release readiness docs, independent review, and broader clean-checkout CI parity remain pending |
| 2026-08-26 | Phase 8/I | Hardened container write boundaries: Compose now runs PantryOS with `read_only: true`, `/tmp` tmpfs, and `no-new-privileges`; the container sets `PANTRYOS_DATA_DIR=/app/data` and `PANTRYOS_BACKUP_DIR=/app/data/backups`; CLI and server startup enforce those path allowlists when configured while checkout use remains flexible without the env vars | `python scripts/check.py` -> python compile: 31 files passed; 55 tests passed; javascript syntax passed; `docker compose config` -> `read_only: true`, `/tmp:mode=1777,size=64m`, `PANTRYOS_DATA_DIR=/app/data`, `PANTRYOS_BACKUP_DIR=/app/data/backups`; `git diff --check` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 55 tests passed; Docker path smoke: `/app` write -> `OSError`, `/tmp` write -> writable, `/app/data` write -> writable, backup to `/app/data/backups/policy-smoke.sqlite3` -> rc 0, backup to `/tmp/policy-smoke.sqlite3` -> rc 1 `Backup output path must be inside /app/data/backups`, DB `/tmp/policy-smoke.sqlite3` -> rc 1 `Database path must be inside /app/data` | Broader image hardening and final release readiness docs remain pending |
| 2026-08-26 | Phase 7/H | Added a Home Assistant runtime coordinator/cache stored on `entry.runtime_data`, switched sensors to read coordinator snapshots without direct client I/O in entity properties, routed service actions through mutation-plus-refresh coordinator calls, scheduled bounded event-audit polling with recovery refresh, and expanded diagnostics with coordinator revision/error status | `python scripts/check.py` -> python compile: 32 files passed; 57 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose config` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 57 tests passed; Docker runtime smoke `docker inspect pantryos` -> `running healthy`; authenticated `GET /api/v1/dashboard` -> revision 12, total_items 7 | Full HA runtime harness, config reconfigure/reauth flows, service lifecycle tests against Home Assistant, and long-lived SSE/WebSocket push fanout remain pending |
| 2026-08-26 | Phase 6/F | Replaced average-only price comparison with a documented recent-median compatible-unit anomaly policy, added product price `analysis` output with latest status/baseline/ratio/sample evidence, and documented the threshold/window in `docs/operations/price_anomalies.md` | `python scripts/check.py` -> python compile: 32 files passed; 58 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose config` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 58 tests passed; Docker runtime smoke through receipts and `GET /api/v1/products/{id}/prices` -> `status=high baseline=1.10 ratio=1.82 sample=3 unit=count`; container `docker inspect pantryos` -> `running healthy` | Receipt image/OCR ingestion, camera barcode feature detection, richer browser price-anomaly UI, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 6/G | Added browser barcode camera feature detection as a progressive enhancement: the scanner panel appears in the barcode workflow, uses `BarcodeDetector` with environment-facing `getUserMedia` when available, writes detected values into the existing barcode form, stops media tracks on capture/stop/logout, and preserves manual barcode entry when camera scanning is unavailable | `python scripts/check.py` -> python compile: 32 files passed; 58 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose config` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 58 tests passed; Docker static smoke `GET /`, `/app.js`, `/styles.css` -> `indexScanner=True appDetector=True appMedia=True cssPanel=True`; container `docker inspect pantryos` -> `running healthy` | Real-camera browser automation, receipt image/OCR ingestion, richer browser price-anomaly UI, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 6/G | Added browser purchase-history and price-analysis panel: authenticated browser sessions now load committed purchases, inspect purchase lines, and fetch the Core product price analysis through a browser-session `/api/products/{id}/prices` route without storing the setup token in client state | `python scripts/check.py` -> python compile: 32 files passed; 58 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose config` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 58 tests passed; Docker static smoke `GET /`, `/app.js`, `/styles.css` -> `purchaseHistory=True priceAnalysis=True appPurchases=True appPrices=True cssDetail=True`; container `docker inspect pantryos` -> `running healthy` | Receipt image/OCR ingestion, receipt upload retention policy, final release docs, independent review, and broader browser accessibility/camera coverage remain pending |
| 2026-08-26 | Phase 2/G | Added browser inventory move reachability: each inventory row now exposes a destination field with known-location suggestions and a Core-backed Move action, so browser inventory lifecycle controls cover add, consume, move, and discard without toast-only stubs | `python scripts/check.py` -> python compile: 32 files passed; 58 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose config` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 58 tests passed; Docker static smoke `GET /`, `/app.js`, `/styles.css` -> `knownLocations=True moveLocation=True moveRoute=True moveCss=True`; Docker browser-session HTTP smoke add+move+state -> `created=Move Smoke Beans movedLocation=Kitchen/Refrigerator/Top Shelf stateLocation=Kitchen/Refrigerator/Top Shelf revisionAdvanced=True`; container `docker inspect pantryos` -> `running healthy` | Richer product/recipe/settings editing UI, receipt image/OCR ingestion, receipt upload retention policy, final release docs, independent review, and broader browser accessibility/camera coverage remain pending |
| 2026-08-26 | Phase 4/G/J | Added recipe edit and retire workflows: Core now updates recipes by stable ID, preserves identity across renames, rewrites ingredients transactionally, soft-retires unused recipes, rejects deletion of recipes referenced by active meal plans, and the browser recipe form now supports edit/cancel/delete controls backed by authenticated routes documented in OpenAPI | `python scripts/check.py` -> python compile: 32 files passed; 58 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose config` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/run_tests.py` -> 58 tests passed; Docker static smoke `GET /`, `/app.js` -> `recipeTitle=True recipeCancel=True recipeEdit=True recipeDelete=True recipePatch=True`; Docker browser-session HTTP smoke create+patch+delete+state -> `updated=Smoke Recipe Deluxe minutes=9 deletedOk=True oldVisible=False deletedVisible=False`; container `docker inspect pantryos` -> `running healthy` | Richer product/settings UI, receipt image/OCR ingestion, receipt upload retention policy, final release docs, independent review, and broader browser accessibility/camera coverage remain pending |
| 2026-08-26 | Phase 2/G/I/J | Added product settings/catalog workflow: Core now updates product metadata by stable product ID, the browser exposes category/default-unit/min-stock/preferred-location/shelf-life settings from the shared dashboard product list, browser and `/api/v1` PATCH routes persist through Core, and OpenAPI documents the product update contract; `scripts/check.py` now compiles to a temporary cache so the read-only Docker image can run the plain check command | `python scripts/check.py` -> python compile: 32 files passed; 58 tests passed; javascript syntax passed; `git diff --check` -> passed; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 58 tests passed, javascript syntax skipped because node is not in image; Docker readiness `GET /api/v1/health/ready` -> `{"status":"ready"}`; Docker static smoke `GET /`, `/app.js` -> `productSettings=True renderProductSettings=True productPatch=True dataProductSave=True`; Docker browser-session HTTP smoke add+PATCH product -> `product=Settings Smoke Oats category=Smoke min=5 unit=count shelf=30 revision=32`; container `docker inspect pantryos` -> `running healthy` | Receipt image/OCR ingestion, receipt upload retention policy, final release docs, independent review, and broader browser accessibility/camera coverage remain pending |
| 2026-08-26 | Phase 6/8/I/J | Added documented receipt upload retention cleanup: Core `purge_receipt_uploads` dry-runs and purges old `uploaded`, `review`, or `rejected` receipt payload files, marks metadata `purged`, preserves committed receipt evidence, excludes purged payloads from upload-inclusive backup archives, refuses purge paths outside the PantryOS data directory, and lets a same-content re-upload reactivate the existing metadata row; CLI exposes `purge-receipts` with `--older-than-days`, repeated `--status`, and `--dry-run` | `python scripts/check.py` -> python compile: 32 files passed; 61 tests passed; javascript syntax passed; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 61 tests passed, javascript syntax skipped because node is not in image; Docker readiness `GET /api/v1/health/ready` -> `{"status":"ready"}`; Docker CLI smoke on `/app/data/retention-smoke.sqlite3`: stale rejected receipt dry-run -> `eligible_count=1 purged_count=0`, real purge -> `purged_count=1 deleted_files=1 revision=6`, row/file check -> `status=purged path_exists=False`; container `docker inspect pantryos` -> `running healthy` | Full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 6/8/F/G/I/J | Added local receipt image ingestion and OCR: Core now accepts private `image/png` and `image/jpeg` uploads via JSON `content_base64`, validates MIME magic, decoded byte size, image dimensions/pixels, and OCR timeout, extracts images through the local `tesseract` subprocess outside the SQLite write transaction, keeps review-before-commit semantics, and the browser receipt workflow can choose an image file; Docker image installs `tesseract-ocr` and OpenAPI/docs describe the image contract. | `python scripts/check.py` -> python compile: 32 files passed; 64 tests passed; javascript syntax passed; `git diff --check` -> passed with CRLF notices only; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built with `tesseract-ocr`; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 64 tests passed, javascript syntax skipped because node is not in image; Docker readiness `GET /api/v1/health/ready` -> `ready`; live Docker OCR smoke generated PNG upload/extract -> `mime=image/png status=review store=OCR Market item=OCR Beans quantity=2`; container `docker inspect pantryos` -> `running healthy`. | Broader real-world OCR corpus coverage, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 7/H/J | Added Home Assistant reconfigure and reauth flows that validate replacement Core URL/token pairs against `/api/v1/instance`, preserve the returned PantryOS instance unique ID, update and reload the existing entry, and distinguish setup authentication failures with `ConfigEntryAuthFailed`; added dependency-light HA runtime harness tests that import the integration with fake HA modules and exercise setup, runtime_data, service registration/action refresh, event-poll refresh, unload cleanup, auth-failure mapping, reconfigure, and reauth. Removed the placeholder manifest documentation URL and documented the HA recovery behavior. | `python scripts/check.py` -> python compile: 32 files passed; 66 tests passed; javascript syntax passed; placeholder scan `rg github.com/example|https://example|placeholder URL|fake response|hard-coded success simulation|toast-only stub custom_components docs README.md app tests src` -> no current placeholder/fake matches outside acceptance/status text; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 66 tests passed, javascript syntax skipped because node is not in image; container `docker inspect pantryos` -> `running healthy`. | Full HA test harness against installed Home Assistant, long-lived SSE/WebSocket push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 7/8/H/I/J | Replaced the finite event replay response with a bounded authenticated SSE stream that accepts `after_revision`, `Last-Event-ID`, `timeout`, and `heartbeat`, replays missed Core events, polls for newer revisions until the bounded timeout, and emits heartbeats. Home Assistant now tries bounded SSE stream refresh through the coordinator before falling back to the event audit and snapshot recovery path; OpenAPI documents the stream parameters. | `python scripts/check.py` -> python compile: 32 files passed; 68 tests passed; javascript syntax passed; `git diff --check` -> passed with CRLF notices only; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 68 tests passed, javascript syntax skipped because node is not in image; live Docker SSE smoke after readiness -> `ready=True contentType=text/event-stream; charset=utf-8 hasAdd=True hasHeartbeat=True startRevision=35`; `docker inspect pantryos` -> `running healthy`. | Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 8/I/G/J | Hardened browser session deployment behavior: browser sessions are file-backed by default in `browser_sessions.json` beside the SQLite database, survive PantryOS process restart without persisting the setup token, can be explicitly forced to memory-only, and support `PANTRYOS_BROWSER_SECURE_COOKIES=true` plus `X-Forwarded-Proto: https` reporting for HTTPS/Secure-cookie deployments. Compose and `.env.example` expose the secure-cookie flag, and README documents persistence, TTL, memory mode, and reverse-proxy behavior. | `python scripts/check.py` -> python compile: 32 files passed; 70 tests passed; javascript syntax passed; `git diff --check` -> passed with CRLF notices only; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token PANTRYOS_BROWSER_SECURE_COOKIES=true` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 70 tests passed, javascript syntax skipped because node is not in image; live Docker restart smoke with secure cookies -> `readyAfterRestart=True secureCookie=True authenticated=True item=Docker Persistent Session Rice ...`. | Token setup UX polish, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 8/G/J | Added `scripts/browser_smoke.cjs` Playwright automation for the browser vertical slice across phone (`390x844`) and kitchen-tablet (`1024x768`) viewports; it starts PantryOS on a temporary SQLite database, signs in through the browser session flow, checks for horizontal overflow and critical form/control accessibility issues, and exercises inventory add/delete keyboard focus, unknown-barcode manual mapping, recipe add/edit/cancel, meal planning, cooking completion with leftovers, receipt review/commit, and purchase visibility. Fixed the inventory filter accessible name and included the smoke script in `scripts/check.py` JavaScript syntax coverage. | `python scripts/check.py` -> python compile: 32 files passed; 70 tests passed; javascript syntax: 2 files passed; browser smoke command with bundled Python/Node -> `browser smoke passed` for `phone` and `kitchen-tablet`; `git diff --check` -> passed with CRLF notices only; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 70 tests passed, javascript syntax skipped because node is not in image; `docker inspect pantryos --format ...` -> `running healthy`. | This is deterministic viewport/workflow smoke with custom critical accessibility checks, not full axe coverage, real-camera automation, or an installed Home Assistant end-to-end browser harness; token setup UX polish, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 6/G/J | Added deterministic browser camera-scanner coverage: the barcode scanner now runs through an injectable adapter while preserving the native `BarcodeDetector` + `getUserMedia` implementation, and `scripts/browser_smoke.cjs` installs a mock adapter that exercises camera start, capture, barcode field population, and media-track cleanup on both target viewports. | `python scripts/check.py` -> python compile: 32 files passed; 75 tests passed; javascript syntax: 2 files passed; Playwright browser smoke with mocked scanner adapter -> camera capture path plus existing browser vertical slice passed for `phone` and `kitchen-tablet`; `git diff --check` -> passed with CRLF notices only; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 75 tests passed, javascript syntax skipped because node is not in image; live Docker static smoke -> `ready=True adapter=True nativeAdapter=True detector=True trackStop=True cameraButton=True health=running healthy`. | Full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 2/5/G/J | Added first-class location management: Core now lists and updates active hierarchical location records by stable ID, rejects empty names, duplicate sibling names, invalid location types, self-parent moves, and descendant-parent cycles, and preserves inventory lot `location_id`/lot version while rename/move changes resolved paths. Versioned and browser-session APIs expose location listing/update, OpenAPI documents the contract, and the browser settings rail now includes a location editor with parent/type/temperature metadata controls. | `python scripts/check.py` -> python compile: 32 files passed; 75 tests passed; javascript syntax: 2 files passed; Playwright browser smoke -> `/api/locations/{id}` settings save exercised in both `phone` and `kitchen-tablet` viewports and `browser smoke passed`; `git diff --check` -> passed with CRLF notices only; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 75 tests passed, javascript syntax skipped because node is not in image; live Docker location smoke -> `ready=True locationIdStable=True lotVersionStable=True renamedPath=Docker Location Smoke .../Fridge/Shelf ... cycleRejected=True health=running healthy`. | Full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 2/5/G/H/J | Added first-class inventory lot opening: Core `open_lot` records one `OPEN` event, is idempotent on repeat calls, rejects inactive lots, exposes `opened_at` in item responses, applies product `opened_shelf_life_days` without extending an earlier expiration, and honors already-opened lot creation. Versioned API, browser compatibility route, browser inventory Open button, HA API client, `open_item` service, services metadata, and OpenAPI contract were updated. | `python scripts/check.py` -> python compile: 32 files passed; 73 tests passed; javascript syntax: 2 files passed; Playwright browser smoke -> `/api/items/{id}/open` exercised in both `phone` and `kitchen-tablet` viewports and `browser smoke passed`; `git diff --check` -> passed with CRLF notices only; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose build` -> built; `docker compose run --rm pantryos python scripts/check.py` -> python compile: 32 files passed, 73 tests passed, javascript syntax skipped because node is not in image; live Docker open smoke -> `ready=True opened=True openedAgain=False expires=2026-08-29 revisionStable=True health=running healthy`. | Broader browser accessibility/camera coverage, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 7/H/J | Added Home Assistant example automations for use-soon notification, grocery arrival count, cooking mode, and freezer risk/value alert; `pantryos_updated` HA events now include bounded recent event metadata (`events`, `event_types`, revision cursors) so automations can react to Core events such as `cooking.started`. Docker packaging now copies `docs/` so container checks include the automation contract docs. | `python scripts/check.py` -> python compile: 32 files passed; 77 tests passed; javascript syntax: 2 files passed; Playwright browser smoke with bundled Python/Node -> `browser smoke passed` for `phone` and `kitchen-tablet`; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose up -d --build --force-recreate` -> image rebuilt and container started; `docker exec pantryos python scripts/check.py` -> python compile: 32 files passed, 77 tests passed, javascript syntax skipped because node is not in image; Docker readiness `GET /api/v1/health/ready` -> `{"status":"ready"}`; `docker inspect pantryos` -> `running healthy`. | Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, token setup UX polish, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 8/G/I/J | Polished browser token setup UX: `/api/session` reports non-secret `setup_token_configured` metadata, the sign-in panel starts disabled until session configuration is known, displays local cookie policy, gives specific missing-token and invalid-token errors, preserves problem codes in client errors, supports a show/hide token control, and keeps token material out of persisted session state. | `python scripts/check.py` -> python compile: 32 files passed; 78 tests passed; javascript syntax: 2 files passed; Playwright browser smoke with bundled Python/Node -> setup-token status plus show/hide toggle and existing vertical slice passed for `phone` and `kitchen-tablet`; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose up -d --build --force-recreate` -> image rebuilt and container started; `docker exec pantryos python scripts/check.py` -> python compile: 32 files passed, 78 tests passed, javascript syntax skipped because node is not in image; live Docker `GET /api/session` -> `setup_token_configured=true`, `cookie.same_site=Lax`, `cookie.secure=false`; `docker inspect pantryos` -> `running healthy`. | Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 3/4/5/8/J | Added `scripts/smoke_e2e.py`, a scripted release demo that starts a temporary PantryOS server, signs in through the browser session surface, adds an expiring lot through `/api/items`, verifies the same lot through the Home Assistant API client, creates/plans a recipe, adds missing shopping demand, completes a purchase, starts/completes cooking with confirmed allocation and leftovers, verifies browser-visible meal plan/leftovers/use-soon state, and checks cooking events. Registered the script in the dependency-free test runner and documented the command in README. | `PANTRYOS_API_TOKEN=e2e-smoke-token python scripts/smoke_e2e.py` -> `ok=true`, browser-created lot id matched HA-synced lot id, purchase/session ids returned, use-soon included `Smoke Rice` and `Smoke Rice Bowl Leftovers`; `python scripts/check.py` -> python compile: 34 files passed; 79 tests passed; javascript syntax: 2 files passed; Playwright browser smoke with bundled Python/Node -> `browser smoke passed` for `phone` and `kitchen-tablet`; `docker compose config --quiet` with `PANTRYOS_API_TOKEN=local-dev-token` -> passed; `docker compose up -d --build --force-recreate` -> image rebuilt and container started; `docker exec pantryos python scripts/check.py` -> python compile: 34 files passed, 79 tests passed, javascript syntax skipped because node is not in image; `docker exec -e PANTRYOS_API_TOKEN=e2e-smoke-token pantryos python scripts/smoke_e2e.py` -> `ok=true` with matching browser/HA lot ids; Docker readiness `GET /api/v1/health/ready` -> `{"status":"ready"}`; `docker inspect pantryos` -> `running healthy`. | Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, and independent review remain pending |
| 2026-08-26 | Phase 8/I/J | Added `scripts/container_smoke.py`, a dependency-free Docker release smoke runner that validates Compose config, builds and starts the service, waits for readiness, checks the actual PantryOS process UID/GID and `no-new-privileges`, confirms read-only-root behavior, proves bearer-token auth, creates inventory and receipt state through the live API, restarts against the same Docker volume, creates a receipt-inclusive backup archive, restores it into a second database under `/app/data`, compares source/restored counts, and runs `scripts/check.py` inside the image. README now documents the command as the Docker release smoke. | `python scripts/check.py` -> python compile: 35 files passed; 80 tests passed; javascript syntax: 2 files passed. `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, readiness `ready`, created `Container Smoke Rice 20260826065932`, extracted/committed `Container Smoke Beans 20260826065932`, backup `/app/data/backups/container-smoke-20260826065932.zip`, restore database `/app/data/container-smoke-restored-20260826065932.sqlite3`, source/restored counts matched (`events=56`, `lots=25`, `products=22`, `purchases=7`, `recipes=4`), backup/restore and in-image verifier ran as UID/GID `10001:10001`, and JS syntax was skipped inside the image because node is not installed there. | Full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, full axe-style browser accessibility coverage, physical-device camera validation, release readiness PASS, broader image hardening review, and independent review remain pending |
| 2026-08-26 | Phase 8/G/J | Expanded `scripts/browser_smoke.cjs` from form/control checks into a broader axe-style browser accessibility audit across login, initial app, and completed workflow states. The audit now checks visible `main`/header/h1 landmarks, document language/title, duplicate IDs, missing ARIA references, unexpected roles, visible form labels, button names, media labels, live status regions, focusable target size, visible focus styling, and text contrast. The login surface now uses a visible `main` landmark, barcode camera preview has an accessible label, warning badge text was darkened after the audit caught a `3.76:1` contrast failure, and online browser smoke blocks service workers so offline-handler tests stay isolated. | `python scripts/check.py` -> python compile: 35 files passed; 80 tests passed; javascript syntax: 2 files passed. `PANTRYOS_API_TOKEN=browser-smoke-token node scripts/browser_smoke.cjs` with bundled Node/Python -> `browser smoke passed` for `phone` and `kitchen-tablet` with expanded login/initial/completed accessibility audits. `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, readiness `ready`, created `Container Smoke Rice 20260826071620`, extracted/committed `Container Smoke Beans 20260826071620`, backup `/app/data/backups/container-smoke-20260826071620.zip`, restore database `/app/data/container-smoke-restored-20260826071620.sqlite3`, source/restored counts matched (`events=64`, `lots=29`, `products=26`, `purchases=9`, `recipes=4`). | Physical-device camera validation, full installed-Home-Assistant harness, unbounded WebSocket-style push fanout, release readiness PASS, broader image hardening review, and independent review remain pending |
| 2026-08-26 | Phase 7/H/J | Added `scripts/ha_installed_smoke.py`, a Docker-backed installed Home Assistant runtime smoke that creates a temporary HA config, copies `custom_components/pantryos`, imports the integration inside the HA Python environment, and exercises setup, sensor platform forwarding, bounded event polling/update metadata, actual service schemas and add/open handlers, unload cleanup, and auth-failure recovery against a fake PantryOS client. README documents the command and `PANTRYOS_HA_IMAGE` override. | `python scripts/check.py` -> python compile: 36 files passed; 81 tests passed; javascript syntax: 2 files passed. `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true` against Home Assistant `2026.8.3`; services registered/removed: `add_item`, `consume_item`, `delete_item`, `discard_item`, `move_item`, `open_item`, `add_recipe`, `plan_meal`, `add_shopping_item`, `add_missing_to_shopping_list`, `rebuild_shopping`, `promote_suggested_purchases`, `start_cooking`, `complete_cooking`, and `cancel_cooking`; `last_event_types=[cooking.started]`, `last_revision=4`. | Full live HA Core config-entry UI/end-to-end harness, physical-device camera validation, unbounded WebSocket-style push fanout, release readiness PASS, broader image hardening review, and independent review remain pending |
| 2026-08-26 | Phase 7/H/J | Replaced the Home Assistant interval-style event refresh with a continuous background SSE subscription task stored on `entry.runtime_data`; the coordinator now loops across bounded stream windows, refreshes snapshots on newer revisions, emits `pantryos_updated` through an async callback, falls back to event-audit/snapshot recovery on stream errors, and cancels the task during unload. | `python scripts/check.py` -> python compile: 36 files passed; 82 tests passed; javascript syntax: 2 files passed. `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true` against Home Assistant `2026.8.3`, `last_event_types=[cooking.started]`, `last_revision=4`, `stream_task_cancelled=true`, sensor platform forwarded, and all PantryOS services registered/removed. | Full live HA Core config-entry UI/end-to-end harness, physical-device camera validation, release readiness PASS, broader image hardening review, and independent review remain pending |
| 2026-08-26 | Phase 8/J | Added `scripts/release_readiness.py` and generated `docs/release/RELEASE_READINESS.md`. The readiness artifact parses the binding acceptance criteria and implementation status ledger, records required release commands, latest evidence, open phase gates, and residual risks, and stays `NOT READY` until phase gates and blockers are cleared. `scripts/check.py` now enforces that the generated readiness document is current. | `python scripts/release_readiness.py --write --json` -> decision `NOT READY`, acceptance criteria `85`, phase gates `11`, open phase gates `9`. `python scripts/check.py` -> python compile: 37 files passed; 83 tests passed; `release readiness: current`; javascript syntax: 2 files passed. | Release readiness PASS, physical-device camera validation, full live HA Core config-entry UI/end-to-end harness, broader image hardening review, and independent review remain pending |
| 2026-08-26 | Phase 8/I/J | Added `scripts/image_hardening_audit.py` and hardened Compose with `cap_drop: ALL`, minimal startup-only `CHOWN`/`SETGID`/`SETUID` capability additions, and `pids_limit: 256`; the audit checks Dockerfile, packaged `.dockerignore`, rendered Compose, image metadata, live container settings, writable mounts, runtime env, UID/GID, `NoNewPrivs`, and process effective capabilities. | `python scripts/image_hardening_audit.py --skip-live` -> `ok=true`, `check_count=27`; `docker compose up -d --build --force-recreate pantryos` -> image rebuilt and container started; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=43`, container healthy, `CapDrop=ALL`, minimal startup `CapAdd`, `PidsLimit=256`, single writable `/app/data` volume, UID/GID `10001`, and zero effective Linux capabilities after privilege drop; `python scripts/run_tests.py` -> 86 tests passed; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, backup `/app/data/backups/container-smoke-20260826082654.zip`, restore database `/app/data/container-smoke-restored-20260826082654.sqlite3`, source/restored counts matched (`events=72`, `lots=33`, `products=30`, `purchases=11`, `recipes=4`), and the in-image verifier completed with JS syntax skipped because node is not installed there. | Physical-device camera validation, full live HA Core config-entry UI/end-to-end harness, release readiness PASS, supply-chain hardening items such as digest pinning/SBOM/signature policy, and independent review remain pending |
| 2026-08-26 | Phase 8/I/J | Pinned the Docker base image to `python:3.12-slim@sha256:7a8b475003c4fe15a2cd4e55e5cfc2f3560bdc9333d624f24cdd6d4340fd7a17`; added `scripts/supply_chain_audit.py`, `docs/release/container-image.lock.json`, `docs/release/pantryos-image-sbom.spdx.json`, and `docs/release/SUPPLY_CHAIN.md` so the release candidate has a deterministic lock, SPDX-style SBOM, required `tesseract-ocr` package evidence, and signing-key handling policy. | `docker compose up -d --build --force-recreate pantryos` -> image rebuilt from the digest-pinned base and container started; `python scripts/supply_chain_audit.py --write` -> `ok=true`, source files `61`, OS packages `157`, base digest `sha256:7a8b475003c4fe15a2cd4e55e5cfc2f3560bdc9333d624f24cdd6d4340fd7a17`; `python scripts/supply_chain_audit.py` -> `ok=true`; `python scripts/image_hardening_audit.py --skip-live` -> `ok=true`, `check_count=31`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, backup `/app/data/backups/container-smoke-20260826084239.zip`, restore database `/app/data/container-smoke-restored-20260826084239.sqlite3`, source/restored counts matched (`events=76`, `lots=35`, `products=32`, `purchases=12`, `recipes=4`), and the in-image verifier completed with JS syntax skipped because node is not installed there. | Physical-device camera validation, full live HA Core config-entry UI/end-to-end harness, release readiness PASS, published-image signature verification after final release tag, and independent review remain pending |
| 2026-08-26 | Phase 7/H/J | Added the first `scripts/ha_core_live_smoke.py` Docker-backed Home Assistant Core harness that mounted the local custom integration into a temporary HA config, bootstrapped real Home Assistant Core with `async_setup_hass`, created a real config entry, verified PantryOS sensors/services, called `pantryos.add_item`, then performed a direct Core API item write and waited for the HA event-stream listener to advance the coordinator revision. This initial live-token execution path was superseded by the isolated Core runner recorded below. | `python scripts/check.py` -> python compile: 42 files passed; 90 tests passed; release readiness current; javascript syntax: 2 files passed. `python scripts/supply_chain_audit.py --write` -> `ok=true`, source files `62`, OS packages `157`. `docker compose up -d --build --force-recreate pantryos` -> image rebuilt and container started. `python scripts/supply_chain_audit.py` -> `ok=true`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true` against Home Assistant `2026.8.3`, services registered/removed, `last_event_types=[cooking.started]`, `stream_task_cancelled=true`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, backup `/app/data/backups/container-smoke-20260826090337.zip`, restore database `/app/data/container-smoke-restored-20260826090337.sqlite3`, source/restored counts matched (`events=80`, `lots=37`, `products=34`, `purchases=13`, `recipes=4`). The initial live-token execution path was not run and was superseded by the isolated Core runner recorded below. | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 2/8/J | Tightened the OpenAPI 3.1 contract from generic success envelopes to concrete per-resource response schemas for instance/dashboard snapshots, inventory lots, consume/open/discard actions, events, barcode lookup/mapping, locations, waste, purchases, product price analysis, shopping, receipts, recipes, meal planning, cooking sessions, leftovers, product/location updates, and delete responses. Empty POST actions no longer claim a JSON request body, and contract tests now reject unresolved `$ref`s or generic `ObjectEnvelope` success responses. | `python scripts/check.py` -> python compile: 42 files passed; 93 tests passed; release readiness current; javascript syntax: 2 files passed. `python scripts/supply_chain_audit.py --write` -> `ok=true`, source files `62`, OS packages `157`. `docker compose up -d --build --force-recreate pantryos` -> image rebuilt and container started. `python scripts/supply_chain_audit.py` -> `ok=true`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; live Docker OpenAPI probe -> `dashboard=#/components/schemas/DashboardResponse`, `lot=#/components/schemas/InventoryLotResponse`, `receipt=#/components/schemas/ReceiptCommitResponse`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, backup `/app/data/backups/container-smoke-20260826092552.zip`, restore database `/app/data/container-smoke-restored-20260826092552.sqlite3`, source/restored counts matched (`events=84`, `lots=39`, `products=36`, `purchases=14`, `recipes=4`). | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 8/J | Added `scripts/release_artifact_audit.py`, a dependency-free release artifact debt gate that scans release-critical app, integration, script, source, test, README, and handoff/home-assistant docs for completion-critical TODO/FIXME/HACK markers, placeholder URLs, fake responses, hard-coded success/stub language, disabled tests, not-implemented markers, and skipped release-check paths. The audit excludes generated/status-ledger artifacts, requires every intentional match to be path-aware with a non-blocking reason, has tests for both injected debt and allowance coverage, is called from `scripts/check.py`, is listed in release readiness required commands, and is documented in README. | `python scripts/release_artifact_audit.py` -> `ok`, `scanned_files=75`, `allowed_matches=21`; `python scripts/check.py` -> python compile: 43 files passed; 95 tests passed; `release readiness: current`; `release artifact audit: current`; javascript syntax: 2 files passed. `python scripts/supply_chain_audit.py --write` -> `ok=true`, source files `63`, OS packages `157`; `docker compose up -d --build --force-recreate pantryos` -> image rebuilt and container started; `python scripts/supply_chain_audit.py` -> `ok=true`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, backup `/app/data/backups/container-smoke-20260826094249.zip`, restore database `/app/data/container-smoke-restored-20260826094249.sqlite3`, source/restored counts matched (`events=88`, `lots=41`, `products=38`, `purchases=15`, `recipes=4`), and the in-image verifier completed with the documented Node JS syntax skip. | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 6/F/J | Added `scripts/receipt_ocr_corpus_smoke.py`, a deterministic Docker OCR corpus smoke that runs inside the PantryOS container without tokens, generates three receipt images with `text2image`, extracts them through the local `tesseract` boundary used by PantryOS image receipts, parses the OCR text through PantryOS Core, and commits each result into a temporary SQLite database. README documents the command and the fact that it does not mutate the live PantryOS database; release readiness now lists it as a required command. An earlier live-token `ha_core_live_smoke.py` attempt was superseded by the isolated HA Core smoke runner recorded below. | `python scripts/check.py` -> python compile: 44 files passed; 96 tests passed; `release readiness: current`; `release artifact audit: current`; javascript syntax: 2 files passed. `python scripts/receipt_ocr_corpus_smoke.py --timeout 180` -> `ok=true`, `case_count=3`, cases `standard_single_item`, `multi_item_with_barcode`, and `header_footer_noise`, stores `OCR Market`, `Corner Grocery`, and `Farmers Market`, parsed items `OCR Beans`, `Milk`, `Bananas`, `Tomatoes`, and `Eggs`, totals `5`, `6.49`, and `9`. `python scripts/supply_chain_audit.py --write` -> `ok=true`, source files `64`, OS packages `157`; `docker compose up -d --build --force-recreate pantryos` -> image rebuilt and container started; `python scripts/supply_chain_audit.py` -> `ok=true`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, process UID `10001`, backup `/app/data/backups/container-smoke-20260826095611.zip`, restore database `/app/data/container-smoke-restored-20260826095611.sqlite3`, source/restored counts matched (`events=96`, `lots=45`, `products=42`, `purchases=17`, `recipes=4`), and the in-image verifier completed with the documented Node JS syntax skip. The earlier live-token `ha_core_live_smoke.py` path was superseded before execution by the isolated HA Core smoke runner recorded below. | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 0/8/J | Added reproducible Python dev-tooling gates to `pyproject.toml` and README: ruff formatting/lint policy, mypy package-base configuration for `src` plus the Home Assistant custom component, pytest, and subprocess-aware coverage with an 80% release threshold. Fixed the direct `scripts/smoke_e2e.py`, `scripts/check.py`, `scripts/run_tests.py`, and `app/server.py` import bootstraps so the checkout wrapper `scripts/pantryos.py` cannot shadow the installed `pantryos` package under a venv or script runner, and tightened the small typing issues surfaced by mypy. | `.\.venv\Scripts\python.exe -m pip install -e '.[dev]'` -> installed dev tooling; `.\.venv\Scripts\python.exe -m ruff format --check .` -> 70 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components\pantryos` -> no issues in 16 source files; `.\.venv\Scripts\python.exe -m pytest -q` -> 96 passed; `.\.venv\Scripts\python.exe -m coverage run -m pytest -q` -> 96 passed; `.\.venv\Scripts\python.exe -m coverage report` -> total coverage 83%, above 80% gate; `python scripts/check.py` -> python compile 44 files passed, 96 tests passed, release readiness current, release artifact audit current, javascript syntax 2 files passed. | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 0/8/J | Reverified the reproducible dev-tooling slice against Docker after adding the local venv gates and deterministic import bootstraps. The rebuilt Compose service is running at `http://127.0.0.1:8765` with the local development token. | `PANTRYOS_API_TOKEN=local-dev-token docker compose up -d --build --force-recreate pantryos` -> rebuilt `pantryos-pantryos:latest` and restarted `pantryos`; `python scripts/supply_chain_audit.py --write` and `python scripts/supply_chain_audit.py` -> `ok=true`, source files `70`, OS packages `157`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python scripts/receipt_ocr_corpus_smoke.py --timeout 180` -> `ok=true`, `case_count=3`, parsed `OCR Beans`, `Milk`, `Bananas`, `Tomatoes`, and `Eggs`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, UID `10001`, backup `/app/data/backups/container-smoke-20260826102812.zip`, restored counts matched source counts (`events=104`, `lots=49`, `products=46`, `purchases=19`, `recipes=4`). | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 7/H/J | Reworked `scripts/ha_core_live_smoke.py` so the required live Home Assistant Core proof no longer needs the user's `PANTRYOS_API_TOKEN` or mutates the user's PantryOS database. The smoke now creates a disposable Docker network, starts an isolated PantryOS Core container from the local image with a fixed non-secret smoke token and disposable volume, mounts the local custom integration into a real Home Assistant Core container, creates a real config entry, verifies sensors/services, calls `pantryos.add_item`, writes a direct Core item through the HA runtime client, waits for coordinator revision movement, unloads the entry, and removes the generated Core container/network/volume. | `python scripts/ha_core_live_smoke.py --timeout 180` -> `ok=true`, `core_mode=isolated`, Home Assistant `2026.8.3`, `sensor_count=16`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `event_count=1`, `unloaded=true`, `remaining_services=[]`; cleanup checks `docker ps/network/volume ls --filter name=pantryos-ha-core-smoke` -> no generated containers, networks, or volumes remained. | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 7/8/H/I/J | Completed a Docker-backed release verification pass for the isolated Home Assistant Core smoke and hardened local tooling. `scripts/ha_core_live_smoke.py` now avoids live user tokens/databases by creating a disposable PantryOS Core container/network/volume with a fixed non-secret smoke token, the browser smoke now ignores invalid directory-style Python paths and requires a Python 3.12+ runtime, and the status/readiness wording records the old live-token HA path as superseded. | `.\.venv\Scripts\python.exe -m ruff format --check .` -> 70 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components\pantryos scripts\ha_core_live_smoke.py` -> no issues in 17 source files; `.\.venv\Scripts\python.exe -m pytest -q` -> 96 passed; `.\.venv\Scripts\python.exe -m coverage report` -> total coverage 83%, above 80% gate; `PANTRYOS_API_TOKEN=local-dev-token docker compose up -d --build --force-recreate pantryos` -> rebuilt `pantryos-pantryos:latest` and restarted `pantryos`; `python scripts/supply_chain_audit.py --write` -> `ok=true`, source files `70`, OS packages `157`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python scripts/check.py` -> python compile 44 files passed, 96 tests passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `PANTRYOS_API_TOKEN=e2e-smoke-token python scripts/smoke_e2e.py` -> `ok=true`, revision `22`, matching browser/HA lot id, leftover count `2`; `PANTRYOS_API_TOKEN=browser-smoke-token node scripts/browser_smoke.cjs` -> browser smoke passed for `phone` and `kitchen-tablet`; `python scripts/ha_installed_smoke.py --timeout 240` -> `ok=true`, Home Assistant `2026.8.3`, `stream_task_cancelled=true`; `python scripts/receipt_ocr_corpus_smoke.py --timeout 180` -> `ok=true`, `case_count=3`; `PANTRYOS_API_TOKEN=local-dev-token python scripts/container_smoke.py` -> `ok=true`, Docker server `29.6.1`, UID `10001`, backup `/app/data/backups/container-smoke-20260826113158.zip`, source/restored counts matched (`events=112`, `lots=53`, `products=50`, `purchases=21`, `recipes=4`); `python scripts/ha_core_live_smoke.py --timeout 180` -> `ok=true`, isolated Home Assistant `2026.8.3`, `sensor_count=16`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `unloaded=true`, `remaining_services=[]`; cleanup checks for `pantryos-ha-core-smoke` containers/networks/volumes -> none remained. | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 1/2/8/J | Added `scripts/concurrency_smoke.py`, a dependency-free authenticated API durability smoke for A6, and sized the local `PantryHTTPServer` accept queue for the 20-request burst. The smoke starts a temporary threaded PantryOS Core server, seeds independent lots, releases 20 concurrent supported API mutations across inventory add/open/consume/discard endpoints, verifies every successful mutation in the final dashboard, checks revision and event-count deltas, and runs SQLite integrity verification against the resulting database. `scripts/check.py`, release readiness, and the Docker in-image verifier now include this smoke as a required gate. | `python scripts/concurrency_smoke.py` -> `ok=true`, `successful_mutations=20`, `baseline_revision=24`, `final_revision=44`, `event_count=44`, `product_count=27`, `active_lot_count=23`; `docker exec --user 10001:10001 pantryos python scripts/check.py` -> python compile 45 files passed, 97 tests passed, api concurrency smoke 20 mutations passed, release checks current; `python scripts/container_smoke.py` -> `ok=true`, `uid=10001`, `revision_after_restart=120`, restored counts matched source counts; `python -m pytest -q` -> 97 passed; `python scripts/supply_chain_audit.py --write` -> `ok=true`, `source_file_count=71`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`; `python -m mypy src custom_components\pantryos scripts\ha_core_live_smoke.py scripts\concurrency_smoke.py` -> success; `python scripts/check.py` -> passed. | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-driven HA frontend config-entry UI proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 7/H/J | Strengthened `scripts/ha_core_live_smoke.py` so the isolated live Home Assistant Core proof creates the PantryOS entry through Home Assistant's config-flow manager against the live disposable Core API instead of manually constructing a `ConfigEntry`. The smoke now records the flow result type and PantryOS instance unique ID before proving setup, services, event listener revision movement, sensors, unload, and cleanup. README and HA contract tests now require this config-flow path. | `python scripts/ha_core_live_smoke.py --timeout 240` -> `ok=true`, `config_flow_result_type=create_entry`, `entry_unique_id=inst_ca167d95cf7a4c329ccb390b3ce46896`, Home Assistant `2026.8.3`, `core_mode=isolated`, `revision_before=0`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `sensor_count=16`, `unloaded=true`, `remaining_services=[]`; cleanup checks `docker ps/network/volume ls --filter name=pantryos-ha-core-smoke` -> no generated containers, networks, or volumes remained; `python -m pytest tests/test_ha_contract.py -q` -> 11 passed; `python -m pytest -q` -> 97 passed; `python -m ruff format --check .` -> 71 files already formatted; `python -m ruff check .` -> all checks passed; `python -m mypy src custom_components\pantryos scripts\ha_core_live_smoke.py scripts\concurrency_smoke.py` -> success; `docker compose up -d --build --force-recreate pantryos` -> rebuilt image and restarted container; `docker exec --user 10001:10001 pantryos python scripts/check.py` -> python compile 45 files passed, 97 tests passed, api concurrency smoke 20 mutations passed, release checks current; `python scripts/container_smoke.py` -> `ok=true`, `uid=10001`, `revision_after_restart=124`, restored counts matched source counts; `python scripts/check.py` -> passed; `python scripts/supply_chain_audit.py --write` -> `ok=true`, `source_file_count=71`; `python scripts/image_hardening_audit.py` -> `ok=true`, `check_count=47`. | Physical-device camera/real-receipt validation, release readiness PASS, published-image signature verification after final release tag, browser-clicked HA frontend config-entry proof if required, and final independent review refresh remain pending |
| 2026-08-26 | Phase 7/8/H/I/J | Fixed the live Home Assistant Core release smoke false-pass gap by requiring the final HA `state_revision` and `total_items` sensor states to match the coordinator snapshot after a direct Core mutation. Added `scripts/manual_release_evidence.py`, a release gate for external proof that validates `docs/release/manual-validation.json` against the current Git commit and requires PASS evidence for physical barcode camera, real receipt OCR, published image signature, and independent full review checks. Release readiness now lists the manual evidence validator as a required release command. | `.\.venv\Scripts\python.exe scripts\ha_core_live_smoke.py --timeout 240` -> `ok=true`, Home Assistant `2026.8.3`, `config_flow_result_type=create_entry`, `revision_after_service=1`, `revision_after_core_push=2`, `state_revision_state=2`, `total_items_state=2`, `sensor_count=16`, `unloaded=true`; `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 100 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\.venv\Scripts\python.exe -m ruff format --check .` -> 72 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py` -> success; `.\.venv\Scripts\python.exe scripts\manual_release_evidence.py --json` -> `ok=false`, missing `docs/release/manual-validation.json` as the expected remaining release blocker. | Physical-device camera/real-receipt validation, published-image signature verification after final release tag, manual evidence JSON, release readiness PASS, and final independent review refresh remain pending |
| 2026-08-26 | Phase 8/I/J | Added an isolated release-review mode to `scripts/container_smoke.py` and made `python scripts/container_smoke.py --isolated` the required release command. The isolated mode writes a temporary Compose file under the checkout with the same hardened service contract, a generated container name, Docker-assigned host port, and generated data volume; it reruns the same live API, restart, backup/restore, and in-image verifier checks, then removes the generated Compose project resources. | `.\.venv\Scripts\python.exe scripts\container_smoke.py --isolated --build-timeout 360 --ready-timeout 120` -> `ok=true`, `isolated=true`, Docker server `29.6.1`, generated base URL `http://127.0.0.1:50820`, generated container `pantryos-container-smoke-91b02b6a901b`, generated volume `pantryos-container-smoke-data-91b02b6a901b`, UID `10001`, `revision_after_restart=4`, backup `/app/data/backups/container-smoke-20260826131152.zip`, restore database `/app/data/container-smoke-restored-20260826131152.sqlite3`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`), in-image verifier completed with documented Node JS syntax skip; cleanup checks for generated isolated smoke containers/networks/volumes -> none remained; `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 100 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\.venv\Scripts\python.exe -m ruff format --check .` -> 72 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py scripts/container_smoke.py` -> success. | Physical-device camera/real-receipt validation, published-image signature verification after final release tag, manual evidence JSON, release readiness PASS, and final independent review refresh remain pending |
| 2026-08-26 | Phase 4/6/7/8/J | Fixed the D9 generated shopping override gap and reconciled phase-gate status against current automated evidence and the superseding independent review for pre-fix HEAD `82d7f32`. Meal-plan shopping rebuild now refreshes generated quantities without silently reactivating user-suppressed or removed generated rows; explicit user reactivation restores the row to accepted active demand. Phases 2, 3, 4, 5, and 7 are now marked complete from accumulated automated evidence; Phase 6, Phase 8, independent review, and release PASS remain open for manual/external release proof. | `.\.venv\Scripts\python.exe -m pytest tests\core\test_sqlite_core.py::test_meal_plan_rebuild_preserves_suppressed_generated_demand_until_reactivated -q` -> 1 passed; `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 101 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\.venv\Scripts\python.exe -m ruff format --check .` -> 73 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py scripts/container_smoke.py` -> success in 21 source files; `.\.venv\Scripts\python.exe scripts\container_smoke.py --isolated --build-timeout 360 --ready-timeout 120` -> `ok=true`, Docker server `29.6.1`, generated base URL `http://127.0.0.1:57379`, generated container `pantryos-container-smoke-04a1643c62f9`, generated volume `pantryos-container-smoke-data-04a1643c62f9`, UID `10001`, `revision_after_restart=4`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`). `docs/reviews/2026-08-26-current-head-review.md` reviewed exact pre-fix HEAD `82d7f3284ea3457139e332c7cafb77a037ce3d52`, confirmed prior HA/container High findings resolved, found no open Critical/High findings, and reported release-blocking Medium gaps for physical barcode camera, representative real receipt OCR, published image signature, and missing manual release evidence JSON. | Physical-device camera/real-receipt validation, published-image signature verification after final tag, `docs/release/manual-validation.json`, final no-blocker independent review against the final commit, and release readiness PASS remain pending |
| 2026-08-26 | Phase 8/J | Hardened `scripts/manual_release_evidence.py` so the final manual evidence file cannot satisfy release gates with weak signature or review details. The validator now requires `published-image-signature` to use a `sha256:<64 lowercase hex>` digest and record a `cosign verify` command, and requires `independent-full-review` to point at an existing review artifact for the current commit with `decision=PASS`, `open_critical_high=0`, and `release_blocking_medium=0`. README and the release plan document these requirements. | `.\.venv\Scripts\python.exe -m pytest tests\test_release_smoke.py::test_manual_release_evidence_accepts_complete_current_commit_record tests\test_release_smoke.py::test_manual_release_evidence_rejects_weak_signature_and_review_records tests\test_release_smoke.py::test_manual_release_evidence_rejects_incomplete_records -q` -> 3 passed; `.\.venv\Scripts\python.exe scripts\check.py` -> python compile 46 files passed, 102 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current, javascript syntax 2 files passed; `.\.venv\Scripts\python.exe -m ruff format --check .` -> 73 files already formatted; `.\.venv\Scripts\python.exe -m ruff check .` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy src custom_components/pantryos scripts/ha_core_live_smoke.py scripts/concurrency_smoke.py scripts/manual_release_evidence.py scripts/release_readiness.py scripts/container_smoke.py` -> success in 21 source files; `.\.venv\Scripts\python.exe scripts\manual_release_evidence.py --json` -> `ok=false`, missing `docs/release/manual-validation.json` as the expected remaining release blocker. | Physical-device camera/real-receipt validation, published-image signature verification after final tag, `docs/release/manual-validation.json`, final no-blocker independent review against the final commit, and release readiness PASS remain pending |
| 2026-08-26 | Phase 8/I/J | Reverified the live Docker path now that Docker Desktop is available. The Compose service renders with the required token, rebuilds from the digest-pinned image, runs healthy at `http://127.0.0.1:8765`, serves host health/dashboard requests, executes the in-image release verifier as UID `10001`, and the isolated container smoke still proves disposable rebuild/start/API/restart/backup/restore/cleanup behavior. | `docker version` -> client/server `29.6.1`; `PANTRYOS_API_TOKEN=local-dev-token docker compose config` -> passed with hardened service contract and port `8765`; `PANTRYOS_API_TOKEN=local-dev-token docker compose up -d --build --force-recreate pantryos` -> rebuilt `pantryos-pantryos:latest` and started healthy `pantryos`; host `GET /api/v1/health/live` -> `live`; host `GET /api/v1/health/ready` -> `ready`; host authenticated `GET /api/v1/dashboard` -> revision `124`; `docker exec --user 10001:10001 pantryos python scripts/check.py` -> python compile 46 files passed, 102 tests passed, api concurrency smoke 20 mutations passed, release readiness current, release artifact audit current; `.\.venv\Scripts\python.exe scripts\container_smoke.py --isolated --build-timeout 360 --ready-timeout 120` -> `ok=true`, Docker server `29.6.1`, generated base URL `http://127.0.0.1:54650`, generated container `pantryos-container-smoke-1ef53960f7a9`, generated volume `pantryos-container-smoke-data-1ef53960f7a9`, UID `10001`, `revision_after_restart=4`, backup `/app/data/backups/container-smoke-20260826135437.zip`, source/restored counts matched (`events=4`, `lots=2`, `products=2`, `purchases=1`, `recipes=0`). | Physical-device camera/real-receipt validation, published-image signature verification after final tag, `docs/release/manual-validation.json`, final no-blocker independent review against the final commit, and release readiness PASS remain pending |
| 2026-08-26 | Phase 8/J | Added a guarded manual release evidence scaffold to reduce final-release operator error without weakening the release gate. `scripts/manual_release_evidence.py --print-template` now emits the exact current-commit JSON shape for all required external checks, and `--write-template` writes `docs/release/manual-validation.template.json` while refusing to overwrite the real `manual-validation.json` evidence file. README and the release plan document the flow from incomplete scaffold to concrete `PASS` evidence. | `.\.venv\Scripts\python.exe -m pytest tests\test_release_smoke.py::test_manual_release_evidence_template_is_current_commit_scaffold tests\test_release_smoke.py::test_manual_release_evidence_template_refuses_evidence_path tests\test_release_smoke.py::test_manual_release_evidence_accepts_complete_current_commit_record tests\test_release_smoke.py::test_manual_release_evidence_rejects_incomplete_records tests\test_release_smoke.py::test_manual_release_evidence_rejects_weak_signature_and_review_records -q` -> 5 passed; `.\.venv\Scripts\python.exe -m ruff check scripts\manual_release_evidence.py tests\test_release_smoke.py` -> all checks passed; `.\.venv\Scripts\python.exe scripts\manual_release_evidence.py --print-template` -> emitted schema_version `1`, release commit `da7e6567950423c48ed34af7c9659507d6b947ee`, and scaffold records for `physical-barcode-camera`, `real-receipt-ocr`, `published-image-signature`, and `independent-full-review`. | Physical-device camera/real-receipt validation, published-image signature verification after final tag, concrete `docs/release/manual-validation.json`, final no-blocker independent review against the final commit, and release readiness PASS remain pending |
| 2026-08-26 | Phase 6/8/J | Tightened the manual release evidence contract for the remaining physical barcode and representative real-receipt blockers. Final `physical-barcode-camera` evidence must now identify the app URL, separate known and unknown barcodes, the resolved product/lot result, and the manual fallback result. Final `real-receipt-ocr` evidence must now identify the real receipt source/capture environment, committed `receipt_` and `purchase_` IDs, positive committed lot count, and price-history visibility. | `.\.venv\Scripts\python.exe -m pytest tests\test_release_smoke.py::test_manual_release_evidence_template_is_current_commit_scaffold tests\test_release_smoke.py::test_manual_release_evidence_accepts_complete_current_commit_record tests\test_release_smoke.py::test_manual_release_evidence_rejects_incomplete_records tests\test_release_smoke.py::test_manual_release_evidence_rejects_weak_physical_and_receipt_records tests\test_release_smoke.py::test_manual_release_evidence_rejects_weak_signature_and_review_records -q` -> 5 passed; `.\.venv\Scripts\python.exe -m ruff check scripts\manual_release_evidence.py tests\test_release_smoke.py` -> all checks passed; `.\.venv\Scripts\python.exe -m mypy scripts\manual_release_evidence.py` -> success in 1 source file; `.\.venv\Scripts\python.exe scripts\manual_release_evidence.py --print-template` -> emitted `physical-barcode-camera` acceptance `F1`, `F2`, `G5`, `G6` with `known_barcode`, `known_result`, `unknown_barcode`, and `manual_fallback_result`, and `real-receipt-ocr` acceptance `F5`, `F6`, `F8` with `receipt_id`, `purchase_id`, `committed_lot_count`, and `price_history_result`. | Physical-device camera/real-receipt validation, published-image signature verification after final tag, concrete `docs/release/manual-validation.json`, final no-blocker independent review against the final commit, and release readiness PASS remain pending |
## Architectural decisions

Record ADR paths here as they are created.

| ADR | Status | Summary |
|---|---|---|
| docs/adr/0001-stdlib-core-foundation.md | Accepted | Use Python stdlib sqlite3 and explicit SQL migrations for the Phase 1 Core extraction; final API/auth/OpenAPI gates remain required |

## Open blockers

- `docs/release/manual-validation.json` is absent. The manual release gate requires PASS evidence for `physical-barcode-camera`, `real-receipt-ocr`, `published-image-signature`, and `independent-full-review` on the final release commit; signature evidence must include a `sha256:<digest>` plus passing `cosign verify`, and review evidence must prove `decision=PASS`, `open_critical_high=0`, `release_blocking_medium=0`, and matching commit.
- Physical-device barcode camera validation and representative real-receipt OCR/review/commit evidence remain pending. Deterministic browser/OCR automation exists, but these user-facing capture paths still need real-device/manual release artifacts. Final barcode evidence must cover separate known-barcode and unknown/manual-fallback outcomes; final real-receipt evidence must include committed receipt/purchase IDs, lot count, and price-history confirmation.
- Published-image signature verification remains pending until the final tag/image is published and verified.
- The independent review at `docs/reviews/2026-08-26-current-head-review.md` reviewed pre-D9 HEAD `82d7f3284ea3457139e332c7cafb77a037ce3d52`, found no open Critical/High issues, but still reports release-blocking Medium evidence gaps; J7 cannot pass until a final review against the final commit reports no release-blocking Medium findings.
- Release readiness remains `NOT READY` until the manual evidence JSON validates, the final image signature evidence exists, every required release command is rerun, readiness is regenerated as PASS, and a final no-blocker independent review is recorded.
